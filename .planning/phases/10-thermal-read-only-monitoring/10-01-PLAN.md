---
phase: 10-thermal-read-only-monitoring
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MacStatus/MacStatus/Readers/SMCReader.swift
  - MacStatus/MacStatus/Readers/ThermalReader.swift
  - MacStatus/MacStatus.xcodeproj/project.pbxproj
autonomous: true
requirements:
  - THERM-01
  - THERM-02
  - THERM-03
  - THERM-04
user_setup: []
must_haves:
  truths:
    - "D-01/D-13/D-14: CPU/SoC primary temperature is populated only from explicitly trusted Mac15,9 CPU/SoC keys; otherwise it is nil for UI N/A."
    - "D-02: ProcessInfo.thermalState is captured as a separate semantic state, never as a CPU/SoC temperature substitute."
    - "D-03/D-04/D-06: GPU and battery temperatures are independent optional fields; no secondary value can replace CPU/SoC."
    - "D-05/D-08: SSD, fan, write, popup, shell-out, and dependency paths are absent from the reader layer."
  artifacts:
    - path: "MacStatus/MacStatus/Readers/SMCReader.swift"
      provides: "read-only SMC raw/numeric/temperature decoding helpers"
      contains: "readRawValue"
    - path: "MacStatus/MacStatus/Readers/ThermalReader.swift"
      provides: "ThermalSnapshot, SystemThermalState, ThermalReader, ThermalSensorCatalog"
      contains: "ThermalReader"
    - path: "MacStatus/MacStatus.xcodeproj/project.pbxproj"
      provides: "ThermalReader.swift target membership"
      contains: "ThermalReader.swift in Sources"
  key_links:
    - from: "ThermalReader.readValue()"
      to: "SMCReader.readTemperatureCelsius(key:)"
      via: "read-only trusted SMC candidate reads"
      pattern: "readTemperatureCelsius"
    - from: "ThermalReader.readValue()"
      to: "ProcessInfo.processInfo.thermalState"
      via: "SystemThermalState mapping"
      pattern: "thermalState"
---

<objective>
Create the read-only thermal sensor foundation for Phase 10.

Purpose: This plan handles the hardest constraint first: a CPU/SoC value is only acceptable when the source is explicitly trusted for Mac15,9; otherwise the downstream UI must render `N/A` without crashes, popups, stale values, fan work, SMC writes, or dependency additions.

Output: `SMCReader` read-only decoding helpers, new `ThermalReader.swift` with snapshot/trust/catalog types, and Xcode target registration for the new Swift file.
</objective>

<execution_context>
@/Users/halo/.codex/gsd-core/workflows/execute-plan.md
@/Users/halo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-thermal-read-only-monitoring/10-CONTEXT.md
@.planning/phases/10-thermal-read-only-monitoring/10-UI-SPEC.md
@.planning/phases/10-thermal-read-only-monitoring/10-RESEARCH.md
@.planning/phases/10-thermal-read-only-monitoring/10-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/milestones/v2.0-phases/07-battery-power/07-01-SUMMARY.md
@.planning/milestones/v2.0-phases/07-battery-power/07-02-SUMMARY.md
@MacStatus/MacStatus/Readers/SMCReader.swift
@MacStatus/MacStatus/Readers/BatteryReader.swift
@MacStatus/MacStatus.xcodeproj/project.pbxproj
</context>

## Artifacts This Phase Produces

- File: `MacStatus/MacStatus/Readers/ThermalReader.swift`
- File: `.planning/phases/10-thermal-read-only-monitoring/10-HARDWARE-PROBE.md`
- Type: `SMCValue`
- Type: `SystemThermalState`
- Type: `ThermalSnapshot`
- Type: `ThermalSensorCatalog`
- Type: `ThermalReader`
- Function: `SMCReader.readRawValue(key:) -> SMCValue?`
- Function: `SMCReader.readTemperatureCelsius(key:) -> Double?`
- Function: `ThermalSnapshot.unavailable(capturedAt:) -> ThermalSnapshot`
- Function: `ThermalReader.setup()`
- Function: `ThermalReader.readValue() -> ThermalSnapshot`
- Function: `ThermalReader.diagnosticReadings() -> [ThermalDiagnosticReading]`
- Type: `ThermalDiagnosticReading`
- Field: `MetricCollector.thermalReader`
- Field: `MetricCollector.lastThermalSnapshot`
- Field: `DashboardState.thermal`
- Function: `DashboardState.updateThermal(_:)`
- View: `ThermalSectionView`
- Setting key: `SettingsManager.Keys.showThermalSection`
- Setting backing field: `SettingsManager._showThermalSection`
- Setting property: `SettingsManager.showThermalSection`
- Settings UI control: `Toggle("散热区块", isOn: $settings.showThermalSection)`
- Explicit non-artifacts: no fan RPM symbols, no fan capability/control symbols, no SMC write API, no helper/XPC, no status-bar thermal metric, no SSD reader, no notifications, no raw sensor browser.

## Multi-Source Coverage Audit

| Source | ID | Feature / Requirement | Plan | Status | Notes |
|---|---|---|---|---|---|
| GOAL | Phase 10 | Trusted CPU/SoC thermal state in popover with stable N/A degradation | 10-01, 10-02, 10-03 | COVERED | Reader trust, UI rows, setting, verification |
| REQ | THERM-01 | CPU/SoC primary temperature from trusted sensor or N/A | 10-01, 10-02, 10-03 | COVERED | D-01/D-13/D-14 enforced in ThermalReader and probe |
| REQ | THERM-02 | System thermal state visible as semantic pressure | 10-01, 10-02 | COVERED | ProcessInfo row separate from temperature |
| REQ | THERM-03 | GPU/battery secondary temperatures only when trusted | 10-01, 10-02, 10-03 | COVERED | GPU/battery independent nil fields; SSD omitted per D-05 |
| REQ | THERM-04 | Missing/unsupported/read failure degrades quietly | 10-01, 10-02, 10-03 | COVERED | Optional snapshots, stable rows, no popups |
| RESEARCH | SMC read-only decoding | Add raw metadata and decode `sp78`, `fpe2`, `flt `, `ui8`, `ui16`, `ui32` | 10-01 | COVERED | No write helpers |
| RESEARCH | ThermalReader architecture | `ThermalReader -> MetricCollector -> DashboardState/UI` | 10-01, 10-02 | COVERED | Matches battery snapshot pattern |
| RESEARCH | Package legitimacy | No package installs | 10-01, 10-02, 10-03 | COVERED | No npm/pip/cargo/brew dependency |
| RESEARCH | Xcode project trap | New Swift files registered in pbxproj | 10-01 | COVERED | Target membership gates |
| RESEARCH | Hardware probe | Mac15,9 probe evidence before claiming trusted CPU/SoC | 10-03 | COVERED | Probe doc and source assertions |
| CONTEXT | D-01 | Strict primary CPU/SoC trust | 10-01, 10-03 | COVERED | No package/proximity/hottest fallback |
| CONTEXT | D-02 | Thermal state separate semantic row | 10-01, 10-02 | COVERED | Dedicated snapshot field and UI row |
| CONTEXT | D-03 | No fabricated, smoothed, cached, or substituted values | 10-01, 10-02 | COVERED | Current-read optional values |
| CONTEXT | D-04 | GPU and battery secondary temps | 10-01, 10-02 | COVERED | Independent optional fields |
| CONTEXT | D-05 | SSD deferred unless existing no-dependency path | 10-01, 10-03 | COVERED | No SSD implementation planned |
| CONTEXT | D-06 | Secondary rows do not replace primary | 10-01, 10-02 | COVERED | Separate labels and fields |
| CONTEXT | D-07 | Stable rows with N/A/-- | 10-02 | COVERED | ThermalSectionView rows fixed |
| CONTEXT | D-08 | Quiet unsupported/read failure | 10-01, 10-02, 10-03 | COVERED | No alerts/popups/noisy loop |
| CONTEXT | D-09 | Stable formatting | 10-02 | COVERED | Celsius, monospaced, right-aligned values |
| CONTEXT | D-10 | Dedicated `散热` section | 10-02 | COVERED | New ThermalSectionView |
| CONTEXT | D-11 | Default-on settings toggle | 10-03 | COVERED | `showThermalSection` |
| CONTEXT | D-12 | Keep 320pt, avoid jitter | 10-02, 10-03 | COVERED | No width expansion in plan |
| CONTEXT | D-13 | Validate on Mac15,9 M3 Max | 10-03 | COVERED | Probe doc |
| CONTEXT | D-14 | Other machines degrade gracefully | 10-01, 10-03 | COVERED | Model gate and nil fallback |

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SMCReader with read-only raw and temperature decoding</name>
  <files>MacStatus/MacStatus/Readers/SMCReader.swift</files>
  <read_first>
    - MacStatus/MacStatus/Readers/SMCReader.swift lines 6-18, 24-31, 91-145, 174-200 for the current read-only AppleSMC boundary and decode path.
    - .planning/phases/10-thermal-read-only-monitoring/10-CONTEXT.md decisions D-01, D-03, D-08.
    - .planning/phases/10-thermal-read-only-monitoring/10-RESEARCH.md sections "Pattern 1: Read-Only SMC Boundary Extension", "Pitfall 2: Missing SMC data type support", and "Pitfall 4: Accidentally adding fan/control scope".
    - .planning/phases/10-thermal-read-only-monitoring/10-PATTERNS.md section `SMCReader.swift`.
    - .planning/research/PITFALLS.md "Safety Guardrails" row "Read-only first".
  </read_first>
  <behavior>
    - `readRawValue(key:)` returns nil when the connection is closed, key info fails, size is zero, read bytes fails, or SMC result is nonzero.
    - `readValue(key:)` preserves existing callers by decoding through the new raw helper and still returns `Double?`.
    - `readTemperatureCelsius(key:)` returns a Celsius `Double?` only for supported numeric formats and only inside a strict plausible range of `0...120`.
    - SMC decoding supports read-only numeric formats needed by Phase 10: `sp78`, `fpe2`, `flt `, `ui8`, `ui16`, `ui32`; unknown formats return nil.
    - No method, constant, or comment introduces SMC writes, fan keys, fan mode, fan target, `FNum`, `FS!`, helper/XPC, or raw sensor browsing.
  </behavior>
  <action>
    Add an internal value type `SMCValue: Sendable, Equatable` near `SMCReader` with fields `key: String`, `dataType: String`, `dataSize: UInt32`, and `bytes: [UInt8]`. Extract the current key-info/read-bytes sequence into `func readRawValue(key: String) -> SMCValue?` and keep the existing `func readValue(key: String) -> Double?` as a compatibility wrapper over the same decoder.

    Extend the decoder so Phase 10 can read temperature data types without widening the boundary: keep `cmdReadBytes`/`cmdReadKeyInfo`, keep the 80-byte ABI guard, keep `open()`/`close()` behavior, and add only read-side decoding for `sp78`, `fpe2`, `flt `, `ui8`, `ui16`, and `ui32`. Add `func readTemperatureCelsius(key: String) -> Double?` that calls the numeric decoder and bounds-checks `0...120`.

    Do not add a generic SMC writer, fan-control writer, fan key constants, fan RPM reads, helper/XPC hooks, shell-outs, logging loops, or public raw sensor enumeration. This implements D-03 and D-08 while preserving the strict D-01 trust boundary for the `ThermalReader` task.
  </action>
  <verify>
    <automated>xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build</automated>
    <automated>rg -n "func readRawValue|struct SMCValue|func readTemperatureCelsius" MacStatus/MacStatus/Readers/SMCReader.swift</automated>
    <automated>! rg -n "cmdWriteBytes|writeBytes|writeValue|writeRaw|FS!|FNum|F[0-9](Ac|Mn|Mx|Tg|Md|md)|XPC|helper" MacStatus/MacStatus/Readers/SMCReader.swift</automated>
  </verify>
  <acceptance_criteria>
    Source contains exactly one read-only raw value path, existing `readValue(key:)` callers remain valid, supported numeric temperature formats decode through one shared helper, impossible/unreadable values return nil, and no Phase 10 reader source contains fan/control/write tokens.
  </acceptance_criteria>
  <done>`SMCReader.swift` exposes read-only raw and Celsius decoding helpers; build passes; no SMC write or fan scope appears in this file.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create ThermalReader snapshot, strict catalog, and target membership</name>
  <files>MacStatus/MacStatus/Readers/ThermalReader.swift, MacStatus/MacStatus.xcodeproj/project.pbxproj</files>
  <read_first>
    - MacStatus/MacStatus/Readers/BatteryReader.swift lines 8-14, 67-98, 108-132, 163-239 for optional hardware snapshot and AppleSmartBattery patterns.
    - MacStatus/MacStatus/Readers/SMCReader.swift after Task 1 for `readTemperatureCelsius(key:)`.
    - MacStatus/MacStatus.xcodeproj/project.pbxproj lines 9-40, 43-76, 125-138, 274-306 for the exact new Swift file registration pattern.
    - .planning/phases/10-thermal-read-only-monitoring/10-CONTEXT.md decisions D-01 through D-06, D-13, D-14.
    - .planning/phases/10-thermal-read-only-monitoring/10-RESEARCH.md sections "Pattern 2: Strict Trusted Sensor Catalog", "Thermal Snapshot", "Battery Temperature Candidate Read", "Open Questions".
    - .planning/phases/10-thermal-read-only-monitoring/10-UI-SPEC.md copy/value contract for `CPU/SoC`, `系统状态`, `GPU`, `电池`.
    - .planning/research/PITFALLS.md "Trap 1: xcodebuild succeeds while new files are not in the Xcode target".
  </read_first>
  <behavior>
    - `ThermalReader.readValue()` always returns a `ThermalSnapshot`, even when all temperature values are nil.
    - `SystemThermalState` maps `ProcessInfo.ThermalState` into `.nominal`, `.fair`, `.serious`, `.critical`, `.unknown` without implying temperature.
    - For `hw.model == Mac15,9`, CPU/SoC candidates are limited to the explicit M3 CPU/SoC list from research: `Te05`, `Te0L`, `Te0P`, `Te0S`, `Tf04`, `Tf09`, `Tf0A`, `Tf0B`, `Tf0D`, `Tf0E`, `Tf44`, `Tf49`, `Tf4A`, `Tf4B`, `Tf4D`, `Tf4E`.
    - For `hw.model == Mac15,9`, GPU candidates are limited to the explicit M3 GPU list from research: `Tf14`, `Tf18`, `Tf19`, `Tf1A`, `Tf24`, `Tf28`, `Tf29`, `Tf2A`.
    - Battery temperature reads only trusted battery sources: `AppleSmartBattery` `Temperature` raw value divided by 100 with `0...100` bounds, plus optional SMC `TB1T`/`TB2T` fallback through `readTemperatureCelsius(key:)`.
    - For unsupported models or absent/unreadable candidate keys, every affected field is nil; no value is fabricated, cached, averaged, or promoted from a different row.
    - `ThermalReader.swift` is present in PBXFileReference, Readers group, PBXBuildFile, and PBXSourcesBuildPhase.
  </behavior>
  <action>
    Create `MacStatus/MacStatus/Readers/ThermalReader.swift` with value-only `SystemThermalState`, `ThermalSnapshot`, `ThermalDiagnosticReading`, `ThermalSensorCatalog`, and `ThermalReader`. `ThermalSnapshot` must include `cpuSocTemperatureCelsius: Double?`, `systemState: SystemThermalState`, `gpuTemperatureCelsius: Double?`, `batteryTemperatureCelsius: Double?`, and `capturedAt: Date`; add `ThermalSnapshot.unavailable(capturedAt:)` for stable UI defaults.

    Implement `ThermalReader.setup()` to open its owned `SMCReader`. Implement `ThermalReader.readValue()` as a synchronous, cheap, read-only snapshot path driven later by `MetricCollector`. Determine the hardware model with `sysctlbyname("hw.model")`; only use the Mac15,9 CPU/SoC and GPU candidate lists on Mac15,9. Return nil for CPU/SoC when the model is not Mac15,9, no trusted candidate decodes, or the decoded value is implausible. Keep `ProcessInfo.processInfo.thermalState` as `systemState`, not as a substitute for `cpuSocTemperatureCelsius` per D-02.

    Implement battery temperature by probing `AppleSmartBattery` with `IOServiceGetMatchingService` and `IORegistryEntryCreateCFProperties`, validating `Temperature` as centi-Celsius, and falling back only to `TB1T`/`TB2T` if those trusted battery SMC keys decode. Implement `diagnosticReadings()` to return read-only candidate key/type/value evidence for Phase 10 verification without exposing raw sensor UI.

    Register `ThermalReader.swift` in `MacStatus.xcodeproj/project.pbxproj` using a unique ID prefix and the existing `BatteryReader.swift`/`SMCReader.swift` pattern: PBXFileReference, Readers group child, PBXBuildFile, and PBXSourcesBuildPhase entry.
  </action>
  <verify>
    <automated>xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build</automated>
    <automated>rg -n "struct ThermalSnapshot|enum SystemThermalState|final class ThermalReader|enum ThermalSensorCatalog|struct ThermalDiagnosticReading|func diagnosticReadings" MacStatus/MacStatus/Readers/ThermalReader.swift</automated>
    <automated>test "$(rg -n "ThermalReader.swift" MacStatus/MacStatus.xcodeproj/project.pbxproj | wc -l | tr -d ' ')" -ge 4</automated>
    <automated>! rg -n "MetricSample|HistoryStore|RingBuffer|StatusBarManager|notification|alert|SSD|SMART|NVMe|FNum|FS!|F[0-9](Ac|Mn|Mx|Tg|Md|md)|write" MacStatus/MacStatus/Readers/ThermalReader.swift</automated>
  </verify>
  <acceptance_criteria>
    `ThermalReader.readValue()` produces a complete snapshot with current optional values and semantic thermal state; Mac15,9 CPU/SoC and GPU candidates are explicit and model-gated; battery temperature is validated; unsupported data is nil; the new file is compiled into the Xcode target; no SSD/fan/write/history/status-bar/popup scope appears.
  </acceptance_criteria>
  <done>`ThermalReader.swift` exists, compiles, is registered in the target, and enforces D-01 through D-06 plus D-13/D-14 at the reader boundary.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| AppleSMC/IORegistry -> Reader | Undocumented hardware values cross into app logic and can be absent, ambiguous, malformed, or stale. |
| Reader -> UI snapshot | Optional fields must not become fake user-facing certainty. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-01-T | Tampering | `SMCReader` decoded bytes | mitigate | Validate SMC result byte, type, size, and Celsius bounds; nil on unknown formats. |
| T-10-01-I | Information Integrity | `ThermalReader` CPU/SoC source | mitigate | Use explicit Mac15,9 CPU/SoC candidate list only; unsupported or unconfirmed sources return nil for UI `N/A`. |
| T-10-01-D | Denial of Service | IORegistry/AppleSMC unavailable | mitigate | Probe-and-nil optional casts, no force unwraps, snapshot still returns with nil fields. |
| T-10-01-E | Elevation of Privilege | SMC write/control scope | mitigate | No write constants, no fan keys, no helper/XPC, no generic writer, and verification greps block those tokens. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install tasks; Package Legitimacy Audit says none. |
</threat_model>

<verification>
Overall plan verification:

- `xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build` succeeds.
- `ThermalReader.swift` appears at least four times in `project.pbxproj`.
- `rg -n "cmdWriteBytes|writeBytes|writeValue|writeRaw|FNum|FS!|F[0-9](Ac|Mn|Mx|Tg|Md|md)|helper|XPC" MacStatus/MacStatus/Readers` returns no Phase 10 write/control additions.
- `rg -n "struct ThermalSnapshot|final class ThermalReader|ProcessInfo.processInfo.thermalState|AppleSmartBattery|Mac15,9" MacStatus/MacStatus/Readers/ThermalReader.swift` shows the required snapshot, semantic state, battery temperature, and model gate.
</verification>

<success_criteria>
Plan 10-01 is complete when the reader layer can produce read-only thermal snapshots, CPU/SoC trust is strict and model-gated, unsupported data degrades to nil, `ProcessInfo.thermalState` is separate, `ThermalReader.swift` is compiled into the app target, and no fan/control/write/status-bar/history/dependency scope has been introduced.
</success_criteria>

<output>
Create `.planning/phases/10-thermal-read-only-monitoring/10-01-SUMMARY.md` when done.
</output>
