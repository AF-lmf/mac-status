---
phase: 10-thermal-read-only-monitoring
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - MacStatus/MacStatus/Collectors/MetricCollector.swift
  - MacStatus/MacStatus/UI/Views/DashboardView.swift
autonomous: true
requirements:
  - THERM-01
  - THERM-02
  - THERM-03
  - THERM-04
user_setup: []
must_haves:
  truths:
    - "D-07/D-08: The popover can render stable thermal rows from a current snapshot even when every temperature is unavailable."
    - "D-02/D-10: `系统状态` is a row inside a dedicated `散热` section, not a replacement for `CPU/SoC`."
    - "D-09/D-12: Temperature values use compact Celsius/N/A formatting, monospaced trailing values, and the existing 320pt dashboard width."
    - "D-03/D-05: Thermal values are popover-only current snapshots, not persisted history, status-bar metrics, SSD data, or cached stale values."
  artifacts:
    - path: "MacStatus/MacStatus/Collectors/MetricCollector.swift"
      provides: "ThermalReader setup/read/cache/updateUI integration"
      contains: "lastThermalSnapshot"
    - path: "MacStatus/MacStatus/UI/Views/DashboardView.swift"
      provides: "DashboardState thermal field and ThermalSectionView stable rows"
      contains: "ThermalSectionView"
  key_links:
    - from: "MetricCollector.tick()"
      to: "ThermalReader.readValue()"
      via: "same unified timer tick as battery"
      pattern: "thermalReader\\.readValue"
    - from: "MetricCollector.updateUI(sample:)"
      to: "DashboardState.updateThermal(_:)"
      via: "lastThermalSnapshot, outside MetricSample"
      pattern: "dashboard\\.updateThermal"
    - from: "DashboardView"
      to: "ThermalSectionView"
      via: "state.thermal"
      pattern: "ThermalSectionView"
---

<objective>
Wire the thermal snapshot into the live popover without changing the status bar, persistence, or sampling cadence.

Purpose: Phase 10 thermal values belong in the popover as stable read-only rows. This plan connects `ThermalReader` to the existing `MetricCollector -> DashboardState -> DashboardView` path and renders the dedicated `散热` section defined by the UI-SPEC.

Output: `MetricCollector` reads/caches thermal snapshots, `DashboardState` exposes thermal state, and `DashboardView` renders `CPU/SoC`, `系统状态`, `GPU`, and `电池` rows with stable unavailable values.
</objective>

<execution_context>
@/Users/halo/.codex/gsd-core/workflows/execute-plan.md
@/Users/halo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-thermal-read-only-monitoring/10-CONTEXT.md
@.planning/phases/10-thermal-read-only-monitoring/10-UI-SPEC.md
@.planning/phases/10-thermal-read-only-monitoring/10-RESEARCH.md
@.planning/phases/10-thermal-read-only-monitoring/10-PATTERNS.md
@.planning/milestones/v2.0-phases/07-battery-power/07-02-SUMMARY.md
@.planning/milestones/v2.0-phases/09-settings-window-ui-customization/09-03-SUMMARY.md
@MacStatus/MacStatus/Readers/ThermalReader.swift
@MacStatus/MacStatus/Collectors/MetricCollector.swift
@MacStatus/MacStatus/UI/Views/DashboardView.swift
</context>

## Artifacts This Phase Produces

- File: `MacStatus/MacStatus/Readers/ThermalReader.swift`
- File: `.planning/phases/10-thermal-read-only-monitoring/10-HARDWARE-PROBE.md`
- Type: `SMCValue`
- Type: `SystemThermalState`
- Type: `ThermalSnapshot`
- Type: `ThermalSensorCatalog`
- Type: `ThermalReader`
- Function: `SMCReader.readRawValue(key:) -> SMCValue?`
- Function: `SMCReader.readTemperatureCelsius(key:) -> Double?`
- Function: `ThermalSnapshot.unavailable(capturedAt:) -> ThermalSnapshot`
- Function: `ThermalReader.setup()`
- Function: `ThermalReader.readValue() -> ThermalSnapshot`
- Function: `ThermalReader.diagnosticReadings() -> [ThermalDiagnosticReading]`
- Type: `ThermalDiagnosticReading`
- Field: `MetricCollector.thermalReader`
- Field: `MetricCollector.lastThermalSnapshot`
- Field: `DashboardState.thermal`
- Function: `DashboardState.updateThermal(_:)`
- View: `ThermalSectionView`
- Setting key: `SettingsManager.Keys.showThermalSection`
- Setting backing field: `SettingsManager._showThermalSection`
- Setting property: `SettingsManager.showThermalSection`
- Settings UI control: `Toggle("散热区块", isOn: $settings.showThermalSection)`
- Explicit non-artifacts: no fan RPM symbols, no fan capability/control symbols, no SMC write API, no helper/XPC, no status-bar thermal metric, no SSD reader, no notifications, no raw sensor browser.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire ThermalReader into MetricCollector current snapshot flow</name>
  <files>MacStatus/MacStatus/Collectors/MetricCollector.swift</files>
  <read_first>
    - MacStatus/MacStatus/Collectors/MetricCollector.swift lines 28-46, 57-72, 101-119, 146-156, 188-210 for battery reader setup/cache/updateUI analogs.
    - MacStatus/MacStatus/Readers/ThermalReader.swift from Plan 10-01 for `ThermalReader`, `ThermalSnapshot`, and `ThermalSnapshot.unavailable`.
    - .planning/phases/10-thermal-read-only-monitoring/10-PATTERNS.md section `MetricCollector.swift`.
    - .planning/phases/10-thermal-read-only-monitoring/10-CONTEXT.md decisions D-03, D-07, D-08, D-12.
    - .planning/phases/10-thermal-read-only-monitoring/10-UI-SPEC.md "Interaction Contract" and "Must Not Change in Phase 10".
  </read_first>
  <behavior>
    - `MetricCollector.start()` calls `thermalReader.setup()` and performs an initial `readValue()` baseline beside existing reader setup.
    - `MetricCollector.tick()` calls `thermalReader.readValue()` once per existing timer tick and assigns `lastThermalSnapshot`.
    - `MetricCollector.applyNow()` reuses the cached `lastThermalSnapshot` through `updateUI(sample:)` and does not force a fresh SMC read when settings change.
    - Thermal data stays out of `MetricSample`, `RingBuffer`, `HistoryStore`, `StatusBarManager.updateTitle`, `Metric`, metric order, and enabled metrics.
    - `reconfigure()` remains reader-free and does not touch `thermalReader`.
  </behavior>
  <action>
    Edit `MetricCollector.swift` to mirror the existing battery snapshot integration. Add `private let thermalReader = ThermalReader()` near the other reader properties and `private var lastThermalSnapshot: ThermalSnapshot = .unavailable()` near `lastBatterySnapshot`.

    In `start()`, call `thermalReader.setup()` with the other reader setup calls and assign the first `lastThermalSnapshot = thermalReader.readValue()` with the first-read block. In `tick()`, read `let thermal = thermalReader.readValue()` after battery or GPU reads and assign it to `lastThermalSnapshot`. In `updateUI(sample:)`, call `dashboard.updateThermal(lastThermalSnapshot)` next to `dashboard.updateBattery(lastBatterySnapshot)`.

    Do not modify `MetricSample`, `RingBuffer`, `HistoryStore`, `StatusBarManager`, `Metric`, status-bar enabled metrics, SQLite persistence, or any history/sparkline schema. This keeps D-03 current-read semantics and preserves D-12 sampling/layout boundaries.
  </action>
  <verify>
    <automated>xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build</automated>
    <automated>rg -n "thermalReader|lastThermalSnapshot|dashboard\\.updateThermal" MacStatus/MacStatus/Collectors/MetricCollector.swift</automated>
    <automated>! rg -n "thermal|Thermal" MacStatus/MacStatus/Storage MacStatus/MacStatus/UI/StatusBarManager.swift MacStatus/MacStatus/Utils/Metric.swift</automated>
    <automated>! rg -n "thermalReader" -A 20 MacStatus/MacStatus/Collectors/MetricCollector.swift | rg -n "func reconfigure|reconfigure\\(|MetricSample\\(|StatusBarManager\\.shared\\.updateTitle"</automated>
  </verify>
  <acceptance_criteria>
    The collector creates and reads exactly one thermal reader on the existing cadence, pushes cached thermal snapshots to `DashboardState`, repaints without fresh reads on cosmetic settings changes, and does not persist or status-bar-render thermal data.
  </acceptance_criteria>
  <done>`MetricCollector.swift` has thermal setup/read/cache/updateUI wiring and no persistence/status-bar/schema changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add DashboardState thermal data and dedicated stable ThermalSectionView</name>
  <files>MacStatus/MacStatus/UI/Views/DashboardView.swift</files>
  <read_first>
    - MacStatus/MacStatus/UI/Views/DashboardView.swift lines 10-126 for layout/320pt width and section placement.
    - MacStatus/MacStatus/UI/Views/DashboardView.swift lines 185-228 for `BatterySectionView` card/row style.
    - MacStatus/MacStatus/UI/Views/DashboardView.swift lines 347-461 for `DashboardState` field/update method patterns.
    - MacStatus/MacStatus/Readers/ThermalReader.swift for `ThermalSnapshot` and `SystemThermalState`.
    - .planning/phases/10-thermal-read-only-monitoring/10-UI-SPEC.md sections "Phase 10 Popover Contract", "Copywriting Contract", "Typography", "Accessibility Contract", and "Must Not Change in Phase 10".
    - .planning/phases/10-thermal-read-only-monitoring/10-PATTERNS.md section `DashboardView.swift`.
    - .planning/phases/10-thermal-read-only-monitoring/10-CONTEXT.md decisions D-01, D-02, D-06, D-07, D-09, D-10, D-12.
  </read_first>
  <behavior>
    - `DashboardState` has a non-optional `thermal: ThermalSnapshot` defaulting to `.unavailable()` and `updateThermal(_:)`.
    - `DashboardView` renders `ThermalSectionView(snapshot: state.thermal)` after the battery section and before process sections; Plan 10-03 will add the settings gate.
    - `ThermalSectionView` always contains exactly the Phase 10 rows `CPU/SoC`, `系统状态`, `GPU`, and `电池` when rendered.
    - `CPU/SoC` displays integer Celsius with no decimal when `cpuSocTemperatureCelsius` is non-nil, otherwise `N/A`; no other value is substituted into that row.
    - `系统状态` displays `正常`, `偏热`, `严重`, `临界`, or `未知`; it is a semantic row, not a temperature row.
    - `GPU` and `电池` display integer Celsius or `N/A` independently.
    - Values use monospaced digits, right alignment, and a fixed or minimum trailing width sufficient for `100°C`, `N/A`, and `临界`.
    - Dashboard width remains `.frame(width: 320)`.
  </behavior>
  <action>
    Edit `DashboardView.swift` to add `@Published var thermal: ThermalSnapshot = .unavailable()` and `func updateThermal(_ snapshot: ThermalSnapshot)` to `DashboardState`. Do not add a hardware gate equivalent to `hasBattery`; the section is owned by Phase 10 and unavailable readings are represented inside the stable rows.

    Add a dedicated `ThermalSectionView` near `BatterySectionView`. Use the UI-SPEC copy exactly: section title `散热`, header value `CPU/SoC 58°C` or `CPU/SoC N/A`, and rows `系统状态`, `GPU`, `电池`. Use helper formatting functions for temperature and thermal-state labels. Apply `.font(.system(.body, design: .monospaced))` to the header value and `.font(.system(.caption, design: .monospaced))` plus trailing alignment to row values. Use stable spacing consistent with UI-SPEC: row spacing around 8pt and card corner radius 8.

    Insert the section after the existing battery block and before process sections. Keep `.frame(width: 320)` unchanged. Add accessibility labels for the section and primary/status values as specified by UI-SPEC. Do not add charts, progress bars, click actions, popups, threshold colors based on raw temperature, status-bar metric toggles, fan rows, or SSD rows.
  </action>
  <verify>
    <automated>xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build</automated>
    <automated>rg -n "ThermalSectionView|updateThermal|@Published var thermal|CPU/SoC|系统状态|散热|GPU|电池" MacStatus/MacStatus/UI/Views/DashboardView.swift</automated>
    <automated>rg -n "\\.frame\\(width: 320\\)" MacStatus/MacStatus/UI/Views/DashboardView.swift</automated>
    <automated>! rg -n "fan|风扇|RPM|SSD|SMART|NVMe|alert|Alert|notification|status.*thermal|metricOrder|enabledMetrics" MacStatus/MacStatus/UI/Views/DashboardView.swift</automated>
  </verify>
  <acceptance_criteria>
    The dashboard has a dedicated `散热` card with stable rows and correct value semantics; unavailable readings show `N/A` or `未知` inline; the section uses monospaced/right-aligned values, keeps the 320pt width, and introduces no fan/SSD/alert/status-bar scope.
  </acceptance_criteria>
  <done>`DashboardView.swift` exposes and renders thermal snapshots according to D-01/D-02/D-06/D-07/D-09/D-10/D-12 and UI-SPEC.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Reader snapshot -> MainActor collector | Optional hardware values enter the unified app update loop. |
| DashboardState -> SwiftUI popover | Hardware absence or ambiguity becomes user-facing copy. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-02-T | Tampering | `MetricCollector` thermal integration | mitigate | Keep thermal outside persistence/status-bar schemas and only pass current `ThermalSnapshot` to `DashboardState`. |
| T-10-02-I | Information Integrity | `ThermalSectionView` labels | mitigate | Dedicated fields for `CPU/SoC`, `系统状态`, `GPU`, `电池`; no secondary promotion or thermal-state substitution. |
| T-10-02-D | Denial of Service | SwiftUI rendering with nil values | mitigate | Non-optional default snapshot and stable inline unavailable formatting prevent crashes/row churn. |
| T-10-02-R | Repudiation | Settings-driven repaint behavior | accept | No audit need for local read-only popover repaint; source assertions verify no fresh SMC read in `applyNow()`. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install tasks; Package Legitimacy Audit says none. |
</threat_model>

<verification>
Overall plan verification:

- `xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build` succeeds.
- `MetricCollector.swift` contains `thermalReader`, `lastThermalSnapshot`, and `dashboard.updateThermal`, while storage/status-bar/metric files contain no thermal references.
- `DashboardView.swift` contains `ThermalSectionView`, `updateThermal`, `CPU/SoC`, `系统状态`, `GPU`, `电池`, `散热`, and still contains `.frame(width: 320)`.
- `rg -n "fan|风扇|RPM|SSD|SMART|NVMe|alert|notification" MacStatus/MacStatus/UI/Views/DashboardView.swift MacStatus/MacStatus/Collectors/MetricCollector.swift` returns no Phase 10 scope additions.
</verification>

<success_criteria>
Plan 10-02 is complete when the app builds with the thermal snapshot on the unified collector tick, the popover has a dedicated stable `散热` section with truthful `N/A` degradation, and thermal data remains absent from status-bar metrics, history, fan/control scope, SSD work, and alerting.
</success_criteria>

<output>
Create `.planning/phases/10-thermal-read-only-monitoring/10-02-SUMMARY.md` when done.
</output>
