---
phase: 10-thermal-read-only-monitoring
plan: 03
type: execute
wave: 3
depends_on:
  - 10-02
files_modified:
  - MacStatus/MacStatus/Utils/SettingsManager.swift
  - MacStatus/MacStatus/UI/Views/SettingsView.swift
  - MacStatus/MacStatus/UI/Views/DashboardView.swift
  - .planning/phases/10-thermal-read-only-monitoring/10-HARDWARE-PROBE.md
autonomous: true
requirements:
  - THERM-01
  - THERM-02
  - THERM-03
  - THERM-04
user_setup: []
must_haves:
  truths:
    - "D-10/D-11: The dedicated `散热` section is default-on and controlled by a Settings toggle named `散热区块`."
    - "D-07/D-08: Toggling the section changes only visibility; missing sensors remain quiet inline `N/A`/`—` states when visible."
    - "D-01/D-13/D-14: Hardware verification records Mac15,9 evidence; unsupported or unconfirmed CPU/SoC sources remain `N/A`."
    - "D-03/D-05/D-12: Final gates prove no stale substitution, SSD, fan, control, notification, helper/XPC, status-bar thermal, or width expansion was added."
  artifacts:
    - path: "MacStatus/MacStatus/Utils/SettingsManager.swift"
      provides: "showThermalSection default-on UserDefaults setting"
      contains: "showThermalSection"
    - path: "MacStatus/MacStatus/UI/Views/SettingsView.swift"
      provides: "散热区块 toggle"
      contains: "散热区块"
    - path: "MacStatus/MacStatus/UI/Views/DashboardView.swift"
      provides: "settings-gated thermal section"
      contains: "showThermalSection"
    - path: ".planning/phases/10-thermal-read-only-monitoring/10-HARDWARE-PROBE.md"
      provides: "Mac15,9 hardware probe and final anti-scope verification record"
      contains: "Mac15,9"
  key_links:
    - from: "SettingsView"
      to: "SettingsManager.showThermalSection"
      via: "Toggle binding"
      pattern: "散热区块"
    - from: "DashboardView"
      to: "SettingsManager.showThermalSection"
      via: "@Observable read inside body"
      pattern: "showThermalSection"
    - from: "ThermalReader.diagnosticReadings()"
      to: "10-HARDWARE-PROBE.md"
      via: "temporary local probe executable"
      pattern: "diagnostic"
---

<objective>
Finish Phase 10 with a default-on settings toggle, final source gates, and a Mac15,9 hardware probe record.

Purpose: The UI contract requires a dedicated `散热区块` toggle, and the strict CPU/SoC trust policy requires proof that the implementation either reads from confirmed Mac15,9 trusted candidates or renders stable `N/A`.

Output: `showThermalSection` setting, Settings toggle, Dashboard gating, and a committed hardware/verification note for Phase 10.
</objective>

<execution_context>
@/Users/halo/.codex/gsd-core/workflows/execute-plan.md
@/Users/halo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-thermal-read-only-monitoring/10-CONTEXT.md
@.planning/phases/10-thermal-read-only-monitoring/10-UI-SPEC.md
@.planning/phases/10-thermal-read-only-monitoring/10-RESEARCH.md
@.planning/phases/10-thermal-read-only-monitoring/10-PATTERNS.md
@.planning/research/PITFALLS.md
@.planning/milestones/v2.0-phases/09-settings-window-ui-customization/09-01-SUMMARY.md
@.planning/milestones/v2.0-phases/09-settings-window-ui-customization/09-02-SUMMARY.md
@.planning/milestones/v2.0-phases/09-settings-window-ui-customization/09-03-SUMMARY.md
@MacStatus/MacStatus/Utils/SettingsManager.swift
@MacStatus/MacStatus/UI/Views/SettingsView.swift
@MacStatus/MacStatus/UI/Views/DashboardView.swift
@MacStatus/MacStatus/Readers/ThermalReader.swift
</context>

## Artifacts This Phase Produces

- File: `MacStatus/MacStatus/Readers/ThermalReader.swift`
- File: `.planning/phases/10-thermal-read-only-monitoring/10-HARDWARE-PROBE.md`
- Type: `SMCValue`
- Type: `SystemThermalState`
- Type: `ThermalSnapshot`
- Type: `ThermalSensorCatalog`
- Type: `ThermalReader`
- Function: `SMCReader.readRawValue(key:) -> SMCValue?`
- Function: `SMCReader.readTemperatureCelsius(key:) -> Double?`
- Function: `ThermalSnapshot.unavailable(capturedAt:) -> ThermalSnapshot`
- Function: `ThermalReader.setup()`
- Function: `ThermalReader.readValue() -> ThermalSnapshot`
- Function: `ThermalReader.diagnosticReadings() -> [ThermalDiagnosticReading]`
- Type: `ThermalDiagnosticReading`
- Field: `MetricCollector.thermalReader`
- Field: `MetricCollector.lastThermalSnapshot`
- Field: `DashboardState.thermal`
- Function: `DashboardState.updateThermal(_:)`
- View: `ThermalSectionView`
- Setting key: `SettingsManager.Keys.showThermalSection`
- Setting backing field: `SettingsManager._showThermalSection`
- Setting property: `SettingsManager.showThermalSection`
- Settings UI control: `Toggle("散热区块", isOn: $settings.showThermalSection)`
- Explicit non-artifacts: no fan RPM symbols, no fan capability/control symbols, no SMC write API, no helper/XPC, no status-bar thermal metric, no SSD reader, no notifications, no raw sensor browser.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add default-on thermal section setting and live dashboard gate</name>
  <files>MacStatus/MacStatus/Utils/SettingsManager.swift, MacStatus/MacStatus/UI/Views/SettingsView.swift, MacStatus/MacStatus/UI/Views/DashboardView.swift</files>
  <read_first>
    - MacStatus/MacStatus/Utils/SettingsManager.swift lines 62-86, 97-114, 280-306, 411-495 for section-visibility key/backing/property/load patterns.
    - MacStatus/MacStatus/UI/Views/SettingsView.swift lines 43-47 for the existing `弹窗区块` section.
    - MacStatus/MacStatus/UI/Views/DashboardView.swift body after Plan 10-02 for thermal section placement.
    - .planning/phases/10-thermal-read-only-monitoring/10-UI-SPEC.md "Settings Contract", "Interaction Contract", and "Must Not Change in Phase 10".
    - .planning/phases/10-thermal-read-only-monitoring/10-CONTEXT.md decisions D-10, D-11, D-12.
    - .planning/phases/10-thermal-read-only-monitoring/10-PATTERNS.md sections `SettingsManager.swift`, `SettingsView.swift`, and `DashboardView.swift`.
    - .planning/milestones/v2.0-phases/09-settings-window-ui-customization/09-03-SUMMARY.md for direct `SettingsManager.shared` observation in `DashboardView`.
  </read_first>
  <behavior>
    - `SettingsManager.showThermalSection` defaults to true for new installs and existing users with no key.
    - The setter uses `withMutation`, stores to `UserDefaults` key `showThermalSection`, and posts `.settingsDidChange` with that key.
    - `SettingsView` shows `Toggle("散热区块", isOn: $settings.showThermalSection)` inside existing Section `弹窗区块`.
    - `DashboardView` renders the entire `ThermalSectionView` only when `settings.showThermalSection` is true.
    - When visible, the thermal section still renders stable rows even when all readings are `N/A`/`未知`; no hardware availability gate is introduced.
    - No status-bar metric toggle, threshold control, alert rule, fan setting, or relaunch requirement is added.
  </behavior>
  <action>
    Edit `SettingsManager.swift` by adding `Keys.showThermalSection`, `_showThermalSection: Bool = true`, and computed property `showThermalSection` beside `showBatterySection` and `showProcessSection`. In `loadAll()`, use the existing nil-check pattern: if `defaults.object(forKey: Keys.showThermalSection) == nil`, set `_showThermalSection = true`; otherwise load `defaults.bool(forKey:)`. Do not bump schema version or alter migration logic.

    Edit `SettingsView.swift` Section `弹窗区块` to add `Toggle("散热区块", isOn: $settings.showThermalSection)` with the existing `@Bindable` settings object. Keep this as a visibility setting only.

    Edit `DashboardView.swift` so the Phase 10 thermal section is gated by `let settings = SettingsManager.shared` and `if settings.showThermalSection { ThermalSectionView(snapshot: state.thermal) }`. Do not add `state.hasThermal`; D-07 requires stable rows with inline unavailable values, and D-11 makes the setting the visibility gate.
  </action>
  <verify>
    <automated>xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build</automated>
    <automated>rg -n "showThermalSection|_showThermalSection|散热区块" MacStatus/MacStatus/Utils/SettingsManager.swift MacStatus/MacStatus/UI/Views/SettingsView.swift MacStatus/MacStatus/UI/Views/DashboardView.swift</automated>
    <automated>rg -n "object\\(forKey: Keys\\.showThermalSection\\)" MacStatus/MacStatus/Utils/SettingsManager.swift</automated>
    <automated>! rg -n "thermalThreshold|ThermalThreshold|status.*thermal|enabledMetrics.*thermal|metricOrder.*thermal|fan|风扇|RPM|Alert\\(|notification" MacStatus/MacStatus/UI/Views/SettingsView.swift MacStatus/MacStatus/UI/Views/DashboardView.swift</automated>
  </verify>
  <acceptance_criteria>
    The thermal section is visible by default, controlled live by the `散热区块` toggle, hidden only when the setting is false, and no extra thermal controls, fan controls, alerts, status-bar toggles, or migration churn are introduced.
  </acceptance_criteria>
  <done>`showThermalSection` exists in SettingsManager, SettingsView exposes `散热区块`, and DashboardView gates the dedicated thermal section by that setting.</done>
</task>

<task type="auto">
  <name>Task 2: Record Mac15,9 hardware probe and final no-scope-regression gates</name>
  <files>.planning/phases/10-thermal-read-only-monitoring/10-HARDWARE-PROBE.md</files>
  <read_first>
    - .planning/phases/10-thermal-read-only-monitoring/10-CONTEXT.md decisions D-01 through D-14.
    - .planning/phases/10-thermal-read-only-monitoring/10-RESEARCH.md "Verification Strategy", "Assumptions Log", and "Open Questions".
    - .planning/phases/10-thermal-read-only-monitoring/10-UI-SPEC.md "Unavailable and Failure States" and "Must Not Change in Phase 10".
    - .planning/research/PITFALLS.md "Verification Traps" and "Safety Guardrails".
    - MacStatus/MacStatus/Readers/ThermalReader.swift for `diagnosticReadings()` and candidate catalog.
    - MacStatus/MacStatus/Readers/SMCReader.swift for read-only helper verification.
  </read_first>
  <action>
    Create `.planning/phases/10-thermal-read-only-monitoring/10-HARDWARE-PROBE.md` as the execution record for Phase 10 thermal verification. Record exact command outputs for `sysctl -n hw.model`, `sw_vers`, the build command, source grep gates, and a temporary local Swift probe that compiles `SMCReader.swift`, `ThermalReader.swift`, and a temporary `/tmp/.../main.swift` together into a local executable. The temporary probe should instantiate `ThermalReader`, call `setup()`, print `readValue()` fields, and print `diagnosticReadings()` key/type/value rows; do not commit the temporary file or add a product UI.

    The probe record must explicitly state one of these outcomes for `CPU/SoC`: confirmed trusted Mac15,9 candidate read, or `N/A` because no candidate met the trust gate. If the probe reports candidate values but the source is not explicitly trusted by the catalog/model gate, record that the UI remains `CPU/SoC N/A`; do not relabel package/proximity/hottest values. Record GPU and battery outcomes independently, with unavailable values treated as normal `N/A` states.

    Run final anti-scope gates over the codebase and record their outputs: no SMC write APIs, no fan keys or RPM UI, no helper/XPC, no status-bar thermal metric, no SSD/SMART/NVMe reader, no notifications/alerts for thermal read failures, no `MetricSample`/history thermal fields, and `DashboardView` width remains 320pt. If any gate fails because of a real Phase 10 addition, fix the source and rerun every affected gate before writing the summary.
  </action>
  <verify>
    <automated>xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build</automated>
    <automated>test -f .planning/phases/10-thermal-read-only-monitoring/10-HARDWARE-PROBE.md</automated>
    <automated>rg -n "hw.model|Mac15,9|CPU/SoC|ProcessInfo|diagnostic|N/A|xcodebuild|BUILD SUCCEEDED|no SMC write|no fan|no status-bar thermal" .planning/phases/10-thermal-read-only-monitoring/10-HARDWARE-PROBE.md</automated>
    <automated>! rg -n "cmdWriteBytes|writeBytes|writeValue|writeRaw|FNum|FS!|F[0-9](Ac|Mn|Mx|Tg|Md|md)|RPM|风扇|FanController|helper|XPC" MacStatus/MacStatus</automated>
    <automated>! rg -n "thermal|Thermal|散热" MacStatus/MacStatus/Storage MacStatus/MacStatus/UI/StatusBarManager.swift MacStatus/MacStatus/Utils/Metric.swift</automated>
    <automated>rg -n "\\.frame\\(width: 320\\)" MacStatus/MacStatus/UI/Views/DashboardView.swift</automated>
  </verify>
  <acceptance_criteria>
    The probe note exists and proves the final app behavior on Mac15,9 or records stable `N/A` when trust is not established; source gates prove no fan/control/write/helper/status-bar/history/SSD/alert scope was introduced; build succeeds after all verification.
  </acceptance_criteria>
  <done>`10-HARDWARE-PROBE.md` contains exact hardware/build/source-gate evidence and Phase 10 final code satisfies all locked constraints D-01 through D-14.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| UserDefaults -> DashboardView | Local preference controls section visibility but must not change hardware reads or semantics. |
| Hardware probe -> Phase signoff | Local hardware evidence determines whether CPU/SoC can be displayed as trusted or must remain `N/A`. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-10-03-T | Tampering | `showThermalSection` UserDefaults value | accept | Local visibility preference only; setting false hides section and does not alter read/control behavior. |
| T-10-03-I | Information Integrity | `10-HARDWARE-PROBE.md` trust claim | mitigate | Record exact model, build, probe, and diagnostic output; CPU/SoC stays `N/A` unless catalog/model gate proves trust. |
| T-10-03-D | Denial of Service | Settings toggle repaint | mitigate | Dashboard reads `SettingsManager.shared` directly; no fresh SMC read or relaunch is required. |
| T-10-03-E | Elevation of Privilege | Fan/SMC write regression | mitigate | Final source gates block write APIs, fan keys, helper/XPC, and FanController symbols. |
| T-10-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install tasks; Package Legitimacy Audit says none. |
</threat_model>

<verification>
Overall plan verification:

- `xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build` succeeds.
- `showThermalSection` appears in `SettingsManager.swift`, `SettingsView.swift`, and `DashboardView.swift`; first-run default uses `object(forKey:) == nil`.
- `.planning/phases/10-thermal-read-only-monitoring/10-HARDWARE-PROBE.md` records model, OS, build, thermal snapshot, diagnostic readings, and final source gates.
- No Phase 10 source contains SMC write APIs, fan keys/RPM/control, helper/XPC, SSD/SMART/NVMe implementation, status-bar thermal metric, thermal persistence, thermal notification, or thermal alerting.
- `DashboardView` remains `.frame(width: 320)`.
</verification>

<success_criteria>
Plan 10-03 is complete when the user has a default-on `散热区块` settings toggle, the thermal section is gated live by the setting, the Mac15,9 probe record verifies or rejects CPU/SoC trust honestly, and final source gates prove Phase 10 stayed read-only, popover-only, stable, and quiet.
</success_criteria>

<output>
Create `.planning/phases/10-thermal-read-only-monitoring/10-03-SUMMARY.md` when done.
</output>
