---
phase: 08-per-process-top-n-cpu-memory
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - MacStatus/MacStatus/UI/PopoverManager.swift
  - MacStatus/MacStatus/UI/Views/DashboardView.swift
  - MacStatus/MacStatus/UI/Views/ProcessListView.swift
autonomous: true
requirements:
  - PROC-01
  - PROC-02
  - PROC-03
must_haves:
  truths:
    - "弹窗打开时，用户在 'CPU 占用 Top 5' 区块看到首帧'—'，约 1.5s 后切换为实时 CPU%（PROC-01）"
    - "弹窗打开时，用户在 '内存占用 Top 5' 区块立即看到进程名 + 内存大小（PROC-02）"
    - "弹窗关闭后采样循环被取消（resourceSampleTask?.cancel()）且快照 map 被清空（clearSnapshot()），无后台 CPU 开销（PROC-03）"
    - "ProcessMetricRow 泛型视图被网络/CPU/内存三处复用；现有网络进程列表继续正常显示 ↑↓ 双值"
    - "DashboardState 新增 topCPUProcesses、topMemoryProcesses、resourceLoading @Published 字段"
    - "PopoverManager 新增 resourceSampleTask: Task<Void,Never>? 属性，在 toggle() 打开时启动，在 popoverDidClose() 取消并重置"
    - "整体构建通过（xcodebuild exit 0），无 Swift 6 并发错误"
  artifacts:
    - path: "MacStatus/MacStatus/UI/PopoverManager.swift"
      provides: "resourceSampleTask 属性 + startResourceSampling() + popoverDidClose() 扩展"
      contains: "resourceSampleTask"
    - path: "MacStatus/MacStatus/UI/Views/DashboardView.swift"
      provides: "DashboardState 新字段 + CPU/内存两个 Top-N 区块"
      contains: "topCPUProcesses"
    - path: "MacStatus/MacStatus/UI/Views/ProcessListView.swift"
      provides: "ProcessMetricRow<Trailing: View> 泛型组件 + ProcessListView 重构"
      contains: "ProcessMetricRow"
  key_links:
    - from: "PopoverManager.toggle()"
      to: "startResourceSampling()"
      via: "popover 打开后立即调用，与 refreshProcessList() 并列"
      pattern: "startResourceSampling"
    - from: "PopoverManager.popoverDidClose()"
      to: "resourceSampleTask?.cancel() + resourceReader.clearSnapshot()"
      via: "PROC-03 门控：关闭即停，快照清零"
      pattern: "clearSnapshot"
    - from: "DashboardView body"
      to: "ProcessResourceSectionView / ProcessMetricRow"
      via: "state.topCPUProcesses + state.topMemoryProcesses 驱动两个新区块"
      pattern: "topCPUProcesses"
---

<objective>
接入 Plan 01 产出的 ProcessResourceReader，完成端到端的 popover 门控采样链：PopoverManager 添加 1.5s 循环启停逻辑；DashboardState 添加 CPU/内存 Top-N 的 @Published 字段；DashboardView 插入两个新区块；ProcessListView 抽出通用 ProcessMetricRow 并将网络行重构为使用它。

覆盖需求：PROC-01（CPU Top 5 可见）、PROC-02（内存 Top 5 可见）、PROC-03（采样门控：打开启动、关闭停止）。

Purpose: 将 libproc 采样结果完整地渲染到用户界面，同时保证关闭弹窗后零后台开销。
Output: 可运行的 macOS 应用——弹窗打开显示 CPU/内存 Top 5，关闭后采样停止。
</objective>

<execution_context>
@/Users/halo/work/my-project/mac-status/.planning/phases/08-per-process-top-n-cpu-memory/08-RESEARCH.md
@/Users/halo/work/my-project/mac-status/.planning/phases/08-per-process-top-n-cpu-memory/08-PATTERNS.md
@/Users/halo/work/my-project/mac-status/.planning/phases/08-per-process-top-n-cpu-memory/08-01-SUMMARY.md
</execution_context>

<context>
@/Users/halo/work/my-project/mac-status/.planning/ROADMAP.md
@/Users/halo/work/my-project/mac-status/MacStatus/MacStatus/UI/PopoverManager.swift
@/Users/halo/work/my-project/mac-status/MacStatus/MacStatus/UI/Views/DashboardView.swift
@/Users/halo/work/my-project/mac-status/MacStatus/MacStatus/UI/Views/ProcessListView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: PopoverManager — 添加 resourceSampleTask 采样循环启停（PROC-03）</name>
  <files>MacStatus/MacStatus/UI/PopoverManager.swift</files>

  <read_first>
    1. PopoverManager.swift 完整内容（已在 context 中）——关键位置：
       - line 23：private var processRefreshTask: Task<Void, Never>?（新属性紧跟其后）
       - line 65：refreshProcessList()（toggle() 中的调用点；startResourceSampling() 紧随其后添加）
       - lines 103-105：popoverDidClose（在 stopOutsideClickMonitor() 后添加 cancel + clearSnapshot + reset）
       - lines 118-139：refreshProcessList() 完整实现（采样循环的结构范本）
    2. PATTERNS.md §"New task property" — 新增两行：private var resourceSampleTask: Task<Void, Never>? 和 private let resourceReader = ProcessResourceReader()
    3. PATTERNS.md §"Sampling loop pattern" — startResourceSampling() 完整实现蓝图：while !Task.isCancelled 循环，Task.detached(priority: .utility) 调用 reader.sample()，await MainActor.run 更新 DashboardState，try? await Task.sleep(nanoseconds: 1_500_000_000)。
    4. PATTERNS.md §"toggle() call site" — 确认 startResourceSampling() 在 refreshProcessList() 之后调用。
    5. PATTERNS.md §"popoverDidClose()" — 添加顺序：resourceSampleTask?.cancel()、resourceSampleTask = nil、resourceReader.clearSnapshot()、dashboardState.resourceLoading = true（供下次打开显示 spinner）。
    6. RESEARCH.md §"PopoverManager 采样循环启停模式" — 对照完整代码示例确认 [weak self] 用法和 guard !Task.isCancelled 检查位置。
  </read_first>

  <action>
    修改 PopoverManager.swift，三处变更：

    变更 A — 属性声明（紧接 private var processRefreshTask 行之后添加）：
    - private var resourceSampleTask: Task<Void, Never>?
    - private let resourceReader = ProcessResourceReader()
    - dashboardState.resourceLoading 初始值为 true（DashboardState 初始化时需声明，见 Task 2）

    变更 B — 新增 startResourceSampling() 方法（放在 refreshProcessList() 同一 MARK 区域）：
    - resourceSampleTask?.cancel()（取消上一次，防止 popover 重复打开叠加）
    - resourceSampleTask = Task { [weak self] in ... }，循环体：while !Task.isCancelled；guard let self；Task.detached(priority: .utility) 调用 [reader = self.resourceReader] reader.sample()；guard !Task.isCancelled；await MainActor.run 更新 dashboardState.topCPUProcesses、topMemoryProcesses、resourceLoading = false；try? await Task.sleep(nanoseconds: 1_500_000_000)。
    - 注意：resourceLoading 在循环第一帧完成后才置 false，首帧期间 spinner 可见。

    变更 C — popoverDidClose() 扩展（在 stopOutsideClickMonitor() 调用后追加）：
    - resourceSampleTask?.cancel()
    - resourceSampleTask = nil
    - resourceReader.clearSnapshot()（下次打开时首帧重新显示"—"，per PROC-03 关闭清零要求）
    - dashboardState.resourceLoading = true（重置 spinner 状态，下次打开立即显示 Sampling…）

    变更 D — toggle() 调用点（在 refreshProcessList() 调用行之后添加）：
    - startResourceSampling()

    不改动：现有 refreshProcessList() 逻辑、processRefreshTask 生命周期、outside-click monitor 逻辑。
  </action>

  <verify>
    <automated>
      grep -c 'resourceSampleTask' MacStatus/MacStatus/UI/PopoverManager.swift && grep -c 'clearSnapshot' MacStatus/MacStatus/UI/PopoverManager.swift
    </automated>
  </verify>

  <acceptance_criteria>
    - grep -c 'resourceSampleTask' MacStatus/MacStatus/UI/PopoverManager.swift 返回 >= 3（属性声明 + cancel + nil 赋值）
    - grep -c 'startResourceSampling' MacStatus/MacStatus/UI/PopoverManager.swift 返回 >= 2（方法定义 + toggle() 调用点）
    - grep -c 'clearSnapshot' MacStatus/MacStatus/UI/PopoverManager.swift 返回 >= 1（popoverDidClose 中调用）
    - grep -c 'resourceLoading' MacStatus/MacStatus/UI/PopoverManager.swift 返回 >= 2（startResourceSampling 中 = false 和 popoverDidClose 中 = true）
    - 现有 refreshProcessList 仍完整存在（grep -c 'refreshProcessList' 返回 >= 2）
    - xcodebuild BUILD SUCCEEDED（在 Task 2 和 Task 3 完成后一并验证）
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2 + 3: DashboardState 新字段 + DashboardView 两个 Top-N 区块 + ProcessMetricRow 泛化</name>
  <files>
    MacStatus/MacStatus/UI/Views/DashboardView.swift,
    MacStatus/MacStatus/UI/Views/ProcessListView.swift
  </files>

  <read_first>
    DashboardView.swift（已在 context 中）：
    - lines 262-381：DashboardState class——现有字段布局；在 "// Processes" 注释块（lines 289-291）之后添加新字段。
    - lines 66-71：DashboardView body 中 ProcessListView 的位置——新的两个区块紧跟其后插入（电池区块之后、ProcessListView 之后）。
    - lines 148-154：MetricCardWithSparkline 的卡片背景样式（RoundedRectangle cornerRadius 8 / Color.primary.opacity(0.04)）——新区块沿用此风格。

    ProcessListView.swift（已在 context 中）：
    - lines 57-91：现有 private struct ProcessRow——将其泛化为 ProcessMetricRow<Trailing: View>；ProcessListView 内部改用 ProcessMetricRow。
    - lines 19-45：loading/error/empty state 渲染模式——新 ProcessResourceSectionView 照搬此模式。
    - lines 47-51：.padding(10) + RoundedRectangle 背景——新区块 container 复用。

    PATTERNS.md §"Generalized ProcessMetricRow" — 泛型组件精确定义（processName、pid: Int32?、@ViewBuilder trailing）。
    PATTERNS.md §"New sections in DashboardView body" — 两个新区块的结构蓝图（ProcessResourceSectionView 或等效内联 VStack）。
    RESEARCH.md §"ProcessMetricRow 泛化设计" — CPU 行 trailing（cpuPercent.map { "%.1f%%" } ?? "—"）和内存行 trailing（ByteFormatting.format(Double(proc.memoryBytes))）的精确格式。
  </read_first>

  <action>
    修改 DashboardView.swift，两处变更：

    变更 A — DashboardState 新 @Published 字段（在 "// Processes" 区块的现有三个字段之后添加）：
    - @Published var topCPUProcesses: [ProcessResourceUsage] = []
    - @Published var topMemoryProcesses: [ProcessResourceUsage] = []
    - @Published var resourceLoading: Bool = true
    - @Published var resourceError: String? = nil

    变更 B — DashboardView body（在现有 ProcessListView(...) 之后、Footer HStack 之前插入两个新 VStack 区块）：

    CPU Top 5 区块结构（VStack，沿用 RoundedRectangle 卡片背景）：
    - 标题 Text("CPU 占用 Top 5") — font(.caption)，foregroundStyle(.secondary)
    - 状态分支（isLoading → spinner + "Sampling…"；items 为空 → "无数据"；否则 ForEach）
    - ForEach 使用 ProcessMetricRow(processName: proc.processName, pid: proc.pid) trailing：Text(proc.cpuPercent.map { String(format: "%.1f%%", $0) } ?? "—").font(.system(.caption2, design: .monospaced))
    - 绑定 state.topCPUProcesses 和 state.resourceLoading

    内存 Top 5 区块结构（同上）：
    - 标题 Text("内存占用 Top 5")
    - ForEach 使用 ProcessMetricRow trailing：Text(ByteFormatting.format(Double(proc.memoryBytes))).font(.system(.caption2, design: .monospaced))
    - 绑定 state.topMemoryProcesses 和 state.resourceLoading

    两个区块共享 resourceLoading（首帧两者均显示 spinner，第一帧完成后同时切换为数据）。

    修改 ProcessListView.swift，两处变更：

    变更 C — 新增 struct ProcessMetricRow<Trailing: View>: View（在现有 ProcessRow 的位置，改为 internal 级别以供其他视图使用）：
    - 字段：let processName: String、let pid: Int32?、@ViewBuilder let trailing: () -> Trailing
    - body：HStack(spacing: 6) 包含 Text(processName)（.caption2，lineLimit(1)，truncationMode(.tail)），可选 Text("(\(pid))")（.caption2 monospaced，.tertiary），Spacer()，trailing()
    - .padding(.vertical, 1)

    变更 D — ProcessListView 中的 ForEach 改用 ProcessMetricRow（替换 ProcessRow 调用）：
    ProcessMetricRow(processName: proc.processName, pid: proc.processIdentifier) {
        Label(ByteFormatting.format(proc.uploadBytesPerSec) + "/s", systemImage: "arrow.up")
            .font(.system(.caption2, design: .monospaced)).foregroundStyle(.orange)
        Label(ByteFormatting.format(proc.downloadBytesPerSec) + "/s", systemImage: "arrow.down")
            .font(.system(.caption2, design: .monospaced)).foregroundStyle(.blue)
    }

    删除原有 private struct ProcessRow（已被 ProcessMetricRow 取代）。

    注意：ProcessMetricRow 声明为 struct（非 private），DashboardView 中的两个新区块可直接引用它。ByteFormatting.format() 接受 Double，传入 Double(proc.memoryBytes) 即可复用现有格式化逻辑。ForEach 使用 enumerated + id: \.offset（与现有 ProcessListView 一致）。空状态文案用"无数据"（对应网络区块的英文"No active network processes"保持现有，不改动）。
  </action>

  <verify>
    <automated>
      grep -c 'topCPUProcesses' MacStatus/MacStatus/UI/Views/DashboardView.swift && grep -c 'ProcessMetricRow' MacStatus/MacStatus/UI/Views/ProcessListView.swift && xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build.noindex CODE_SIGN_STYLE=Manual CODE_SIGN_IDENTITY=- build 2>&1 | grep -E "BUILD SUCCEEDED|BUILD FAILED|error:"
    </automated>
  </verify>

  <acceptance_criteria>
    - grep -c 'topCPUProcesses' MacStatus/MacStatus/UI/Views/DashboardView.swift 返回 >= 2（DashboardState 字段声明 + DashboardView body 引用）
    - grep -c 'topMemoryProcesses' MacStatus/MacStatus/UI/Views/DashboardView.swift 返回 >= 2
    - grep -c 'resourceLoading' MacStatus/MacStatus/UI/Views/DashboardView.swift 返回 >= 2
    - grep -c 'ProcessMetricRow' MacStatus/MacStatus/UI/Views/ProcessListView.swift 返回 >= 2（struct 定义 + ForEach 内使用）
    - grep -c 'ProcessRow' MacStatus/MacStatus/UI/Views/ProcessListView.swift 返回 0（旧 ProcessRow 已完全替换）
    - grep -c 'CPU 占用 Top 5' MacStatus/MacStatus/UI/Views/DashboardView.swift 返回 >= 1
    - grep -c '内存占用 Top 5' MacStatus/MacStatus/UI/Views/DashboardView.swift 返回 >= 1
    - grep -c 'ByteFormatting.format' MacStatus/MacStatus/UI/Views/DashboardView.swift 返回 >= 1（内存区块使用）
    - xcodebuild 输出 "BUILD SUCCEEDED"，不含 "error:"，不含 Swift 6 并发违规
    - 行为验证（人工）：打开弹窗 → CPU Top 5 首帧显示"Sampling…"spinner，约 1.5s 后切换为进程名 + "—" 或实时%；内存 Top 5 加载后立即显示进程名 + 内存大小；关闭弹窗 → 控制台/Instruments 无持续 libproc 采样活动
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Task.detached(.utility) → @MainActor DashboardState | [ProcessResourceUsage] Sendable 数组跨 actor 边界；Plan 01 的 ProcessResourceUsage: Sendable 保证合法性 |
| PopoverManager @MainActor → Task.detached | 通过 [reader = self.resourceReader] 捕获，resourceReader 为常量（let）；Task 独占，无竞争 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Denial of Service | 采样循环未取消（Pitfall 4）| mitigate | popoverDidClose 中 resourceSampleTask?.cancel() + nil + clearSnapshot()；resourceLoading = true 重置 |
| T-08-07 | Tampering | ProcessMetricRow 中 cpuPercent nil 渲染 | accept | nil → "—" 字符串；非数值，无注入面 |
| T-08-08 | Info Disclosure | 进程名显示（proc_name 权限受限）| accept | proc_name 返回 0 时用 "PID \(pid)" fallback（Plan 01 处理）；UI 层透明展示，非敏感操作 |
| T-08-09 | Spoofing | processRefreshTask 与 resourceSampleTask 同时运行 | accept | 两个 Task 独立，互不干扰；dashboardState 更新均在 MainActor，串行执行 |
</threat_model>

<verification>
Plan 02 验收（执行者在提交 SUMMARY 前自查）：

1. PopoverManager 属性：grep -c 'resourceSampleTask' >= 3，grep -c 'clearSnapshot' >= 1
2. startResourceSampling 调用点：grep -c 'startResourceSampling' >= 2（定义 + toggle 调用）
3. DashboardState 新字段：grep -c 'topCPUProcesses' DashboardView.swift >= 2
4. ProcessMetricRow 存在且 ProcessRow 已移除：grep -c 'ProcessMetricRow' ProcessListView.swift >= 2，grep -c 'ProcessRow' ProcessListView.swift == 0
5. 两个新区块标题：grep -c 'CPU 占用 Top 5' DashboardView.swift >= 1，grep -c '内存占用 Top 5' >= 1
6. pbxproj 仍完整：grep -c 'ProcessResourceReader.swift in Sources' project.pbxproj >= 1（未被回归）
7. 构建通过：xcodebuild BUILD SUCCEEDED，无 error:，无 Swift 6 并发错误
8. 行为验证：打开弹窗首帧 spinner，1.5s 后 CPU Top 5 "—"再转实时%；内存 Top 5 即时；关闭弹窗后无后台采样
</verification>

<success_criteria>
- 弹窗打开时两个新区块渲染正确：CPU Top 5 首帧"Sampling…"+ "—"→实时%（PROC-01），内存 Top 5 立即显示（PROC-02）
- 弹窗关闭后 resourceSampleTask 取消、快照 map 清空——下次打开重新显示 spinner（PROC-03）
- 现有网络进程列表（ProcessListView + ProcessMetricRow ↑↓ 双值）继续正常工作，无回归
- xcodebuild BUILD SUCCEEDED，无 Swift 6 严格并发错误
- Phase 8 全部需求 PROC-01、PROC-02、PROC-03 均已实现并可人工验证
</success_criteria>

<output>
执行完成后创建 .planning/phases/08-per-process-top-n-cpu-memory/08-02-SUMMARY.md，记录：
- PopoverManager 实际修改的行范围（属性、startResourceSampling、popoverDidClose、toggle）
- DashboardState 新字段列表
- DashboardView 两个新区块的 VStack 结构摘要
- ProcessMetricRow 最终签名
- 行为验证结果（首帧/关闭后）
- 任何与 PATTERNS.md 蓝图的实际偏差及原因
- 构建输出关键行（BUILD SUCCEEDED）
</output>
