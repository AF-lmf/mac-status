---
phase: "09-settings-window-ui-customization"
plan: "02"
type: execute
wave: 2
depends_on:
  - "09-01"
files_modified:
  - MacStatus/MacStatus/UI/Views/SettingsView.swift
autonomous: true
requirements:
  - SET-01
  - SET-02
  - SET-03
  - SET-06
must_haves:
  truths:
    - "设置窗口通过右键菜单 SettingsWindowManager.showSettings() 打开（已存在，无需改动）"
    - "Form 包含 8 个 Section，按 通用/状态栏指标/弹窗区块/状态栏文字模式/告警阈值/配色/数据/关于 顺序排列"
    - "Section 通用 保留刷新间隔 Picker 和登录时启动 Toggle，移除显示模式行"
    - "Section 状态栏指标 包含 List { ForEach(settings.metricOrder).onMove }，List 有 .frame(height: 144) 和 .listStyle(.plain)"
    - "MetricOrderRow 是内嵌私有 struct，持有 @Bindable var settings 和 @State var isHoveringHandle，Toggle 绑定 enabledBinding 计算属性（非 body 内局部 Binding）"
    - "源码中不含任何 editMode 或 EditMode 引用（macOS 不可用，无法编译）"
    - "Section 弹窗区块 包含两个 Toggle 绑定 $settings.showBatterySection 和 $settings.showProcessSection"
    - "Section 状态栏文字模式 是独立 section（从通用移出），Picker 用 .pickerStyle(.segmented)，标签 详细/紧凑/百分比"
    - "原 Section 告警阈值 的全局 CPU/MEM 滑块已移除，由 Plan 03 的按指标版本替代（Plan 02 先用占位注释）"
    - "Section 数据 和 Section 关于 保留（关于版本改为 v2.0）"
    - "SettingsView.body 的 Form .frame 修饰符从 .frame(width: 380, height: 440) 改为 .frame(width: 420).frame(minHeight: 400)（frame 在 SettingsView，不在 SettingsWindowManager）"
    - "所有计算 Binding 定义在子 struct 的计算属性中，未在 var body 内局部构造"
    - "xcodebuild 编译成功，exit 0"
  artifacts:
    - path: "MacStatus/MacStatus/UI/Views/SettingsView.swift"
      provides: "完整 8-section Form + MetricOrderRow 内嵌 struct"
      contains: ".onMove"
    - path: "MacStatus/MacStatus/UI/Views/SettingsView.swift"
      provides: "SettingsWindowManager 新尺寸"
      contains: "frame(width: 420)"
  key_links:
    - from: "MetricOrderRow.enabledBinding"
      to: "settings.enabledMetrics"
      via: "Binding(get:set:) 计算属性"
      pattern: "enabledMetrics\\.contains\\(metric\\)"
    - from: "ForEach.onMove closure"
      to: "settings.metricOrder"
      via: "metricOrder.move(fromOffsets:toOffset:)"
      pattern: "metricOrder\\.move\\(fromOffsets"
---

<objective>
Plan 02：重构 SettingsView，新增"状态栏指标"（含可拖动重排 List + Toggle）、"弹窗区块"、"状态栏文字模式"三个 section；同步移除通用 section 的显示模式行；移除旧全局 CPU/MEM 告警阈值滑块（以占位注释替换，由 Plan 03 实现按指标版本）。

Purpose: 覆盖 SET-01（设置窗口已存在，SettingsWindowManager 不变）、SET-02（开关指标 + 弹窗区块）、SET-03（拖动排序）、SET-06（紧凑/详细文字模式切换，displayMode Picker）。所有改造集中在 SettingsView.swift 一个文件，所有新子 struct 内嵌于该文件（避免 pbxproj 变更）。注：SET-04 阈值 / SET-05 配色在 Plan 03。

Output: 重写 SettingsView.swift — 8-section Form + 内嵌 MetricOrderRow struct + 更新 SettingsWindowManager 尺寸。无新文件，无 pbxproj 变更。
</objective>

<execution_context>
@/Users/halo/work/my-project/mac-status/.claude/gsd-core/workflows/execute-plan.md
@/Users/halo/work/my-project/mac-status/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/halo/work/my-project/mac-status/.planning/ROADMAP.md
@/Users/halo/work/my-project/mac-status/.planning/phases/09-settings-window-ui-customization/09-CONTEXT.md
@/Users/halo/work/my-project/mac-status/.planning/phases/09-settings-window-ui-customization/09-UI-SPEC.md
@/Users/halo/work/my-project/mac-status/.planning/phases/09-settings-window-ui-customization/09-RESEARCH.md
@/Users/halo/work/my-project/mac-status/.planning/phases/09-settings-window-ui-customization/09-PATTERNS.md
@/Users/halo/work/my-project/mac-status/.planning/phases/09-settings-window-ui-customization/09-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1：Section 框架重构 + 状态栏指标 List + 弹窗区块 Toggles + 文字模式 Picker</name>
  <files>MacStatus/MacStatus/UI/Views/SettingsView.swift</files>
  <read_first>
    - MacStatus/MacStatus/UI/Views/SettingsView.swift — 完整读取（当前 129 行）：
      - 现有 Section("通用") 第 13–38 行：刷新间隔 Picker + 显示模式 Picker + 登录时启动 Toggle；Plan 02 保留前两项，移出显示模式，整体保留刷新间隔 + 登录时启动
      - 现有 Section("告警阈值") 第 41–57 行：全局 CPU/MEM 滑块，Plan 02 完全移除，用注释 // TODO: 由 Plan 03 替换为按指标 ThresholdSubsection 占位
      - 现有 Section("数据") 第 60–73 行：保留原样
      - 现有 Section("关于") 第 76–94 行：保留，版本号改为 v2.0
      - SettingsWindowManager.showSettings() 第 119 行：NSHostingController 创建，窗口无 setContentSize；但 SettingsView.body 第 92 行有 .frame(width: 380, height: 440)，需改为 .frame(width: 420).frame(minHeight: 400)
    - 09-RESEARCH.md Q1 节（第 163–289 行）：macOS 14 正确拖动重排模式的完整代码，包括：
      - Section("状态栏指标") List + ForEach.onMove 骨架（不含 EditMode）
      - MetricOrderRow 私有 struct 完整实现（含 @State isHoveringHandle、enabledBinding 计算属性、moveDisabled 修饰符）
      - .frame(height: CGFloat(settings.metricOrder.count) * 36) + .listStyle(.plain) 的必要性说明
    - 09-PATTERNS.md "SettingsView.swift — New and modified sections" 节：显示模式 Picker 重标题迁移、Section 顺序、Divider 模式
    - 09-RESEARCH.md Q7 节：Form 滚动策略和 ColorPicker 布局（窗口宽度 420）
    - 09-RESEARCH.md 陷阱 1（EditMode）、陷阱 2（List 高度折叠）、陷阱 3（body 内局部 Binding）

    关键模式引用（从 RESEARCH.md Q1 直接使用，不要手写）：
    - MetricOrderRow 的 enabledBinding 计算属性（contains + append/removeAll）
    - .moveDisabled(!isHoveringHandle) + .onHover 模式（显式拖动柄）
    - Metric.displayName switch（cpu→"CPU"，memory→"内存"，network→"网络"，gpu→"GPU"）
  </read_first>
  <action>
    完整重写 SettingsView.swift，保留所有已有的 import 和 SettingsWindowManager 类，按以下规格实现：

    SettingsView.body Form 结构（8 个 section，顺序固定）：

    Section 1 "通用"：保留刷新间隔 Picker（现有 HStack 不变），保留登录时启动 Toggle；移除显示模式 HStack/Picker 行（从通用移出，进入 Section 4）。

    Section 2 "状态栏指标"：
    - 包含一个 List { ForEach(settings.metricOrder) { metric in MetricOrderRow(metric: metric, settings: settings) }.onMove { from, to in settings.metricOrder.move(fromOffsets: from, toOffset: to) } }
    - List 修饰符：.frame(height: CGFloat(settings.metricOrder.count) * 36)、.listStyle(.plain)
    - 绝对禁止：任何 editMode / EditMode 引用（macOS SDK 标记为 unavailable，会导致编译失败）

    Section 3 "弹窗区块"：
    - Toggle("电池区块", isOn: $settings.showBatterySection)
    - Toggle("进程区块", isOn: $settings.showProcessSection)

    Section 4 "状态栏文字模式"：
    - 用 LabeledContent 包裹避免 grouped Form 下空标签产生空白左列：LabeledContent("文字模式") { Picker("", selection: $settings.displayMode) { … }.pickerStyle(.segmented) }；或直接给 Picker 一个非空标签。三个 tag：Text("详细").tag(DisplayMode.full)、Text("紧凑").tag(DisplayMode.compact)、Text("百分比").tag(DisplayMode.percentage)
    - .pickerStyle(.segmented)（标签经 LabeledContent 提供，勿留裸空标签）

    Section 5 "告警阈值"（占位，由 Plan 03 替换）：
    - 在 Section 内放一个注释行和空占位 Text("（由 Plan 03 实现按指标编辑）").foregroundStyle(.secondary)，让 Form 在 Plan 03 完成前仍可编译。绝对不保留旧的全局 $settings.cpuWarningThreshold/$settings.memoryWarningThreshold 滑块（Plan 03 会用 ThresholdSubsection 替换，保留旧代码会导致逻辑重复）。

    Section 6 "配色"（占位，由 Plan 03 替换）：
    - 同 Section 5 占位：Text("（由 Plan 03 实现按指标配色）").foregroundStyle(.secondary)

    Section 7 "数据"：原样保留（SettingsView.swift 第 60–73 行不变）。

    Section 8 "关于"：保留，v1.0 (M002) 改为 v2.0 (M009)（executor 按实际版本填写；此处写 v2.0）。

    Form 修饰符：
    - .formStyle(.grouped)（不变）
    - 将原 .frame(width: 380, height: 440) 改为 .frame(width: 420).frame(minHeight: 400)（适应 ColorPicker 行宽度 + Form 可滚动）
    - .padding()（不变）

    内嵌私有 struct MetricOrderRow（在 SettingsView 的 MARK 扩展内，不拆分为独立文件）：
    - private struct MetricOrderRow: View
    - let metric: Metric 和 @Bindable var settings: SettingsManager
    - @State private var isHoveringHandle = false
    - body：HStack 内 Image(systemName: "line.3.horizontal").onHover { isHoveringHandle = $0 } + Text(metric.displayName).foregroundStyle(...) + Spacer() + Toggle("", isOn: enabledBinding).labelsHidden().toggleStyle(.switch)
    - .moveDisabled(!isHoveringHandle) 修饰符（控制仅悬停拖动柄时允许拖动）
    - 计算属性 private var enabledBinding: Binding<Bool>（定义在 struct 的计算属性，不在 body 内）：读时 settings.enabledMetrics.contains(metric)；写时 enabled ? append(若不存在) : removeAll { $0 == metric }

    Metric.displayName 扩展（内嵌在文件末尾，不在 SettingsView 内部）：
    - extension Metric { var displayName: String { switch self { case .cpu: "CPU"; case .memory: "内存"; case .network: "网络"; case .gpu: "GPU"; case .battery: "电池" } } }
    - 注意 .battery 不出现在 metricOrder 列表中，但 displayName 提供其名称以防 switch exhaustive 警告

    约束（强制）：
    - 不拆分任何 struct 为独立 .swift 文件（全部内嵌于 SettingsView.swift），无需 pbxproj 变更
    - MetricOrderRow.enabledBinding 是计算属性，不在 body 内构造（per 陷阱 3）
    - List 必须有显式 .frame(height:)（per 陷阱 2，4 行 × 36pt = 144pt）
    - 源码中零 editMode/EditMode 引用（per 陷阱 1）
    - SettingsWindowManager 类保持不变（仅 SettingsView.body 的 .frame 修饰符变化）
  </action>
  <verify>
    <automated>cd /Users/halo/work/my-project/mac-status && xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build.noindex build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    源码断言（grep，任一失败则任务未完成）：
    - grep -c "\.onMove" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1
    - grep -ic "editmode\|editMode\|\.editMode\|EditMode" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 0（零 EditMode 引用）
    - grep -c "showBatterySection" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1
    - grep -c "showProcessSection" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1
    - grep -c "状态栏指标" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1
    - grep -c "弹窗区块" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1
    - grep -c "状态栏文字模式" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1
    - grep -c "pickerStyle(.segmented)" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1
    - grep -c "frame(width: 420)" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1
    - grep -c "MetricOrderRow" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 2（定义 + 使用）
    - grep -c "isHoveringHandle" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1
    - grep -c "moveDisabled" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1
    - grep -c "enabledBinding" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 2（定义 + 使用）
    - 旧全局滑块已移除：grep -c "cpuWarningThreshold\|memoryWarningThreshold" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 0
    - 无新 .swift 文件：ls MacStatus/MacStatus/UI/Views/ 中不出现新文件
    构建断言：xcodebuild exit 0
    行为意图（不可自动化，记录供人工验证）：
    - 打开设置窗口 → Section 2 可见 4 行指标，可拖动重排（鼠标悬停"≡"图标时拖动）→ 状态栏顺序立即变化
    - Toggle 关闭一个指标 → 状态栏对应段消失，再开启 → 立即恢复
    - 弹窗区块两个 Toggle → 打开弹窗验证电池/进程区块响应
    - 状态栏文字模式 segmented picker → 切换三模式 → 状态栏文字格式即时变化
  </acceptance_criteria>
  <done>SettingsView.swift 包含 8-section Form（通用/状态栏指标/弹窗区块/状态栏文字模式/告警阈值占位/配色占位/数据/关于）；MetricOrderRow 内嵌；零 editMode 引用；List 有显式高度；displayMode Picker 迁入独立 section 使用 .segmented；xcodebuild exit 0</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| SwiftUI 控件 → SettingsManager setter | 用户输入（Toggle/Picker）通过 @Bindable 写入 SettingsManager；setter 已做类型安全校验 |

## STRIDE 威胁登记

| Threat ID | 类别 | 组件 | 处置 | 缓解措施 |
|-----------|------|------|------|---------|
| T-09-02-T | Tampering | metricOrder.move 拖动写入 | accept | 值域为有限 Metric enum 数组；move(fromOffsets:toOffset:) 是 stdlib 安全 API，无越界风险 |
| T-09-02-I | Information Disclosure | enabledBinding 读 enabledMetrics | accept | 仅展示本地用户自己的指标状态，无隐私数据 |
| T-09-02-SC | Tampering | 供应链 | accept | 零新外部包；纯 SDK 控件 |
</threat_model>

<verification>
构建验证：
cd /Users/halo/work/my-project/mac-status && xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build.noindex build 2>&1 | tail -10

关键源码检查：
- grep -ic "editmode\|EditMode" MacStatus/MacStatus/UI/Views/SettingsView.swift 必须返回 0
- grep -c "\.onMove" MacStatus/MacStatus/UI/Views/SettingsView.swift 必须 >= 1
- grep -c "pickerStyle(.segmented)" MacStatus/MacStatus/UI/Views/SettingsView.swift 必须 >= 1
</verification>

<success_criteria>
- SettingsView.swift 包含 8 个正确排序的 Section（通用→状态栏指标→弹窗区块→状态栏文字模式→告警阈值占位→配色占位→数据→关于）
- List.onMove 拖动排序有效，零 editMode 引用
- MetricOrderRow enabledBinding 是计算属性而非 body 内局部 Binding（防无限重渲染）
- displayMode Picker 已迁移至独立 section，.pickerStyle(.segmented)，三标签 详细/紧凑/百分比
- 弹窗区块 Toggle 绑定 $settings.showBatterySection/$settings.showProcessSection（Plan 01 新键）
- 窗口宽度 420pt，Form 可滚动
- xcodebuild exit 0
</success_criteria>

<output>
完成后创建 .planning/phases/09-settings-window-ui-customization/09-02-SUMMARY.md
</output>
