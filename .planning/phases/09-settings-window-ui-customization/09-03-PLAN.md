---
phase: "09-settings-window-ui-customization"
plan: "03"
type: execute
wave: 3
depends_on:
  - "09-02"
files_modified:
  - MacStatus/MacStatus/UI/Views/SettingsView.swift
  - MacStatus/MacStatus/UI/Views/DashboardView.swift
autonomous: true
requirements:
  - SET-02
  - SET-04
  - SET-05
  - SET-06
must_haves:
  truths:
    - "Section 告警阈值 包含按指标（cpu/memory/gpu）分组的 warning + critical Slider，移除 Plan 02 的占位文字"
    - "Section 配色 包含按指标（cpu/memory/gpu）分组的 ColorPicker（warning + critical），移除 Plan 02 的占位文字"
    - "ThresholdSubsection 和 ColorSubsection 是内嵌私有 struct，计算 Binding 定义在各自的计算属性中（非 body 内）"
    - "ThresholdSubsection.warningBinding/criticalBinding 是 Binding<Double>，通过 settings.customThresholds[metric.rawValue] 读写"
    - "ColorSubsection.warningColorBinding/criticalColorBinding 是 Binding<Color>，通过 NSColor(hex:)/hexString/Color(nsColor:)/NSColor(_ color:) 桥接 customColors"
    - "每个指标子块有「恢复默认」Button，清除该指标的 customThresholds/customColors key，StatusBarManager 自动回退内置色"
    - "源码中 ColorPicker 使用 supportsOpacity: false"
    - "warning < critical 约束通过 .onChange(of:) { _, new in } 双参数签名实现（Swift 6），不在 Binding setter 内互锁"
    - "DashboardView.body 中电池区块改为 if settings.showBatterySection && state.hasBattery"
    - "DashboardView.body 中进程相关三个区块（ProcessListView + CPU Top 5 + 内存 Top 5）用 if settings.showProcessSection 包裹"
    - "DashboardView.body 使用 let settings = SettingsManager.shared 建立 @Observable 追踪，不引入 @State 或 @EnvironmentObject"
    - "无新 .swift 文件；ThresholdSubsection 和 ColorSubsection 内嵌于 SettingsView.swift"
    - "xcodebuild 编译成功，exit 0"
  artifacts:
    - path: "MacStatus/MacStatus/UI/Views/SettingsView.swift"
      provides: "ThresholdSubsection + ColorSubsection 内嵌 struct；完整 8-section Form"
      contains: "ColorPicker"
    - path: "MacStatus/MacStatus/UI/Views/DashboardView.swift"
      provides: "showBatterySection + showProcessSection 门控"
      contains: "showBatterySection"
  key_links:
    - from: "ThresholdSubsection.thresholdBinding(for:default:)"
      to: "settings.customThresholds"
      via: "Binding(get:set:) 计算属性"
      pattern: "customThresholds\\[metric\\.rawValue\\]"
    - from: "ColorSubsection.colorBinding(for:defaultHex:)"
      to: "settings.customColors"
      via: "NSColor(hex:) / NSColor(_ color:) / Color(nsColor:)"
      pattern: "customColors\\[metric\\.rawValue\\]"
    - from: "DashboardView.body"
      to: "SettingsManager.shared"
      via: "let settings = SettingsManager.shared（@Observable 自动追踪）"
      pattern: "SettingsManager\\.shared"
---

<objective>
Plan 03：完成 SettingsView 的最后两个 section（告警阈值 + 配色），并为 DashboardView 添加弹窗区块可见性门控。

Purpose: 覆盖 SET-04（自定义阈值）、SET-05（自定义配色）；完成 SET-02 的弹窗区块分支（DashboardView 门控）；确保 SET-06（即时生效）+ SET-07（持久化）已被所有控件满足。这是 Phase 9 的最后一个执行计划，完成后 v2.0 全部 6 个 SET 需求均已覆盖。

Output:
- SettingsView.swift：用 ThresholdSubsection 替换 Section 5 占位 + 用 ColorSubsection 替换 Section 6 占位；内嵌两个私有 struct
- DashboardView.swift：电池区块加 settings.showBatterySection 外层门控；进程三区块加 settings.showProcessSection 门控
无新文件，无 pbxproj 变更。
</objective>

<execution_context>
@/Users/halo/work/my-project/mac-status/.claude/gsd-core/workflows/execute-plan.md
@/Users/halo/work/my-project/mac-status/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/halo/work/my-project/mac-status/.planning/ROADMAP.md
@/Users/halo/work/my-project/mac-status/.planning/phases/09-settings-window-ui-customization/09-CONTEXT.md
@/Users/halo/work/my-project/mac-status/.planning/phases/09-settings-window-ui-customization/09-UI-SPEC.md
@/Users/halo/work/my-project/mac-status/.planning/phases/09-settings-window-ui-customization/09-RESEARCH.md
@/Users/halo/work/my-project/mac-status/.planning/phases/09-settings-window-ui-customization/09-PATTERNS.md
@/Users/halo/work/my-project/mac-status/.planning/phases/09-settings-window-ui-customization/09-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1：SettingsView 告警阈值 + 配色 Section（替换 Plan 02 占位）</name>
  <files>MacStatus/MacStatus/UI/Views/SettingsView.swift</files>
  <read_first>
    - MacStatus/MacStatus/UI/Views/SettingsView.swift — 完整读取（Plan 02 完成后的版本）：
      重点：Section("告警阈值") 内的占位 Text（需替换）；Section("配色") 内的占位 Text（需替换）；文件末尾现有 Metric.displayName 扩展（Plan 02 已添加，不重复添加）
    - 09-RESEARCH.md Q3 节（ThresholdSubsection 完整实现）：
      - thresholdBinding(for:default:) 计算属性（非 body 内）
      - defaultWarning/defaultCritical 计算属性（从 settings.cpuWarningThreshold 等旧键读初始值）
      - .onChange(of: warningBinding.wrappedValue) { _, newWarning in } 双参数签名（Swift 6 / macOS 14）
      - warning < critical 约束：warning >= critical 时推高 critical；critical <= warning 时拉低 warning
      - resetThresholds()：var updated = settings.customThresholds; updated.removeValue(forKey: metric.rawValue); settings.customThresholds = updated
      - Button("恢复默认").buttonStyle(.borderless).foregroundStyle(.accentColor)
    - 09-RESEARCH.md Q2 节（ColorSubsection 完整实现）：
      - colorBinding(for:defaultHex:) 计算属性（非 body 内）
      - 读：NSColor(hex: hex) ?? NSColor(hex: defaultHex)! → Color(nsColor:)（macOS 12+）
      - 写：NSColor(newColor)（macOS 11+）→ .hexString → 更新 settings.customColors
      - ColorPicker("警告色", selection: warningColorBinding, supportsOpacity: false).labelsHidden().frame(width: 44)
      - HStack { Text("警告色"); Spacer(); ColorPicker(...) } 布局
      - resetColors()：removeValue(forKey: metric.rawValue) 从 customColors
    - 09-RESEARCH.md Q6 节：恢复默认语义确认（removeValue + 赋回 setter 触发 postChange）
    - 09-PATTERNS.md "ThresholdSubsection sub-struct shape" 和 "ColorSubsection sub-struct shape" 节
    - 09-PATTERNS.md "Section label + Divider cadence" 节：metrics 间用 Divider()，最后一个指标（.gpu）后不加 Divider
    - 09-RESEARCH.md 陷阱 3（body 内局部 Binding 导致无限重渲染）：所有 Binding 必须是 struct 计算属性
    - 09-RESEARCH.md 陷阱 6（onChange 签名）：使用 { _, newValue in } 双参数版本
    - MacStatus/MacStatus/Utils/NSColor+Hex.swift — 确认 NSColor(hex:) 和 .hexString 的实际接口
  </read_first>
  <action>
    在 SettingsView.swift 中：

    1. 替换 Section("告警阈值") 内容：
       将占位 Text 改为：ForEach([Metric.cpu, .memory, .gpu], id: \.rawValue) { metric in ThresholdSubsection(metric: metric, settings: settings); if metric != .gpu { Divider() } }

    2. 替换 Section("配色") 内容：
       将占位 Text 改为：ForEach([Metric.cpu, .memory, .gpu], id: \.rawValue) { metric in ColorSubsection(metric: metric, settings: settings); if metric != .gpu { Divider() } }

    3. 在文件末尾（Metric.displayName extension 之前或之后）添加两个内嵌私有 struct：

    private struct ThresholdSubsection: View：
    - let metric: Metric 和 @Bindable var settings: SettingsManager
    - defaultWarning 计算属性：cpu → settings.cpuWarningThreshold；memory → settings.memoryWarningThreshold；gpu → 60.0；default → 60.0
    - defaultCritical 计算属性：cpu → settings.cpuCriticalThreshold；memory → settings.memoryCriticalThreshold；gpu → 80.0；default → 80.0
    - body：VStack(alignment: .leading, spacing: 8) 内：
      a. Text(metric.displayName).font(.caption.weight(.semibold))
      b. Text("\(metric.displayName) 警告：\(Int(warningBinding.wrappedValue))%")
      c. Slider(value: warningBinding, in: 30...90, step: 5).onChange(of: warningBinding.wrappedValue) { _, newWarning in if newWarning >= criticalBinding.wrappedValue { criticalBinding.wrappedValue = min(newWarning + 5, 95) } }
      d. Text("\(metric.displayName) 严重：\(Int(criticalBinding.wrappedValue))%")
      e. Slider(value: criticalBinding, in: 50...95, step: 5).onChange(of: criticalBinding.wrappedValue) { _, newCritical in if newCritical <= warningBinding.wrappedValue { warningBinding.wrappedValue = max(newCritical - 5, 30) } }
      f. HStack { Spacer(); Button("恢复默认") { resetThresholds() }.buttonStyle(.borderless).foregroundStyle(.accentColor) }
    - 计算属性 private var warningBinding: Binding<Double>：thresholdBinding(for: "warning", default: defaultWarning)
    - 计算属性 private var criticalBinding: Binding<Double>：thresholdBinding(for: "critical", default: defaultCritical)
    - 函数 private func thresholdBinding(for level: String, default defaultValue: Double) -> Binding<Double>：Binding { settings.customThresholds[metric.rawValue]?[level] ?? defaultValue } set: { newValue in var updated = settings.customThresholds; var levels = updated[metric.rawValue] ?? [:]; levels[level] = newValue; updated[metric.rawValue] = levels; settings.customThresholds = updated }
    - 函数 private func resetThresholds()：copy dict → removeValue → 赋回 settings.customThresholds

    private struct ColorSubsection: View：
    - let metric: Metric 和 @Bindable var settings: SettingsManager
    - private let defaultWarningHex = "#FF9500"（橙色）
    - private let defaultCriticalHex = "#FF3B30"（红色）
    - body：VStack(alignment: .leading, spacing: 4) 内：
      a. Text(metric.displayName).font(.caption).foregroundStyle(.secondary)
      b. HStack { Text("警告色"); Spacer(); ColorPicker("警告色", selection: warningColorBinding, supportsOpacity: false).labelsHidden().frame(width: 44) }
      c. HStack { Text("严重色"); Spacer(); ColorPicker("严重色", selection: criticalColorBinding, supportsOpacity: false).labelsHidden().frame(width: 44) }
      d. HStack { Spacer(); Button("恢复默认") { resetColors() }.buttonStyle(.borderless).foregroundStyle(.accentColor) }
    - 计算属性 private var warningColorBinding: Binding<Color>：colorBinding(for: "warning", defaultHex: defaultWarningHex)
    - 计算属性 private var criticalColorBinding: Binding<Color>：colorBinding(for: "critical", defaultHex: defaultCriticalHex)
    - 函数 private func colorBinding(for level: String, defaultHex: String) -> Binding<Color>：
      get：let hex = settings.customColors[metric.rawValue]?[level] ?? defaultHex; let ns = NSColor(hex: hex) ?? NSColor(hex: defaultHex)!; return Color(nsColor: ns)
      set：let ns = NSColor(newColor); let hex = ns.hexString; var updated = settings.customColors; var levels = updated[metric.rawValue] ?? [:]; levels[level] = hex; updated[metric.rawValue] = levels; settings.customColors = updated
    - 函数 private func resetColors()：copy dict → removeValue(forKey: metric.rawValue) → 赋回 settings.customColors

    约束（强制）：
    - 所有 Binding 是计算属性，不在 var body 内局部构造（per 陷阱 3）
    - .onChange 使用双参数 { _, newValue in }（per 陷阱 6，Swift 6 / macOS 14 签名）
    - warning < critical 约束只在 .onChange 内单向推送，不在 Binding setter 内互锁（per RESEARCH.md Q3 说明）
    - ColorPicker 始终带 supportsOpacity: false
    - 无新文件（ThresholdSubsection 和 ColorSubsection 内嵌于 SettingsView.swift）
  </action>
  <verify>
    <automated>cd /Users/halo/work/my-project/mac-status && xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build.noindex build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    源码断言（grep，任一失败则任务未完成）：
    - grep -c "ColorPicker" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 2（warning + critical 各一）
    - grep -c "supportsOpacity: false" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 2
    - grep -c "ThresholdSubsection" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 2（定义 + 使用）
    - grep -c "ColorSubsection" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 2（定义 + 使用）
    - grep -c "customThresholds\[metric" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1（thresholdBinding 读取）
    - grep -c "customColors\[metric" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1（colorBinding 读取）
    - grep -c "恢复默认" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 2（阈值 + 配色各一）
    - grep -c "removeValue(forKey: metric.rawValue)" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 2
    - grep -c "NSColor(hex:" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1
    - grep -c "Color(nsColor:" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 >= 1
    - 占位文字已移除：grep -c "由 Plan 03 实现" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 0
    - 无新 .swift 文件：ls MacStatus/MacStatus/UI/Views/ 中不出现新文件
    构建断言：xcodebuild exit 0
    行为意图（供人工验证）：
    - 阈值 Slider 拖动 → 标签实时更新（"CPU 警告：XX%"）→ 状态栏颜色随阈值变化
    - 调高 warning 到等于 critical 时 → critical 自动上移（warning < critical 约束生效）
    - 点击"恢复默认" → 对应指标滑块回到 defaultWarning/defaultCritical 值
    - ColorPicker 改色 → 状态栏对应指标 warning/critical 颜色即时变化
    - ColorPicker 恢复默认 → 状态栏回到橙/红内置默认色
  </acceptance_criteria>
  <done>SettingsView.swift Section 5 告警阈值和 Section 6 配色均已实现；ThresholdSubsection + ColorSubsection 内嵌；所有 Binding 是计算属性；xcodebuild exit 0</done>
</task>

<task type="auto">
  <name>Task 2：DashboardView 弹窗区块可见性门控</name>
  <files>MacStatus/MacStatus/UI/Views/DashboardView.swift</files>
  <read_first>
    - MacStatus/MacStatus/UI/Views/DashboardView.swift — 完整读取（当前版本，关注第 60–91 行）：
      - 第 62–64 行：if state.hasBattery, let battery = state.battery { BatterySectionView(snapshot: battery) } — 电池硬件门控，保留并在外层追加设置门控
      - 第 67–71 行：ProcessListView(processes: state.topProcesses, ...) — 进程网络列表
      - 第 74–82 行：ProcessResourceSectionView("CPU 占用 Top 5", ...) — CPU Top 5
      - 第 85–91 行：ProcessResourceSectionView("内存占用 Top 5", ...) — 内存 Top 5
      DashboardView 改造：上述三个进程区块用 if settings.showProcessSection { } 包裹；电池区块门控条件改为 settings.showBatterySection && state.hasBattery
    - 09-RESEARCH.md Q5 节（DashboardView 响应 showBatterySection/showProcessSection）：
      - "直接在 body 内 let settings = SettingsManager.shared 建立 @Observable 追踪" 的完整代码
      - 为何不需要 @State 或 @EnvironmentObject
    - 09-PATTERNS.md "DashboardView.swift — Visibility gating" 节：完整的组合门控代码片段（settings.showBatterySection && state.hasBattery, let battery）
    - MacStatus/MacStatus/Utils/SettingsManager.swift — 确认 showBatterySection/showProcessSection 属性已存在（Plan 01 完成的成果）
  </read_first>
  <action>
    修改 DashboardView.swift：

    位置 1 — DashboardView.body 开头（在 VStack 内，第一条语句之前）：
    添加 let settings = SettingsManager.shared 局部 let（不是 @State，是纯局部赋值以在 body 内读取 @Observable 属性，建立 SwiftUI Observation 追踪依赖）。

    位置 2 — 电池区块（当前第 62–64 行）：
    将原 if state.hasBattery, let battery = state.battery { BatterySectionView(snapshot: battery) }
    改为 if settings.showBatterySection && state.hasBattery, let battery = state.battery { BatterySectionView(snapshot: battery) }
    语义：外层设置门控（showBatterySection）叠加原有硬件门控（hasBattery）。在无电池机型上 hasBattery = false，Toggle 可操作但弹窗不显示（原有行为不变）。

    位置 3 — 进程相关三个区块：
    将 ProcessListView(...) + ProcessResourceSectionView("CPU 占用 Top 5", ...) + ProcessResourceSectionView("内存占用 Top 5", ...) 三段用 if settings.showProcessSection { } 包裹。
    注意：所有三段都在同一 if 分支内（进程区块是整体显示或整体隐藏，与 UI-SPEC 一致）。

    约束（强制）：
    - let settings = SettingsManager.shared 是局部 let，不是 @State 变量（SettingsManager 生命周期自行管理，Swift Observation 会在 body 读取时自动追踪）
    - 不向 DashboardState 添加任何镜像字段（直接读 @Observable SettingsManager 是最简路径）
    - 不改动 BatterySectionView、ProcessListView、ProcessResourceSectionView 的实现
    - 不改动 DashboardState 的任何 @Published 属性
    - 不改动弹窗框架（PopoverManager 等）
  </action>
  <verify>
    <automated>cd /Users/halo/work/my-project/mac-status && xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build.noindex build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    源码断言（grep，任一失败则任务未完成）：
    - grep -c "showBatterySection" MacStatus/MacStatus/UI/Views/DashboardView.swift 返回 >= 1
    - grep -c "showProcessSection" MacStatus/MacStatus/UI/Views/DashboardView.swift 返回 >= 1
    - grep -c "SettingsManager.shared" MacStatus/MacStatus/UI/Views/DashboardView.swift 返回 >= 1
    - 硬件门控保留：grep -c "state.hasBattery" MacStatus/MacStatus/UI/Views/DashboardView.swift 返回 >= 1（原有 hasBattery 判断未被删除）
    - grep -c "ProcessListView" MacStatus/MacStatus/UI/Views/DashboardView.swift 返回 >= 1（进程区块未被删除）
    构建断言：xcodebuild exit 0
    行为意图（供人工验证）：
    - 打开设置窗口 → 关闭"进程区块" Toggle → 弹窗的 ProcessListView + CPU/内存 Top 5 区块立即消失（无需重启）
    - 重新开启"进程区块" Toggle → 进程区块立即恢复
    - 关闭"电池区块" Toggle（仅在笔记本上有效果）→ 弹窗的 BatterySectionView 立即消失
    - 重启应用 → Toggle 状态保持（持久化通过 SettingsManager.UserDefaults 已保证）
    - 在台式机（无电池）上："电池区块" Toggle 可以操作但弹窗不显示电池区块（hasBattery = false 硬件门控）
  </acceptance_criteria>
  <done>DashboardView.swift 中电池区块加 settings.showBatterySection 外层门控；三个进程区块整体加 settings.showProcessSection 门控；通过 let settings = SettingsManager.shared 建立 @Observable 追踪；xcodebuild exit 0</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| ColorPicker → NSColor → hexString → customColors setter | 颜色输入经 NSColor.hexString 截断，customColors setter 过滤非 #RRGGBB 格式 |
| Slider → customThresholds setter | 阈值经 SettingsManager setter 的 clamp(0...100) 处理；UI 层 warning<critical 约束提供额外保证 |
| DashboardView 读 SettingsManager.shared | 只读访问，@Observable 追踪无副作用 |

## STRIDE 威胁登记

| Threat ID | 类别 | 组件 | 处置 | 缓解措施 |
|-----------|------|------|------|---------|
| T-09-03-T | Tampering | customColors hex 写入 | mitigate | SettingsManager.customColors setter 已过滤非 "#RRGGBB" 格式（hex.hasPrefix("#") && hex.count == 7）；NSColor.hexString 输出经 .usingColorSpace(.sRGB) 保证 0–255 范围；双重保障 |
| T-09-03-T | Tampering | customThresholds Double 写入 | mitigate | SettingsManager.customThresholds setter 已 clamp(0...100)；UI 层再加 warning<critical 约束；值来自 Slider 控件（in: 30...90, step: 5 / in: 50...95），本身已限范围 |
| T-09-03-I | Information Disclosure | DashboardView 读 showBatterySection/showProcessSection | accept | 本地用户偏好，无隐私数据 |
| T-09-03-SC | Tampering | 供应链 | accept | 零新外部包；纯 Apple SDK（ColorPicker、NSColor+Hex 项目自有） |
</threat_model>

<verification>
最终全量构建验证：
cd /Users/halo/work/my-project/mac-status && xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build.noindex build 2>&1 | tail -10

Phase 9 全量源码断言（v2.0 最终 section 覆盖）：
- grep -c "ColorPicker" MacStatus/MacStatus/UI/Views/SettingsView.swift >= 2
- grep -ic "editmode\|EditMode" MacStatus/MacStatus/UI/Views/SettingsView.swift 返回 0
- grep -c "\.onMove" MacStatus/MacStatus/UI/Views/SettingsView.swift >= 1
- grep -c "showBatterySection" MacStatus/MacStatus/UI/Views/DashboardView.swift >= 1
- grep -c "showProcessSection" MacStatus/MacStatus/UI/Views/DashboardView.swift >= 1

人工 UAT 检查单（v2.0 竣工前补测）：
[ ] SET-01：右键菜单"偏好设置…"打开设置窗口
[ ] SET-02：关闭 CPU Toggle → 状态栏 CPU 段消失；关闭进程区块 → 弹窗进程列表消失
[ ] SET-03：拖动重排指标 → 状态栏顺序即时变化
[ ] SET-04：调低 CPU warning Slider → 状态栏 CPU 颜色即时变化
[ ] SET-05：改 CPU warning ColorPicker 为蓝色 → 状态栏 CPU 即时变蓝
[ ] SET-06：切换详细/紧凑/百分比 → 状态栏格式即时切换
[ ] 持久化：改动设置 → 退出 → 重启 → 设置保留
[ ] 恢复默认：点阈值"恢复默认" → 滑块回到 60/80；点配色"恢复默认" → 颜色回到橙/红
[ ] Phase 7 电池 UAT（补测）：笔记本渲染、台式机隐藏
[ ] Phase 8 进程 UAT（补测）：CPU/内存 Top 5 实时渲染、关闭弹窗后无后台开销
</verification>

<success_criteria>
- SettingsView.swift 完整实现 8 个 Section，Section 5 告警阈值和 Section 6 配色均已实现（非占位）
- ThresholdSubsection + ColorSubsection 内嵌于 SettingsView.swift；所有计算 Binding 是 struct 计算属性（非 body 内局部）
- warning < critical 约束通过 .onChange { _, new in } 双参数 Swift 6 签名实现
- ColorPicker 使用 supportsOpacity: false；颜色经 NSColor+Hex 双向转换
- DashboardView 电池区块改为 settings.showBatterySection && state.hasBattery；进程三区块被 settings.showProcessSection 包裹
- xcodebuild exit 0
- Phase 9 全部 6 个 SET 需求（SET-01..06）均已实现，v2.0 四个成功标准全部满足
</success_criteria>

<output>
完成后创建 .planning/phases/09-settings-window-ui-customization/09-03-SUMMARY.md
</output>
