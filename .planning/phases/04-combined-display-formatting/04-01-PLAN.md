---
phase: 04-combined-display-formatting
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MacStatus/MacStatus/UI/StatusBarManager.swift
autonomous: true
requirements: [DISP-01, DISP-02, DISP-03, DISP-04]

must_haves:
  truths:
    - "DISP-01 is implemented: one visible combined menu bar item renders `C | G | M | network`"
    - "DISP-02 is preserved: `setupNetworkItem()` keeps fixed status item width `300`"
    - "DISP-03 is implemented: default text uses `NSColor.labelColor`, warning uses `NSColor.systemYellow`, and critical uses `NSColor.systemRed`"
    - "DISP-04 is implemented: fallback strings remain `C --%`, `G --`, `M --`, and `↓-- ↑--`"
    - "D-01 is implemented: CPU label `C` stays default; CPU value `<60%` default, `60...84%` yellow, `>=85%` red"
    - "D-02 is implemented: GPU label `G` stays default; GPU value `<60%` default, `60...84%` yellow, `>=85%` red"
    - "D-03 is implemented: MEM label `M` stays default; `OK` default, `WARN` yellow, `CRIT` red"
    - "D-04 is implemented: only values or status words are colored"
    - "D-05 is implemented: GPU normal/default state no longer uses green"
    - "D-06 is implemented: separator remains ` | `"
    - "D-07 is implemented: target format remains `C 12% | G 34% | M OK | ↓2.1M ↑512K`"
    - "D-08 is implemented: labels remain `C`, `G`, and `M`"
    - "D-09 is implemented: visible combined `NSStatusItem` width remains `300`"
    - "D-10 is implemented: status button remains single-line, clipped, and non-wrapping"
    - "D-11 is implemented: unavailable state uses the existing fallback strings"
  artifacts:
    - path: "MacStatus/MacStatus/UI/StatusBarManager.swift"
      provides: "value-level attributed menu bar coloring for CPU, GPU, and memory"
      contains: "usageColor(for:"
  key_links:
    - from: "StatusBarManager.updateCPU"
      to: "combinedAttributedString"
      via: "`latestCPUUsage` raw state"
      pattern: "latestCPUUsage"
    - from: "StatusBarManager.updateGPU"
      to: "combinedAttributedString"
      via: "`latestGPUUsage` raw state"
      pattern: "latestGPUUsage"
    - from: "StatusBarManager.updateMemory"
      to: "combinedAttributedString"
      via: "`latestMemoryPressure` raw state"
      pattern: "latestMemoryPressure"
---

<objective>
Finalize the v1 combined menu bar display. Keep the existing single visible `networkStatusItem` and target text order, then add value-level CPU/GPU/MEM coloring without changing reader behavior, fixed width, separators, or fallback text.
</objective>

<execution_context>
@/Users/halo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/halo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-combined-display-formatting/04-CONTEXT.md
@.planning/phases/04-combined-display-formatting/04-RESEARCH.md
@.planning/phases/04-combined-display-formatting/04-PATTERNS.md
@.planning/phases/04-combined-display-formatting/04-UI-SPEC.md
@MacStatus/MacStatus/UI/StatusBarManager.swift
@MacStatus/MacStatus/Readers/GPUReader.swift
@MacStatus/MacStatus/Readers/MemoryReader.swift
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Stale warning/critical color remains after a metric becomes unavailable | high | Reset raw CPU/GPU/MEM state to `nil` when the matching update receives `nil` |
| Labels are colored instead of values | medium | Append each metric as label + space + value/status using separate attributes |
| GPU normal remains green from Phase 3 | medium | Replace `gpuPressureColor()` usage with `usageColor(for: latestGPUUsage)` where `<60` returns default |
| Light/dark readability regresses | medium | Use only `NSColor.labelColor`, `NSColor.systemYellow`, and `NSColor.systemRed` |
| Menu bar jitter returns | medium | Preserve fixed `300` width and single-line clipping configuration |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Preserve raw metric state for value-level coloring</name>
  <files>MacStatus/MacStatus/UI/StatusBarManager.swift</files>
  <read_first>
    <file>MacStatus/MacStatus/UI/StatusBarManager.swift</file>
    <file>MacStatus/MacStatus/Readers/GPUReader.swift</file>
    <file>MacStatus/MacStatus/Readers/MemoryReader.swift</file>
    <file>.planning/phases/04-combined-display-formatting/04-CONTEXT.md</file>
  </read_first>
  <action>
Modify `StatusBarManager.swift`.

Add raw state properties:
- `private var latestCPUUsage: Double?`
- `private var latestGPUUsage: Double?`
- `private var latestMemoryPressure: MemoryPressureLevel?`

Update fallback branches:
- In `updateCPU(nil)`, set `latestCPUUsage = nil` before `updateCombinedStatus()`.
- In `updateGPU(nil)`, set `latestGPUUsage = nil` and keep `latestGPUText = "G --"`.
- In `updateMemory(nil)`, set `latestMemoryPressure = nil` and keep `latestMemoryText = "M --"`.

Update successful branches:
- In `updateCPU(_:)`, set `latestCPUUsage = value` when `latestCPUText` becomes `String(format: "C %.0f%%", value)`.
- In `updateGPU(_:)`, set `latestGPUUsage = stats.utilizationPercent` when `latestGPUText` becomes `String(format: "G %.0f%%", stats.utilizationPercent)`.
- In `updateMemory(_:)`, set `latestMemoryPressure = stats.pressureLevel` when `latestMemoryText` becomes `formatMemoryPressure(stats.pressureLevel)`.
  </action>
  <verify>
    <automated>`grep -n "latestCPUUsage" MacStatus/MacStatus/UI/StatusBarManager.swift` prints matches.</automated>
    <automated>`grep -n "latestGPUUsage" MacStatus/MacStatus/UI/StatusBarManager.swift` prints matches.</automated>
    <automated>`grep -n "latestMemoryPressure" MacStatus/MacStatus/UI/StatusBarManager.swift` prints matches.</automated>
  </verify>
  <acceptance_criteria>
- `StatusBarManager` stores raw CPU usage as `Double?`.
- `StatusBarManager` stores raw GPU usage as `Double?`.
- `StatusBarManager` stores raw memory pressure as `MemoryPressureLevel?`.
- CPU, GPU, and memory fallback branches reset their raw state to `nil`.
- Successful CPU, GPU, and memory updates refresh their raw state before redrawing.
  </acceptance_criteria>
  <done>
Raw metric state is available to the attributed title builder and cannot leak stale warning/critical colors after fallback.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build value-level attributed text for CPU, GPU, and memory</name>
  <files>MacStatus/MacStatus/UI/StatusBarManager.swift</files>
  <read_first>
    <file>MacStatus/MacStatus/UI/StatusBarManager.swift</file>
    <file>.planning/phases/04-combined-display-formatting/04-CONTEXT.md</file>
    <file>.planning/phases/04-combined-display-formatting/04-UI-SPEC.md</file>
  </read_first>
  <action>
Modify `combinedAttributedString()` and local helpers in `StatusBarManager.swift`.

Implement:
- `appendMetric(label:value:valueColor:to:)`
- `metricAttributes(valueColor:)`
- `usageColor(for:)`
- `memoryColor(for:)`

Use the helpers so:
- CPU appends label `C` with base attributes and value from `latestCPUText` without the `C ` prefix using `usageColor(for: latestCPUUsage)`.
- GPU appends label `G` with base attributes and value from `latestGPUText` without the `G ` prefix using `usageColor(for: latestGPUUsage)`.
- Memory appends label `M` with base attributes and value/status from `latestMemoryText` without the `M ` prefix using `memoryColor(for: latestMemoryPressure)`.
- Network appends `latestNetworkText` entirely with base attributes.
- Separators remain exactly ` | ` with base attributes.

Thresholds:
- `usageColor(for: nil)` returns `nil`.
- `usageColor(for: value)` returns `nil` for `value < 60`, `.systemYellow` for `60..<85`, and `.systemRed` for `>=85`.
- `memoryColor(for: .warning)` returns `.systemYellow`.
- `memoryColor(for: .critical)` returns `.systemRed`.
- `memoryColor(for: .normal)`, `.unknown`, and `nil` return `nil`.

Remove or stop using the old GPU normal-green helper so `GPUPressureLevel.normal` cannot color GPU green in Phase 4.
  </action>
  <verify>
    <automated>`grep -n "usageColor(for" MacStatus/MacStatus/UI/StatusBarManager.swift` prints a match.</automated>
    <automated>`grep -n "memoryColor(for" MacStatus/MacStatus/UI/StatusBarManager.swift` prints a match.</automated>
    <automated>`grep -n "systemGreen" MacStatus/MacStatus/UI/StatusBarManager.swift` prints no matches.</automated>
  </verify>
  <acceptance_criteria>
- CPU label `C` uses base attributes.
- CPU value uses default/yellow/red thresholds.
- GPU label `G` uses base attributes.
- GPU value uses default/yellow/red thresholds.
- Memory label `M` uses base attributes.
- Memory status word uses default/yellow/red pressure state.
- No code path colors GPU normal state green.
- Separators and network text remain default color.
  </acceptance_criteria>
  <done>
The combined attributed title applies warning/critical color only to CPU/GPU values and memory status words.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Verify build, fixed-width display, and fallback source assertions</name>
  <files>MacStatus/MacStatus/UI/StatusBarManager.swift</files>
  <read_first>
    <file>MacStatus/MacStatus/UI/StatusBarManager.swift</file>
    <file>MacStatus/MacStatus.xcodeproj/project.pbxproj</file>
  </read_first>
  <action>
Run the full Debug build:

`xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build`

Then run source assertions:
- `grep -n "statusItem(withLength: 300)" MacStatus/MacStatus/UI/StatusBarManager.swift`
- `grep -n "byClipping" MacStatus/MacStatus/UI/StatusBarManager.swift`
- `grep -n "C --%" MacStatus/MacStatus/UI/StatusBarManager.swift`
- `grep -n "G --" MacStatus/MacStatus/UI/StatusBarManager.swift`
- `grep -n "M --" MacStatus/MacStatus/UI/StatusBarManager.swift`
- `grep -n "↓-- ↑--" MacStatus/MacStatus/UI/StatusBarManager.swift`

If the build fails, fix Swift compile errors in `StatusBarManager.swift` only unless a compiler diagnostic proves another file must change.
  </action>
  <verify>
    <automated>`xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build` exits 0.</automated>
    <automated>All source assertion `grep` commands print at least one match.</automated>
  </verify>
  <acceptance_criteria>
- Debug build succeeds.
- `StatusBarManager` still creates the visible item with fixed width `300`.
- Status button remains clipped, single-line, and non-wrapping.
- Fallback strings are present in source.
- Source contains no `.systemGreen` in `StatusBarManager.swift`.
  </acceptance_criteria>
  <done>
The final display formatting builds and preserves fixed width, fallbacks, and single-line behavior.
  </done>
</task>

</tasks>

<verification>
Run:

```bash
xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build
```

Then verify:

```bash
grep -n "usageColor(for" MacStatus/MacStatus/UI/StatusBarManager.swift
grep -n "memoryColor(for" MacStatus/MacStatus/UI/StatusBarManager.swift
grep -n "statusItem(withLength: 300)" MacStatus/MacStatus/UI/StatusBarManager.swift
! grep -n "systemGreen" MacStatus/MacStatus/UI/StatusBarManager.swift
```
</verification>

<success_criteria>
- CPU, GPU, memory, and network remain in a single compact combined menu bar item.
- Fixed width `300` and single-line clipping remain unchanged.
- CPU/GPU/MEM color rules exactly match D-01 through D-05.
- Fallback text exactly matches D-11.
- Debug build succeeds.
</success_criteria>
