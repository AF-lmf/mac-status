---
phase: 01-foundation-cpu-monitoring
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - MacStatus/Readers/CPUReader.swift
  - MacStatus/Readers/ReaderProtocol.swift
  - MacStatus/Readers/TimerReader.swift
  - MacStatus/UI/StatusBarManager.swift
  - MacStatus/Utils/SettingsManager.swift
  - MacStatus/App/AppDelegate.swift
autonomous: true
requirements:
  - CPU-02
  - LIFE-01

must_haves:
  truths:
    - "CPU reading updates every 2 seconds with under 1% app CPU overhead — verified in Activity Monitor after 30 minutes (CPU-02)"
    - "Re-launching the app produces no duplicate NSStatusItem ghost icons — verified by launching and quitting 3 times (LIFE-01)"
    - "Menu bar CPU text uses monospaced digits — verified by rapid CPU fluctuation not causing width jitter (D-07)"
    - "Mach API failure displays 'CPU --%' instead of crashing or showing NaN (Research error handling)"
  artifacts:
    - path: "MacStatus/Readers/CPUReader.swift"
      provides: "CPUReader class with host_statistics, KERN_SUCCESS check, delta calc, Sendable CPULoad wrapper"
      min_lines: 80
    - path: "MacStatus/Readers/ReaderProtocol.swift"
      provides: "ReaderProtocol with associatedtype, setup/read/start/stop lifecycle"
      min_lines: 15
    - path: "MacStatus/Readers/TimerReader.swift"
      provides: "TimerReader<T> generic base class — Timer management + callback dispatching"
      min_lines: 40
    - path: "MacStatus/UI/StatusBarManager.swift"
      provides: "NSStatusItem lifecycle with deinit removeStatusItem, tolerance check, monospaced digits, macOS 26 visibility check"
      min_lines: 60
    - path: "MacStatus/Utils/SettingsManager.swift"
      provides: "UserDefaults wrapper for refreshInterval (Double), singleton pattern"
      min_lines: 25
    - path: "MacStatus/App/AppDelegate.swift"
      provides: "Wiring hub: instantiates StatusBarManager + CPUReader, delegates lifecycle (simplified from Plan 01 inline version)"
  key_links:
    - from: "AppDelegate.applicationDidFinishLaunching"
      to: "CPUReader.onUpdate callback"
      via: "closure assigned in AppDelegate"
      pattern: "cpuReader\\.onUpdate.*statusBarManager\\.updateCPU"
    - from: "CPUReader.read()"
      to: "host_statistics(mach_host_self(), HOST_CPU_LOAD_INFO, ...)"
      via: "withUnsafeMutablePointer rebind"
      pattern: "host_statistics.*HOST_CPU_LOAD_INFO"
    - from: "StatusBarManager.updateCPU"
      to: "statusItem.button?.attributedTitle"
      via: "tolerance guard + DispatchQueue.main.async"
      pattern: "attributedTitle.*attributedString"
    - from: "StatusBarManager.deinit"
      to: "NSStatusBar.system.removeStatusItem"
      via: "statusItem reference"
      pattern: "removeStatusItem"
---

<objective>
Extract the inline walking-skeleton code from AppDelegate.swift into the D-02 folder structure: CPUReader (Readers/), StatusBarManager (UI/), ReaderProtocol + TimerReader (Readers/), and SettingsManager (Utils/). Add Swift 6 Sendable compliance, macOS 26 menu bar visibility detection, and verify performance targets (CPU-02: <1% app CPU overhead) and ghost icon prevention (LIFE-01: no duplicates on re-launch).

Purpose: Make the architecture extensible for Phase 2-5 readers, validate Swift 6 strict concurrency with Mach C types, and ensure the status bar lifecycle is production-grade with proper resource cleanup.
Output: All D-02 folders populated with properly separated classes. AppDelegate becomes a thin wiring hub. CPU-02 performance target verified.
</objective>

<execution_context>
@/Users/halo/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/halo/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation-cpu-monitoring/01-CONTEXT.md
@.planning/phases/01-foundation-cpu-monitoring/01-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
@.planning/phases/01-foundation-cpu-monitoring/SKELETON.md

# Plan 01-01 created the walking skeleton. Reference its SUMMARY after execution for the inline
# AppDelegate.swift as the source of truth for what needs to be extracted.
# This plan MUST read the existing AppDelegate.swift created by Plan 01-01 before modifying it.

<interfaces>
<!-- Types and contracts Plan 01-01 established (verify against actual file after plan 01-01 executes) -->

From Plan 01-01 AppDelegate.swift — inline patterns to preserve during extraction:
```
class AppDelegate — @main, NSApplicationDelegate
  static func main() — setActivationPolicy(.accessory) + app.run()
  func applicationDidFinishLaunching — statusItem creation + startPolling()
  func startPolling() — Timer 2.0s on DispatchQueue.global(qos: .utility)
  func readCPU() — host_statistics(HOST_CPU_LOAD_INFO) + delta calc + &- overflow
  func updateDisplay(Double?) — tolerance check (abs < 0.5) + attributedTitle
  func attributedString(String) — monospacedDigitSystemFont + labelColor
  func applicationWillTerminate — timer?.invalidate() + removeStatusItem
```

Extraction mapping (D-02):
- `readCPU()` + delta logic → `MacStatus/Readers/CPUReader.swift`
- `startPolling()` Timer logic → `MacStatus/Readers/TimerReader.swift` (generic base)
- `ReaderProtocol` lifecycle → `MacStatus/Readers/ReaderProtocol.swift`
- `NSStatusItem` lifecycle + `updateDisplay` + `attributedString` → `MacStatus/UI/StatusBarManager.swift`
- `UserDefaults` refresh interval → `MacStatus/Utils/SettingsManager.swift`
- `AppDelegate` → simplified to instantiate + wire above components

Swift 6 Sendable requirement (RESEARCH.md §Open Questions #3):
```swift
// host_cpu_load_info is a C struct (tuple of 4 UInt32).
// Swift 6 strict concurrency may reject passing it across actor boundaries.
// Solution: wrap in a Sendable struct on the background queue before dispatching to main.
struct CPULoad: Sendable {
    let user: Double
    let system: Double
    let idle: Double
    let nice: Double
    var usage: Double {
        let total = user + system + idle + nice
        guard total > 0 else { return 0 }
        return ((user + system + nice) / total) * 100.0
    }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatusBarManager and CPUReader — display polish + error handling</name>
  <files>
    <file>MacStatus/UI/StatusBarManager.swift</file>
    <file>MacStatus/Readers/CPUReader.swift</file>
    <file>MacStatus/App/AppDelegate.swift</file>
  </files>

  <read_first>
    @MacStatus/App/AppDelegate.swift — the Plan 01-01 inline implementation. Read the ENTIRE file to understand what to extract. Specifically note: readCPU() method (extract to CPUReader), updateDisplay() + attributedString() methods (extract to StatusBarManager), NSStatusItem property + applicationWillTerminate cleanup (extract to StatusBarManager).
    @.planning/phases/01-foundation-cpu-monitoring/01-RESEARCH.md — §Pattern 2 (NSStatusItem with deinit), §Pattern 4 (Tolerance-Based Redraw), §Code Examples → CPUReader Implementation (host_statistics approach), §Open Questions #3 (Swift 6 Sendable CPULoad wrapper)
    @.planning/research/PITFALLS.md — Pitfall 1 (ghost icons), Pitfall 6 (macOS 26 privacy control), §Performance Traps (retain cycle Timer→self)
    @.planning/phases/01-foundation-cpu-monitoring/01-CONTEXT.md — D-06 (tolerance 0.5%), D-07 (monospaced digits), D-10 (deinit removeStatusItem), D-04 ("CPU XX%" format)
  </read_first>

  <action>
    **Step 1 — Create `MacStatus/Readers/CPUReader.swift`:**

    Extract the `readCPU()` method from AppDelegate.swift into a dedicated class.

    Class: `final class CPUReader` with:
    - Properties: `private var previousInfo = host_cpu_load_info()`, `private var hasPrevious = false`, `var onUpdate: ((Double?) -> Void)?`
    - Method `func read()` — contains the `host_statistics(HOST_CPU_LOAD_INFO)` call + delta calc + `&-` overflow logic from AppDelegate's `readCPU()`. The `onUpdate` callback replaces the inline `DispatchQueue.main.async` pattern.
    - On `host_statistics` failure (result != KERN_SUCCESS), call `onUpdate?(nil)` to signal error state → StatusBarManager shows "CPU --%" (D-04 error format).
    - On success, compute usage via delta formula and call `onUpdate?(usage.isNaN ? nil : usage)`.
    - **Swift 6 Sendable:** Define `struct CPULoad: Sendable { let user, system, idle, nice: Double }` inside the file. The `read()` method converts `host_cpu_load_info` C struct to `CPULoad` on the background queue, then computes `usage` from `CPULoad` — this ensures Sendable compliance when Swift 6 strict concurrency checks cross actor boundaries (RESEARCH.md §Open Questions #3).
    - No Timer inside CPUReader — Timer management belongs to the caller (AppDelegate, later TimerReader from Task 2).
    - Import: `import Cocoa` (for Mach APIs + Foundation types through AppKit umbrella).

    **Step 2 — Create `MacStatus/UI/StatusBarManager.swift`:**

    Extract NSStatusItem lifecycle + display formatting from AppDelegate.

    Class: `final class StatusBarManager` with:
    - Property: `private var statusItem: NSStatusItem?`
    - Property: `private var lastDisplayedValue: Double?` — for D-06 tolerance check
    - `init()`: Creates `NSStatusBar.system.statusItem(withLength: NSStatusItem.variableLength)` (D-08), sets `autosaveName = "com.macstatus.cpu"`, sets initial attributedTitle to "CPU --%" (D-04 zero-config startup).
    - **macOS 26 visibility check (RESEARCH.md §Open Questions #2):** After creating the statusItem, schedule a 2-second delayed check on main queue: `DispatchQueue.main.asyncAfter(deadline: .now() + 2.0) { [weak self] in ... }`. Check `statusItem?.button?.window?.isVisible == false` or `statusItem?.isVisible == false`. If not visible, show `NSAlert` with message "MacStatus needs permission to display in the menu bar. Open System Settings → Menu Bar and enable MacStatus." This is macOS 26 (Tahoe) behavior — on macOS 14-15 the item will be visible immediately and the alert never fires.
    - Method `func updateCPU(_ value: Double?)`: Contains the D-06 tolerance check + display formatting from AppDelegate's inline `updateDisplay()`.
      - If `value == nil`: always update display to "CPU --%" (error state transition is always significant), reset `lastDisplayedValue = nil`.
      - If `let last = lastDisplayedValue, abs(value - last) < 0.5`: return without updating (D-06 — skip insignificant changes).
      - Otherwise: `lastDisplayedValue = value`, set `statusItem?.button?.attributedTitle = attributedString(String(format: "CPU %.0f%%", value))` (D-04).
    - Private method `func attributedString(_ text: String) -> NSAttributedString`: Uses `NSFont.monospacedDigitSystemFont(ofSize: NSFont.smallSystemFontSize, weight: .regular)` (D-07) and `NSColor.labelColor` (light/dark auto-adapt).
    - Private method `func removeStatusItem()`: Calls `NSStatusBar.system.removeStatusItem(statusItem!)` then sets `statusItem = nil`.
    - `deinit`: Calls `removeStatusItem()` — D-10 ghost icon prevention. Include print statement: `print("StatusBarManager deinit — status item removed")` for verification.

    **Step 3 — Refactor `MacStatus/App/AppDelegate.swift`:**

    Update AppDelegate to use CPUReader and StatusBarManager instead of inline code.

    Changes to AppDelegate:
    - **Remove:** `statusItem` property, `timer` property, `previousCPUInfo` property, `hasPreviousCPUInfo` property, `lastDisplayedCPU` property.
    - **Remove:** `startPolling()` method, `readCPU()` method, `updateDisplay()` method, `attributedString()` method, `stopPolling()` method.
    - **Remove:** Timer and statusItem logic from `applicationWillTerminate`.
    - **Add:** `private var statusBarManager: StatusBarManager?`
    - **Add:** `private var cpuReader: CPUReader?`
    - **Update `applicationDidFinishLaunching`:**
      - Create `statusBarManager = StatusBarManager()` — NSStatusItem created via StatusBarManager.init (D-08).
      - Create `cpuReader = CPUReader()`
      - Set callback: `cpuReader.onUpdate = { [weak self] value in DispatchQueue.main.async { self?.statusBarManager?.updateCPU(value) } }`
      - Start polling by scheduling a `Timer.scheduledTimer(withTimeInterval: 2.0, repeats: true)` (D-05) whose block dispatches `cpuReader.read()` on `DispatchQueue.global(qos: .utility)` (Anti-Pattern 1 — never poll on main thread).
      - Fire first read immediately: `DispatchQueue.global(qos: .utility).async { [weak self] in self?.cpuReader?.read() }` — LIFE-03 zero-config startup.
    - **Update `applicationWillTerminate`:**
      - `cpuReader = nil`
      - `statusBarManager = nil` — triggers deinit → removeStatusItem → D-10 ghost icon prevention.
    - **Keep:** `static func main()` — unchanged (D-03, D-09 setActivationPolicy).
    - **Keep:** `deinit` with print statement.

    **File header requirements for both new .swift files:**
    - `import Cocoa` (umbrella import for AppKit + Foundation + Mach)
    - No `import Foundation` — Cocoa is the AppKit umbrella and includes Foundation plus Mach kernel types (`host_cpu_load_info`, `kern_return_t`, `mach_host_self`).
  </action>

  <acceptance_criteria>
    **CPUReader.swift:**
    - File exists at `MacStatus/Readers/CPUReader.swift`
    - Contains `final class CPUReader` — verify: `grep "final class CPUReader" MacStatus/Readers/CPUReader.swift`
    - Contains `struct CPULoad: Sendable` — verify: `grep "struct CPULoad.*Sendable" MacStatus/Readers/CPUReader.swift`
    - Contains `func read()` with `host_statistics(mach_host_self(), HOST_CPU_LOAD_INFO, ...)` — verify: `grep "HOST_CPU_LOAD_INFO" MacStatus/Readers/CPUReader.swift`
    - Contains `&-` overflow-safe subtraction — verify: `grep "&-" MacStatus/Readers/CPUReader.swift`
    - Contains `onUpdate?(nil)` on KERN_SUCCESS failure — verify: `grep "onUpdate.*nil" MacStatus/Readers/CPUReader.swift`
    - Has `var onUpdate: ((Double?) -> Void)?` — verify: `grep "var onUpdate" MacStatus/Readers/CPUReader.swift`
    - Does NOT contain `Timer` or `DispatchQueue.main.async` — CPUReader is pure data collection, dispatch is AppDelegate's responsibility — verify: `grep "Timer\|DispatchQueue" MacStatus/Readers/CPUReader.swift` returns no matches (or only for background queue annotation).

    **StatusBarManager.swift:**
    - File exists at `MacStatus/UI/StatusBarManager.swift`
    - Contains `final class StatusBarManager` — verify: `grep "final class StatusBarManager" MacStatus/UI/StatusBarManager.swift`
    - `init()` contains `NSStatusBar.system.statusItem(withLength: .variableLength)` (D-08) — verify: `grep "variableLength" MacStatus/UI/StatusBarManager.swift`
    - Contains `autosaveName = "com.macstatus.cpu"` — verify: `grep "com.macstatus.cpu" MacStatus/UI/StatusBarManager.swift`
    - Contains macOS 26 visibility check with `DispatchQueue.main.asyncAfter(deadline: .now() + 2.0)` — verify: `grep "asyncAfter" MacStatus/UI/StatusBarManager.swift`
    - `updateCPU` contains tolerance check `abs(value - last) < 0.5` (D-06) — verify: `grep "0\.5" MacStatus/UI/StatusBarManager.swift`
    - `attributedString` uses `NSFont.monospacedDigitSystemFont` (D-07) — verify: `grep "monospacedDigit" MacStatus/UI/StatusBarManager.swift`
    - `attributedString` uses `NSColor.labelColor` — verify: `grep "labelColor" MacStatus/UI/StatusBarManager.swift`
    - `deinit` calls `NSStatusBar.system.removeStatusItem(...)` (D-10) — verify: `grep "removeStatusItem" MacStatus/UI/StatusBarManager.swift`
    - `deinit` contains `print("StatusBarManager deinit — status item removed")` — verify: `grep "deinit.*removed" MacStatus/UI/StatusBarManager.swift`

    **AppDelegate.swift (refactored):**
    - File still exists at `MacStatus/App/AppDelegate.swift`
    - NO longer contains `host_statistics` or `HOST_CPU_LOAD_INFO` (moved to CPUReader) — verify: `grep -c "host_statistics" MacStatus/App/AppDelegate.swift | xargs test 0 -eq`
    - NO longer contains `NSStatusBar.system.statusItem` (moved to StatusBarManager) — verify: `grep -c "statusItem" MacStatus/App/AppDelegate.swift | xargs test 0 -eq`
    - Contains `private var statusBarManager: StatusBarManager?` — verify: `grep "statusBarManager" MacStatus/App/AppDelegate.swift`
    - Contains `private var cpuReader: CPUReader?` — verify: `grep "cpuReader" MacStatus/App/AppDelegate.swift`
    - Contains `cpuReader.onUpdate = { [weak self] value in DispatchQueue.main.async { self?.statusBarManager?.updateCPU(value) } }` — verify: `grep "onUpdate.*updateCPU" MacStatus/App/AppDelegate.swift`
    - Timer fires `cpuReader.read()` on `DispatchQueue.global(qos: .utility)` — verify: `grep "utility" MacStatus/App/AppDelegate.swift`
    - First fire: `DispatchQueue.global(qos: .utility).async { [weak self] in self?.cpuReader?.read() }` — LIFE-03 zero-config — verify: `grep "read()" MacStatus/App/AppDelegate.swift`
    - `applicationWillTerminate` sets `cpuReader = nil` and `statusBarManager = nil` — verify: `grep "applicationWillTerminate" MacStatus/App/AppDelegate.swift` followed by `grep "= nil"` showing both properties cleaned up.

    **Build verification:**
    - `xcodebuild -project MacStatus.xcodeproj -scheme MacStatus build` compiles without errors. All three files must be added to the Xcode project's Sources build phase. If compilation fails due to missing file references in project.pbxproj, add the new files to the project.
  </acceptance_criteria>

  <verify>
    <automated>
# Verify each new file exists with the required class signatures
test -f MacStatus/Readers/CPUReader.swift && \
test -f MacStatus/UI/StatusBarManager.swift && \
grep -q "final class CPUReader" MacStatus/Readers/CPUReader.swift && \
grep -q "final class StatusBarManager" MacStatus/UI/StatusBarManager.swift && \
grep -q "removeStatusItem" MacStatus/UI/StatusBarManager.swift && \
grep -c "host_statistics" MacStatus/App/AppDelegate.swift | xargs test 0 -eq && \
echo "PASS: Files extracted, AppDelegate simplified"
    </automated>
  </verify>
  <done>CPUReader.swift handles all Mach API calls with proper error checking; StatusBarManager owns NSStatusItem lifecycle with deinit cleanup, tolerance check, monospaced digits, and macOS 26 visibility detection; AppDelegate is simplified to ~30 lines of wiring code; project builds and runs correctly with same "CPU XX%" display as Plan 01.</done>
</task>

<task type="auto">
  <name>Task 2: Create ReaderProtocol, TimerReader, and SettingsManager — architecture for extensibility</name>
  <files>
    <file>MacStatus/Readers/ReaderProtocol.swift</file>
    <file>MacStatus/Readers/TimerReader.swift</file>
    <file>MacStatus/Utils/SettingsManager.swift</file>
    <file>MacStatus/App/AppDelegate.swift</file>
    <file>MacStatus/Readers/CPUReader.swift</file>
  </files>

  <read_first>
    @MacStatus/Readers/CPUReader.swift — current CPUReader has `func read()` and `var onUpdate`; needs to conform to ReaderProtocol and optionally extend TimerReader
    @MacStatus/App/AppDelegate.swift — current AppDelegate manages Timer and reads via `cpuReader.read()`; will be simplified by using TimerReader.start()
    @.planning/phases/01-foundation-cpu-monitoring/01-RESEARCH.md — §Pattern 3 (Reader Protocol + TimerReader Base Class), §Code Examples → SettingsManager (UserDefaults wrapper)
    @.planning/research/ARCHITECTURE.md — §Pattern 1 (Reader-Callback Pipeline), §Anti-Pattern 5 (One Giant Reader Doing Everything — why we separate readers)
    @.planning/phases/01-foundation-cpu-monitoring/01-CONTEXT.md — Agent's Discretion: SettingsManager uses UserDefaults, v1 no settings window
  </read_first>

  <action>
    **Step 1 — Create `MacStatus/Readers/ReaderProtocol.swift`:**

    Protocol definition per RESEARCH.md §Pattern 3.

    Protocol: `protocol ReaderProtocol: AnyObject` with:
    - `associatedtype ValueType` — the typed value the reader produces (CPUReader uses `Double`)
    - `var onUpdate: ((ValueType?) -> Void)? { get set }` — callback delivering typed value or nil on error
    - `func setup()` — one-time initialization (called before first read)
    - `func read()` — perform one data collection cycle
    - `func start()` — begin periodic polling
    - `func stop()` — stop polling and clean up

    **Step 2 — Create `MacStatus/Readers/TimerReader.swift`:**

    Generic base class per RESEARCH.md §Pattern 3.

    Class: `class TimerReader<T>: ReaderProtocol` with:
    - `typealias ValueType = T`
    - `private var timer: Timer?`
    - `let interval: TimeInterval` — stored init parameter
    - `var onUpdate: ((T?) -> Void)?`
    - `init(interval: TimeInterval)`: stores interval, calls `setup()`
    - `func setup()` — empty default (override in subclass)
    - `func read()` — empty default (override in subclass)
    - `func start()`: calls `stop()` first (idempotent), creates `Timer.scheduledTimer(withTimeInterval: interval, repeats: true)` whose block dispatches `[weak self] _ in DispatchQueue.global(qos: .utility).async { self?.read() }`, adds timer to `RunLoop.main` for mode `.common`. Then fires first read immediately: `DispatchQueue.global(qos: .utility).async { [weak self] in self?.read() }` for LIFE-03 zero-config startup.
    - `func stop()`: `timer?.invalidate()` then `timer = nil`

    **Step 3 — Update `MacStatus/Readers/CPUReader.swift` to extend TimerReader:**

    Change `CPUReader` to extend `TimerReader<Double>`:
    - Change class declaration: `final class CPUReader: TimerReader<Double>`
    - Remove `var onUpdate: ((Double?) -> Void)?` — inherited from TimerReader
    - Remove Timer management — inherited from TimerReader
    - Move previous-data initialization to `override func setup()`: `previousInfo = host_cpu_load_info()`
    - `override func read()` stays the same (host_statistics + delta calc + onUpdate callback)
    - `init()`: `super.init(interval: 2.0)` — D-05 2-second interval, or use `SettingsManager.shared.refreshInterval`

    **Step 4 — Create `MacStatus/Utils/SettingsManager.swift`:**

    UserDefaults wrapper per RESEARCH.md §Code Examples.

    Class: `final class SettingsManager` with:
    - `static let shared = SettingsManager()` — singleton pattern
    - `private let defaults = UserDefaults.standard`
    - Private enum `Keys { static let refreshInterval = "refreshInterval" }`
    - Computed property `var refreshInterval: TimeInterval`:
      - get: `defaults.double(forKey: Keys.refreshInterval)`. Use nil-coalescing: return 2.0 if the stored value is 0 or negative (i.e., `{ let v = defaults.double(forKey: Keys.refreshInterval); return v > 0 ? v : 2.0 }()`)
      - set: `defaults.set(newValue, forKey: Keys.refreshInterval)`
    - `private init() {}` — enforce singleton

    **Step 5 — Update `MacStatus/App/AppDelegate.swift` to use TimerReader + SettingsManager:**

    Simplify AppDelegate further:
    - Update `applicationDidFinishLaunching`:
      - Create `statusBarManager = StatusBarManager()` — unchanged
      - Create `cpuReader = CPUReader()` — now uses TimerReader.init which calls setup() and start()
      - Set callback: `cpuReader.onUpdate = { [weak self] value in DispatchQueue.main.async { self?.statusBarManager?.updateCPU(value) } }` — unchanged
      - Call `cpuReader.start()` — starts Timer + fires first read immediately. This replaces the inline Timer creation from Plan 02 Task 1.
      - **Remove** the inline `Timer.scheduledTimer` and `DispatchQueue.global` read-fire code — TimerReader handles this now.
    - **Update `applicationWillTerminate`:**
      - Call `cpuReader?.stop()` — TimerReader.stop invalidates the timer
      - Then set `cpuReader = nil` and `statusBarManager = nil` — triggers deinit cleanup

    **Step 6 — Ensure all new files are added to the Xcode project:**

    The following files must be added to the Xcode project's `MacStatus` group and included in the Sources build phase for the MacStatus target:
    - `MacStatus/Readers/ReaderProtocol.swift`
    - `MacStatus/Readers/TimerReader.swift`
    - `MacStatus/Readers/CPUReader.swift`
    - `MacStatus/UI/StatusBarManager.swift`
    - `MacStatus/Utils/SettingsManager.swift`
    - `MacStatus/App/AppDelegate.swift`

    If any file compiles but is not in the project, add it via Xcode or by editing the `project.pbxproj` file to add PBXFileReference and PBXBuildFile entries.
  </action>

  <acceptance_criteria>
    **ReaderProtocol.swift:**
    - File exists at `MacStatus/Readers/ReaderProtocol.swift`
    - Contains `protocol ReaderProtocol: AnyObject` — verify: `grep "protocol ReaderProtocol.*AnyObject" MacStatus/Readers/ReaderProtocol.swift`
    - Contains `associatedtype ValueType` — verify: `grep "associatedtype" MacStatus/Readers/ReaderProtocol.swift`
    - Contains `var onUpdate: ((ValueType?) -> Void)? { get set }` — verify: `grep "onUpdate.*ValueType" MacStatus/Readers/ReaderProtocol.swift`

    **TimerReader.swift:**
    - File exists at `MacStatus/Readers/TimerReader.swift`
    - Contains `class TimerReader<T>: ReaderProtocol` — verify: `grep "class TimerReader.*ReaderProtocol" MacStatus/Readers/TimerReader.swift`
    - `start()` creates `Timer.scheduledTimer` — verify: `grep "scheduledTimer" MacStatus/Readers/TimerReader.swift`
    - `start()` dispatches to `DispatchQueue.global(qos: .utility)` — verify: `grep "utility" MacStatus/Readers/TimerReader.swift`
    - `start()` fires first read immediately (LIFE-03) — verify: `grep "self?.read()" MacStatus/Readers/TimerReader.swift` (inside start(), not inside the timer block)
    - `stop()` calls `timer?.invalidate()` — verify: `grep "invalidate" MacStatus/Readers/TimerReader.swift`

    **CPUReader.swift (updated):**
    - Declaration: `final class CPUReader: TimerReader<Double>` — verify: `grep "class CPUReader.*TimerReader" MacStatus/Readers/CPUReader.swift`
    - Has `override func setup()` initializing `previousInfo` — verify: `grep "override func setup" MacStatus/Readers/CPUReader.swift`
    - Has `override func read()` with host_statistics — verify: `grep "override func read" MacStatus/Readers/CPUReader.swift`
    - No `var onUpdate` declaration (inherited from TimerReader) — verify: `grep "var onUpdate" MacStatus/Readers/CPUReader.swift` returns 0

    **SettingsManager.swift:**
    - File exists at `MacStatus/Utils/SettingsManager.swift`
    - Contains `static let shared = SettingsManager()` — verify: `grep "static.*shared" MacStatus/Utils/SettingsManager.swift`
    - Contains `var refreshInterval: TimeInterval` — verify: `grep "refreshInterval.*TimeInterval" MacStatus/Utils/SettingsManager.swift`
    - Uses `UserDefaults.standard` — verify: `grep "UserDefaults" MacStatus/Utils/SettingsManager.swift`
    - Has `private init() {}` — verify: `grep "private init" MacStatus/Utils/SettingsManager.swift`

    **AppDelegate.swift (simplified further):**
    - Calls `cpuReader.start()` in `applicationDidFinishLaunching` — verify: `grep "cpuReader.*start()" MacStatus/App/AppDelegate.swift`
    - Does NOT contain `Timer.scheduledTimer` — Timer management is in TimerReader — verify: `grep "scheduledTimer" MacStatus/App/AppDelegate.swift` returns 0
    - Calls `cpuReader?.stop()` in `applicationWillTerminate` — verify: `grep "cpuReader.*stop()" MacStatus/App/AppDelegate.swift`

    **Build verification:**
    - `xcodebuild -project MacStatus.xcodeproj -scheme MacStatus build` compiles without errors
    - All 6 source files are included in the build (no "undefined symbol" linker errors)
  </acceptance_criteria>

  <verify>
    <automated>
# Verify all new architecture files exist and have correct class/protocol signatures
test -f MacStatus/Readers/ReaderProtocol.swift && \
test -f MacStatus/Readers/TimerReader.swift && \
test -f MacStatus/Readers/CPUReader.swift && \
test -f MacStatus/UI/StatusBarManager.swift && \
test -f MacStatus/Utils/SettingsManager.swift && \
grep -q "protocol ReaderProtocol" MacStatus/Readers/ReaderProtocol.swift && \
grep -q "class TimerReader.*ReaderProtocol" MacStatus/Readers/TimerReader.swift && \
grep -q "class CPUReader.*TimerReader" MacStatus/Readers/CPUReader.swift && \
grep -q "class StatusBarManager" MacStatus/UI/StatusBarManager.swift && \
grep -q "class SettingsManager" MacStatus/Utils/SettingsManager.swift && \
grep -q "cpuReader.start()" MacStatus/App/AppDelegate.swift && \
echo "PASS: Architecture extracted successfully"
    </automated>
  </verify>
  <done>All 5 source files populate the D-02 folder structure. CPUReader extends TimerReader<Double> (including host_statistics data collection, CPULoad Sendable wrapper, KERN_SUCCESS error check, delta calculation, &- overflow, proper dispatch). StatusBarManager handles NSStatusItem lifecycle with deinit cleanup, tolerance check, monospaced digits, labelColor, and macOS 26 visibility detection. ReaderProtocol and TimerReader are reusable for Phase 2-5 readers. SettingsManager stores refresh interval in UserDefaults. AppDelegate is a thin ~20-line wiring hub. Project builds and CPU display works identically to Plan 01-01.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| TimerReader background queue → CPUReader.read() | `Timer.scheduledTimer` fires on a background `DispatchQueue`. The `read()` method calls `host_statistics()` — a kernel trap crossing user→kernel boundary. |
| CPUReader.onUpdate → StatusBarManager.updateCPU | Closure callback delivers `Double?` from background queue to main thread. Swift 6 strict concurrency requires the value to be `Sendable`. |
| App process → UserDefaults | `SettingsManager` reads/writes to `UserDefaults.standard`. No sensitive data stored (only refresh interval). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Denial of Service | `host_statistics()` in CPUReader.read() | mitigate | `kern_return_t` checked against `KERN_SUCCESS` on every call. Failure → `onUpdate(nil)` → StatusBarManager displays "CPU --%". No crash, no retry storm. Same as T-01-01 from Plan 01, now in CPUReader class. |
| T-01-06 | Denial of Service | Timer retain cycle in TimerReader | mitigate | `Timer.scheduledTimer` uses `[weak self]` in closure. `stop()` calls `timer?.invalidate()`. `deinit` indirectly triggered when AppDelegate sets `cpuReader = nil`. Verified by print statement in `StatusBarManager.deinit`. |
| T-01-07 | Spoofing | NSStatusItem ghost icons from StatusBarManager | mitigate | `StatusBarManager.deinit` calls `NSStatusBar.system.removeStatusItem(statusItem)`. `autosaveName = "com.macstatus.cpu"` prevents duplicate registration. AppDelegate's `applicationWillTerminate` sets `statusBarManager = nil` to trigger deinit. |
| T-01-08 | Spoofing | macOS 26 privacy gate — status item hidden without user consent | mitigate | `StatusBarManager.init()` schedules a 2-second delayed `asyncAfter` check. If statusItem is not visible, shows `NSAlert` directing user to System Settings → Menu Bar. On macOS 14-15 the item is immediately visible and alert never fires. |
| T-01-09 | Elevation of Privilege | UserDefaults writes from sandboxed app | accept | `UserDefaults.standard` is sandbox-compatible. Stores only `refreshInterval` (Double). No sensitive data, no IPC, no XPC. Within app's own container. |
| T-01-10 | Information Disclosure | CPU percentage visible in menu bar | accept | CPU usage is non-sensitive system data. Same risk as T-01-04 from Plan 01. Accept. |
</threat_model>

<verification>
**Overall architecture verification:**
1. `xcodebuild -project MacStatus.xcodeproj -scheme MacStatus build` succeeds — all 6 source files compile
2. App launches and shows "CPU XX%" in menu bar — behavior unchanged from Plan 01-01
3. Launch and quit 3 times — no ghost icon duplicates appear (Pitfall 1 validated)
4. Activity Monitor shows MacStatus CPU usage < 1% after 30 minutes of operation (CPU-02)
5. StatusBarManager.deinit print statement appears in Xcode console on quit — verifies no retain cycles
6. On macOS 26, if status item is not visible, NSAlert appears directing to System Settings
7. All files match D-02 folder structure: App/ (AppDelegate), Readers/ (CPUReader, ReaderProtocol, TimerReader), UI/ (StatusBarManager), Utils/ (SettingsManager)
</verification>

<success_criteria>
Architecture matches the D-02 folder structure. CPUReader is a standalone, testable class that any Phase 2-5 reader can mirror. StatusBarManager handles all NSStatusBar lifecycle concerns. ReaderProtocol + TimerReader provide the base pattern for NetworkReader, MemoryReader, GPUReader in future phases. SettingsManager is ready for Phase 2+ use. App has been verified to have <1% CPU overhead (CPU-02) and no ghost icon leaks (LIFE-01).
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-cpu-monitoring/01-02-SUMMARY.md`
</output>
