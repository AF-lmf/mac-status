---
phase: 12-popover-layout-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MacStatus/MacStatus/UI/Views/StableValueLayout.swift
  - MacStatus/MacStatus/UI/Views/DashboardView.swift
  - MacStatus/MacStatus.xcodeproj/project.pbxproj
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04]
requirements_addressed: [LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04]
must_haves:
  truths:
    - "D-04/D-05: DashboardView root width is fixed at exactly 372pt and no refresh/data state changes that value."
    - "D-01/D-02/D-03: high-jitter dashboard values render through shared fixed-width, right-aligned, monospaced-digit helpers."
    - "D-06/D-08/D-09: metric cards, battery rows, and temperature/fan rows keep the compact popover identity and do not change network metric meaning."
  artifacts:
    - path: "MacStatus/MacStatus/UI/Views/StableValueLayout.swift"
      provides: "DashboardLayout, StableValueWidth, StableValueText, StableValueRow, and StableCaptionText"
      exports: ["DashboardLayout", "StableValueWidth", "StableValueText", "StableValueRow", "StableCaptionText"]
    - path: "MacStatus/MacStatus/UI/Views/DashboardView.swift"
      provides: "Fixed 372pt popover root and stable dashboard value-column integration"
      contains: "DashboardLayout.popoverWidth"
  key_links:
    - from: "MacStatus/MacStatus/UI/Views/DashboardView.swift"
      to: "MacStatus/MacStatus/UI/Views/StableValueLayout.swift"
      via: "SwiftUI helper usage"
      pattern: "StableValue(Text|Row|Width)|DashboardLayout\\.popoverWidth"
    - from: "MacStatus/MacStatus.xcodeproj/project.pbxproj"
      to: "MacStatus/MacStatus/UI/Views/StableValueLayout.swift"
      via: "app target source membership"
      pattern: "StableValueLayout\\.swift"
---

<objective>
Create the Phase 12 stable value-column foundation and apply it to the existing dashboard popover hotspots that live in `DashboardView`.

Purpose: satisfy D-01 through D-09 for the dashboard-owned surfaces before process-row and fixture verification work consumes the shared helpers.
Output: `StableValueLayout.swift`, fixed `372pt` popover width, and dashboard metric/battery/temperature/fan rows that use fixed value widths instead of local `minWidth`/`Spacer` negotiation.
</objective>

<execution_context>
@/Users/halo/.codex/gsd-core/workflows/execute-plan.md
@/Users/halo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-popover-layout-stability/12-CONTEXT.md
@.planning/phases/12-popover-layout-stability/12-RESEARCH.md
@.planning/phases/12-popover-layout-stability/12-PATTERNS.md
@.planning/phases/12-popover-layout-stability/12-UI-SPEC.md
@MacStatus/MacStatus/UI/Views/DashboardView.swift
@MacStatus/MacStatus/UI/PopoverManager.swift
@MacStatus/MacStatus.xcodeproj/project.pbxproj
</context>

<source_audit>
SOURCE | ID | Feature/Requirement | Plan | Status | Notes
GOAL | Phase 12 | Popover width, rows, columns, and text remain stable under network/temperature/RPM/power/process text changes | 12-01, 12-02, 12-03 | COVERED | Fixed width and stable columns are implemented first, process rows second, deterministic verification third.
REQ | LAYOUT-01 | No horizontal/vertical popover jitter under changing network/temperature/RPM/power values | 12-01, 12-03 | COVERED | Fixed root width and measured short/extreme fixture tests.
REQ | LAYOUT-02 | Fixed width, right aligned, monospaced digit numeric columns for `9999 RPM`, `100°C`, `N/A`, large network values | 12-01, 12-02, 12-03 | COVERED | Shared width constants plus fixture/test assertions.
REQ | LAYOUT-03 | Long process names, sensor labels, and capability copy yield before values move | 12-01, 12-02, 12-03 | COVERED | Stable rows and process row trailing-width reservation.
REQ | LAYOUT-04 | Fixed popover width in approved 360-380pt range | 12-01, 12-03 | COVERED | `DashboardLayout.popoverWidth = 372` and size tests.
REQ | UAT-04 | Deterministic snapshots or test data cover extreme values | 12-02, 12-03 | COVERED | DEBUG fixtures plus XCTest layout checks.
RESEARCH | Fixed 372pt root with `NSHostingController.sizingOptions = [.preferredContentSize]` | 12-01, 12-03 | COVERED | `PopoverManager` remains host-only and does not set competing width.
RESEARCH | No package installs; use SwiftUI/AppKit/XCTest only | 12-01, 12-02, 12-03 | COVERED | No external dependencies or registry changes.
RESEARCH | Treat long process/sensor text as untrusted-length UI input | 12-01, 12-02 | COVERED | Labels/names truncate or become captions before values yield.
CONTEXT | D-01 | Reusable stable value-row/value-column abstraction | 12-01 | COVERED | `StableValueText` and `StableValueRow`.
CONTEXT | D-02 | Scope helper to network, temperature, fan RPM, power/battery, Top-N trailing values | 12-01, 12-02 | COVERED | No dashboard-wide rewrite.
CONTEXT | D-03 | Explicit per-kind widths, right alignment, monospaced digits | 12-01 | COVERED | `StableValueWidth` constants.
CONTEXT | D-04 | Fixed expansion allowed to 360-380pt | 12-01 | COVERED | Uses `372pt`.
CONTEXT | D-05 | Width fixed once and not refresh-dependent | 12-01, 12-03 | COVERED | Root constant plus tests.
CONTEXT | D-06 | Preserve compact popover identity | 12-01 | COVERED | No large dashboard redesign.
CONTEXT | D-07 | Text side yields before numeric value columns | 12-01, 12-02 | COVERED | Stable rows and process left-region rules.
CONTEXT | D-08 | Capability/status copy must not affect paired value width | 12-01 | COVERED | Fan captions are full-width below RPM row.
CONTEXT | D-09 | Network display remains compact up/down; stabilize layout only | 12-01, 12-02 | COVERED | No `ByteFormatting` semantic change.
CONTEXT | D-10 | Deterministic extreme test data required | 12-02, 12-03 | COVERED | Fixtures and tests.
CONTEXT | D-11 | Fixtures cover short/large network, `9999 RPM`, `100°C`, `N/A`, power, long text, mixed availability | 12-02, 12-03 | COVERED | Fixture acceptance criteria list exact values.
CONTEXT | D-12 | Verification checks width and row/value-column stability across short/long states | 12-03 | COVERED | XCTest width/height/contract checks plus verification artifact.
</source_audit>

<artifacts_this_phase_produces>
This plan creates:
- File: `MacStatus/MacStatus/UI/Views/StableValueLayout.swift`
- Symbol: `enum DashboardLayout`
- Symbol: `DashboardLayout.popoverWidth`
- Symbol: `enum StableValueWidth`
- Constants: `StableValueWidth.percentage`, `StableValueWidth.networkCard`, `StableValueWidth.processNetworkRate`, `StableValueWidth.processNetworkPair`, `StableValueWidth.temperature`, `StableValueWidth.fanRPM`, `StableValueWidth.batteryPower`, `StableValueWidth.batteryHealthTime`, `StableValueWidth.processCPU`, `StableValueWidth.processMemory`
- Symbol: `struct StableValueText`
- Symbol: `struct StableValueRow`
- Symbol: `struct StableCaptionText`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Define shared stable value layout primitives</name>
  <files>MacStatus/MacStatus/UI/Views/StableValueLayout.swift, MacStatus/MacStatus.xcodeproj/project.pbxproj</files>
  <read_first>
    - `.planning/phases/12-popover-layout-stability/12-CONTEXT.md` decisions D-01, D-02, D-03, D-04, D-05, D-06, D-07, D-08, D-09.
    - `.planning/phases/12-popover-layout-stability/12-UI-SPEC.md` sections "Fixed Popover Width", "Stable Value Widths", "Text Yield Rules".
    - `.planning/phases/12-popover-layout-stability/12-PATTERNS.md` sections for `StableValueLayout.swift` and `project.pbxproj`.
    - `MacStatus/MacStatus/UI/Views/DashboardView.swift` for existing typography, spacing, and card style.
    - `MacStatus/MacStatus.xcodeproj/project.pbxproj` for the existing Views group and Sources build phase pattern.
  </read_first>
  <action>Create `StableValueLayout.swift` next to other SwiftUI views and register it in the app target per D-01/D-02. Define `DashboardLayout.popoverWidth` as exactly `372`. Define `StableValueWidth` constants exactly as follows: `percentage = 64`, `networkCard = 76`, `processNetworkRate = 68`, `processNetworkPair = 148`, `temperature = 56`, `fanRPM = 78`, `batteryPower = 104`, `batteryHealthTime = 112`, `processCPU = 52`, `processMemory = 68`. Define `StableValueText` with fixed `width`, trailing alignment, `.monospacedDigit()`, `.lineLimit(...)`, `.minimumScaleFactor(0.9)`, and higher layout priority for values per D-03. Define `StableValueRow` so the label uses `.lineLimit(1)`, tail truncation, max-width leading alignment, lower layout priority, and a caller-supplied fixed-width value. Define `StableCaptionText` for fan range/target/control-status captions with `.font(.caption2)`, `.foregroundStyle(.secondary)`, `.lineLimit(2)`, and leading max-width layout per D-07/D-08. Do not add package dependencies, status-bar code, fan controls, SMC write paths, alerts, charts, or raw sensor browsing.</action>
  <verify>
    <automated>xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build</automated>
    <automated>rg -n "enum DashboardLayout|popoverWidth = 372|enum StableValueWidth|struct StableValueText|struct StableValueRow|struct StableCaptionText|monospacedDigit|layoutPriority" MacStatus/MacStatus/UI/Views/StableValueLayout.swift</automated>
    <automated>rg -n "StableValueLayout.swift" MacStatus/MacStatus.xcodeproj/project.pbxproj</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `DashboardLayout.popoverWidth` is exactly `372`, not `320`, `360`, or `380`.
    - Source assertion: each UI-SPEC width constant exists with the exact numeric value listed in this task.
    - Behavior assertion: values rendered through `StableValueText` have fixed `frame(width:alignment: .trailing)` behavior and do not use space padding.
    - Scope assertion: no external package, fan-control, SMC-write, status-bar, alert, chart, or raw sensor browser surface is introduced.
    - Test command: Debug `xcodebuild` succeeds.
  </acceptance_criteria>
  <done>`StableValueLayout.swift` exists, is part of the `MacStatus` app target, exports the required constants/helpers, and builds without changing runtime monitoring or hardware behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Apply fixed width and stable dashboard-owned value columns</name>
  <files>MacStatus/MacStatus/UI/Views/DashboardView.swift, MacStatus/MacStatus/UI/Views/StableValueLayout.swift</files>
  <read_first>
    - `.planning/phases/12-popover-layout-stability/12-CONTEXT.md` decisions D-01 through D-09.
    - `.planning/phases/12-popover-layout-stability/12-UI-SPEC.md` sections "Fixed Popover Width", "Section Contracts", "Interaction Contract".
    - `.planning/phases/12-popover-layout-stability/12-PATTERNS.md` sections for `DashboardView.swift`, `MetricCardWithSparkline`, `TemperatureAndFanSectionView`, and `BatterySectionView`.
    - `MacStatus/MacStatus/UI/Views/DashboardView.swift`.
    - `MacStatus/MacStatus/UI/PopoverManager.swift` to preserve `NSHostingController.sizingOptions = [.preferredContentSize]` and avoid setting a competing popover width.
  </read_first>
  <action>Replace the root `.frame(width: 320)` with `.frame(width: DashboardLayout.popoverWidth)` per D-04/D-05. Keep `PopoverManager` unchanged. Update `MetricCardWithSparkline` so CPU, Memory, and GPU values use `StableValueWidth.percentage`, and Network uses `StableValueWidth.networkCard` while keeping the current two-line compact up/down meaning per D-09. Update `TemperatureAndFanSectionView` so the header CPU/SoC value, `temperatureRow`, and `fanRow` use `StableValueText`/`StableValueRow` with `temperature = 56` and `fanRPM = 78`; remove Phase 12 hotspot `frame(minWidth: 52)` and `frame(minWidth: 72)`. Keep fan range, target, and `边界可读，控制未启用` as `StableCaptionText` below the RPM row so explanatory copy never participates in the paired value width per D-08. Update `BatterySectionView.row` so `电池功率` and `整机功耗` use `StableValueWidth.batteryPower`, while `剩余时间`/`距充满` and `健康度` use `StableValueWidth.batteryHealthTime`. Update `ProcessResourceSectionView` in this file to accept a fixed `trailingWidth` and render CPU rows with `StableValueWidth.processCPU` and memory rows with `StableValueWidth.processMemory`; this covers the CPU/memory Top-N trailing values without touching network process rows, which are handled in Plan 12-02 per D-02. Preserve compact card backgrounds, 8pt section spacing, existing settings gates, and all read-only thermal/fan behavior. Do not add status-bar temperature/fan segments, fan-control UI, helper/XPC, alerts, charts, new readers, history persistence, or raw sensor browsing.</action>
  <verify>
    <automated>xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build</automated>
    <automated>rg -n "DashboardLayout\\.popoverWidth|StableValueText|StableValueRow|StableCaptionText|StableValueWidth\\.(percentage|networkCard|temperature|fanRPM|batteryPower|batteryHealthTime|processCPU|processMemory)" MacStatus/MacStatus/UI/Views/DashboardView.swift</automated>
    <automated>bash -lc 'if rg -n "\\.frame\\(width: 320\\)|frame\\(minWidth: (52|72)" MacStatus/MacStatus/UI/Views/DashboardView.swift; then exit 1; fi'</automated>
    <automated>bash -lc 'if rg -n "StatusBarManager|Slider\\(|Stepper\\(|Button\\(\".*风扇|raw SMC|传感器浏览|history chart|Chart\\(|Alert\\(" MacStatus/MacStatus/UI/Views/DashboardView.swift MacStatus/MacStatus/UI/Views/StableValueLayout.swift; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `DashboardView` has exactly one root popover width source and it is `DashboardLayout.popoverWidth`.
    - Source assertion: Phase 12 dashboard hotspots no longer use `frame(minWidth: 52)` or `frame(minWidth: 72)`.
    - Behavior assertion: long labels/captions yield or wrap below the row before changing temperature, RPM, battery, or metric value column widths.
    - Behavior assertion: network metric text remains the current compact up/down display; this task does not change `ByteFormatting` semantics.
    - Scope assertion: no fan control, SMC write/helper/XPC, status-bar feature work, alerts, charts, or raw sensor browsing is added.
    - Test command: Debug `xcodebuild` succeeds and the grep gates above pass.
  </acceptance_criteria>
  <done>`DashboardView` uses the fixed 372pt root and shared stable value helpers for metric cards, battery rows, temperature rows, fan RPM rows, fan captions, and CPU/memory process trailing values.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process/sensor text -> SwiftUI layout | Process names, PID text, sensor labels, fan captions, and value strings can be longer than expected and must not cause layout denial or misleading clipped values. |
| app source -> hardware readers | This plan must not add hardware write paths, fan control, helper/XPC, or raw SMC surfaces. |
| debug/test support -> user UI | This plan creates no fixtures yet; later deterministic fixtures must remain DEBUG/test-only and non-user-facing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-DOS | Denial of Service | `DashboardView` label/value rows | mitigate | Fixed 372pt root plus fixed value widths; labels/captions truncate or wrap before values yield. |
| T-12-01-I | Information Integrity | temperature/RPM/battery/network values | mitigate | Right-aligned monospaced fixed value cells sized for UI-SPEC worst cases. |
| T-12-01-T | Tampering | hardware control boundary | mitigate | No fan controls, no SMC writes, no helper/XPC, no status-bar fan/thermal work; verification greps touched UI files. |
| T-12-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned; package legitimacy audit says none. |
</threat_model>

<verification>
Overall plan verification:
- `xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build`
- `rg -n "StableValueLayout.swift|DashboardLayout\\.popoverWidth|StableValueWidth" MacStatus/MacStatus.xcodeproj/project.pbxproj MacStatus/MacStatus/UI/Views`
- `bash -lc 'if rg -n "\\.frame\\(width: 320\\)|frame\\(minWidth: (52|72)" MacStatus/MacStatus/UI/Views/DashboardView.swift; then exit 1; fi'`
</verification>

<success_criteria>
Plan 12-01 is complete when the dashboard popover builds with a named `372pt` width, dashboard-owned high-jitter values use shared stable value helpers, and no Phase 13+ fan-control/status-bar/raw-browser scope appears in touched files.
</success_criteria>

<output>
Create `.planning/phases/12-popover-layout-stability/12-01-SUMMARY.md` when done.
</output>
