---
phase: 12-popover-layout-stability
plan: 03
type: execute
wave: 3
depends_on: [12-01, 12-02]
files_modified:
  - MacStatus/MacStatusTests/DashboardLayoutStabilityTests.swift
  - MacStatus/MacStatus/UI/Views/StableValueLayout.swift
  - MacStatus/MacStatus/UI/Views/DashboardView.swift
  - MacStatus/MacStatus/UI/Views/ProcessListView.swift
  - MacStatus/MacStatus.xcodeproj/project.pbxproj
  - MacStatus/MacStatus.xcodeproj/xcshareddata/xcschemes/MacStatus.xcscheme
  - .planning/phases/12-popover-layout-stability/12-LAYOUT-VERIFICATION.md
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04, UAT-04]
requirements_addressed: [LAYOUT-01, LAYOUT-02, LAYOUT-03, LAYOUT-04, UAT-04]
must_haves:
  truths:
    - "D-10/D-11: short and extreme fixture states are executed by an automated deterministic test path."
    - "D-12: tests explicitly assert fixed 372pt width and same-visible-section layout stability between short and long values."
    - "D-12: tests explicitly record and compare value-column x-position and width for network card, temperature, fan RPM, battery/power, and process trailing surfaces."
    - "LAYOUT-01 through LAYOUT-04 and UAT-04 have reproducible command output captured in a verification artifact."
  artifacts:
    - path: "MacStatus/MacStatusTests/DashboardLayoutStabilityTests.swift"
      provides: "Automated XCTest layout stability assertions"
      exports: ["DashboardLayoutStabilityTests"]
    - path: ".planning/phases/12-popover-layout-stability/12-LAYOUT-VERIFICATION.md"
      provides: "Recorded deterministic layout verification output"
  key_links:
    - from: "MacStatus/MacStatusTests/DashboardLayoutStabilityTests.swift"
      to: "MacStatus/MacStatus/UI/Fixtures/DashboardLayoutFixtures.swift"
      via: "DashboardLayoutFixture short/extreme states"
      pattern: "DashboardLayoutFixture\\.(Kind|make|apply)"
    - from: "MacStatus/MacStatusTests/DashboardLayoutStabilityTests.swift"
      to: "MacStatus/MacStatus/UI/Views/DashboardView.swift"
      via: "NSHostingController measurement"
      pattern: "NSHostingController|DashboardView|DashboardLayout\\.popoverWidth"
---

<objective>
Add the deterministic layout verification gate for Phase 12 and record the output that proves fixed popover width and stable value-column behavior.

Purpose: satisfy UAT-04 and D-12 without relying on live hardware readings or one-off visual inspection.
Output: an XCTest target/test file, a shared scheme that runs the tests, and `12-LAYOUT-VERIFICATION.md` with the final command/source-gate evidence.
</objective>

<execution_context>
@/Users/halo/.codex/gsd-core/workflows/execute-plan.md
@/Users/halo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-popover-layout-stability/12-CONTEXT.md
@.planning/phases/12-popover-layout-stability/12-RESEARCH.md
@.planning/phases/12-popover-layout-stability/12-PATTERNS.md
@.planning/phases/12-popover-layout-stability/12-UI-SPEC.md
@.planning/phases/12-popover-layout-stability/12-01-SUMMARY.md
@.planning/phases/12-popover-layout-stability/12-02-SUMMARY.md
@MacStatus/MacStatus/UI/Views/DashboardView.swift
@MacStatus/MacStatus/UI/Views/StableValueLayout.swift
@MacStatus/MacStatus/UI/Views/ProcessListView.swift
@MacStatus/MacStatus/UI/Fixtures/DashboardLayoutFixtures.swift
@MacStatus/MacStatus/UI/PopoverManager.swift
@MacStatus/MacStatus/Utils/SettingsManager.swift
@MacStatus/MacStatus.xcodeproj/project.pbxproj
</context>

<artifacts_this_phase_produces>
This plan creates:
- Directory: `MacStatus/MacStatusTests/`
- File: `MacStatus/MacStatusTests/DashboardLayoutStabilityTests.swift`
- Xcode target: `MacStatusTests`
- Product: `MacStatusTests.xctest`
- Shared scheme: `MacStatus/MacStatus.xcodeproj/xcshareddata/xcschemes/MacStatus.xcscheme`
- File: `.planning/phases/12-popover-layout-stability/12-LAYOUT-VERIFICATION.md`
- Symbol: `final class DashboardLayoutStabilityTests`
- Function: `DashboardLayoutStabilityTests.testPopoverWidthIsFixedForShortAndExtremeFixtures()`
- Function: `DashboardLayoutStabilityTests.testSameVisibilityFixturesKeepStableHeight()`
- Function: `DashboardLayoutStabilityTests.testStableValueWidthContractMatchesUISpec()`
- Function: `DashboardLayoutStabilityTests.testProcessRowsReserveTrailingValueColumns()`
- Function: `DashboardLayoutStabilityTests.testDashboardValueColumnFramesStayStableAcrossFixtures()`
- Function: `DashboardLayoutStabilityTests.measuredDashboardSize(for:)`
- Function: `DashboardLayoutStabilityTests.valueColumnFrameSnapshot(for:)`
- Function: `DashboardLayoutStabilityTests.assertStableValueColumnFrames(_:_:ids:)`
- Function: `DashboardLayoutStabilityTests.withAllPopoverSectionsVisible(_:)`
- Symbol: `LayoutProbeKey`
- Symbol: `LayoutProbeID`
- Symbol: `LayoutProbeFrameSnapshot`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Add XCTest target and shared scheme for layout stability</name>
  <files>MacStatus/MacStatusTests/DashboardLayoutStabilityTests.swift, MacStatus/MacStatus.xcodeproj/project.pbxproj, MacStatus/MacStatus.xcodeproj/xcshareddata/xcschemes/MacStatus.xcscheme</files>
  <read_first>
    - `.planning/phases/12-popover-layout-stability/12-CONTEXT.md` decisions D-10, D-11, D-12.
    - `.planning/phases/12-popover-layout-stability/12-RESEARCH.md` sections "Pitfall 5", "View Size Assertion Shape", and "Environment Availability".
    - `.planning/phases/12-popover-layout-stability/12-PATTERNS.md` sections for `MacStatusTests/DashboardLayoutStabilityTests.swift` and `project.pbxproj`.
    - `.planning/phases/12-popover-layout-stability/12-02-SUMMARY.md` for the fixture symbol names from Plan 12-02.
    - `MacStatus/MacStatus.xcodeproj/project.pbxproj`.
  </read_first>
  <action>Add a native XCTest bundle target named `MacStatusTests` because `xcodebuild -list` currently lists only `MacStatus` and UAT-04 requires deterministic automation. Create `MacStatus/MacStatusTests/DashboardLayoutStabilityTests.swift` with imports and a compiling `final class DashboardLayoutStabilityTests: XCTestCase` declaration. Add `XCTest.framework`, a target dependency on `MacStatus`, product `MacStatusTests.xctest`, bundle id `com.aflmf.macstatus.tests`, `TEST_HOST = $(BUILT_PRODUCTS_DIR)/MacStatus.app/Contents/MacOS/MacStatus`, `BUNDLE_LOADER = $(TEST_HOST)`, `SWIFT_VERSION = 6.0`, `MACOSX_DEPLOYMENT_TARGET = 14.0`, and strict concurrency-compatible Debug/Release settings. Create `MacStatus/MacStatus.xcodeproj/xcshareddata/xcschemes/MacStatus.xcscheme` so `xcodebuild test -scheme MacStatus -destination 'platform=macOS'` builds the app and runs `MacStatusTests`. Do not add external snapshot packages or a user-facing debug menu.</action>
  <verify>
    <automated>xcodebuild -list -project MacStatus/MacStatus.xcodeproj</automated>
    <automated>rg -n "MacStatusTests|DashboardLayoutStabilityTests.swift|XCTest.framework|TEST_HOST|BUNDLE_LOADER|com\\.aflmf\\.macstatus\\.tests" MacStatus/MacStatus.xcodeproj/project.pbxproj</automated>
    <automated>test -f MacStatus/MacStatus.xcodeproj/xcshareddata/xcschemes/MacStatus.xcscheme</automated>
    <automated>xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -destination 'platform=macOS' build-for-testing</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `xcodebuild -list` includes both `MacStatus` and `MacStatusTests`.
    - Source assertion: the shared `MacStatus.xcscheme` contains a TestAction for `MacStatusTests.xctest`.
    - Source assertion: test target is app-hosted and uses the existing `MacStatus.app` as `TEST_HOST`, not a separate app or external package.
    - Scope assertion: no debug menu, settings toggle, status-bar segment, fan control, network/API endpoint, secret, or hardware write path is added.
    - Test command: `build-for-testing` succeeds.
  </acceptance_criteria>
  <done>The project has a runnable XCTest target and shared scheme ready for deterministic layout assertions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic layout stability tests and record evidence</name>
  <files>MacStatus/MacStatusTests/DashboardLayoutStabilityTests.swift, MacStatus/MacStatus/UI/Views/StableValueLayout.swift, MacStatus/MacStatus/UI/Views/DashboardView.swift, MacStatus/MacStatus/UI/Views/ProcessListView.swift, .planning/phases/12-popover-layout-stability/12-LAYOUT-VERIFICATION.md</files>
  <read_first>
    - `.planning/phases/12-popover-layout-stability/12-CONTEXT.md` decisions D-10, D-11, D-12.
    - `.planning/phases/12-popover-layout-stability/12-UI-SPEC.md` sections "Fixed Popover Width", "Stable Value Widths", and "Deterministic Verification Contract".
    - `.planning/phases/12-popover-layout-stability/12-RESEARCH.md` section "View Size Assertion Shape".
    - `MacStatus/MacStatus/UI/Views/DashboardView.swift`.
    - `MacStatus/MacStatus/UI/Views/StableValueLayout.swift`.
    - `MacStatus/MacStatus/UI/Views/ProcessListView.swift`.
    - `MacStatus/MacStatus/UI/Fixtures/DashboardLayoutFixtures.swift`.
    - `MacStatus/MacStatus/Utils/SettingsManager.swift`.
  </read_first>
  <behavior>
    - Test 1: Measuring `DashboardView` with `DashboardLayoutFixture.Kind.short` returns width `DashboardLayout.popoverWidth` with tolerance `0.5`.
    - Test 2: Measuring `DashboardView` with `DashboardLayoutFixture.Kind.extreme` returns width `DashboardLayout.popoverWidth` with tolerance `0.5`.
    - Test 3: Short and extreme same-visibility fixtures have equal measured height within tolerance `0.5`, proving text length does not create vertical jump for the same visible section set.
    - Test 4: `StableValueWidth` constants equal the UI-SPEC contract values exactly.
    - Test 5: `ProcessMetricRow` with short and long process names uses the same fixed trailing width for `StableValueWidth.processNetworkPair`.
    - Test 6: Short and extreme `DashboardView` fixtures produce equal `x` and `width` frames for these value-column identifiers: `networkMetricCardValue`, `temperatureValueColumn`, `fanRPMValueColumn`, `batteryPowerValueColumn`, `systemPowerValueColumn`, and process trailing value columns for network, CPU, and memory rows.
  </behavior>
  <action>Implement `DashboardLayoutStabilityTests` with `@MainActor` measurement helpers that create a `DashboardState` through `DashboardLayoutFixture.make(_:)`, host `DashboardView().environmentObject(state)` in `NSHostingController`, set `sizingOptions = [.preferredContentSize]`, force layout, and assert `fittingSize.width == DashboardLayout.popoverWidth` with accuracy `0.5` for both `.short` and `.extreme` per D-12. Add `withAllPopoverSectionsVisible(_:)` to capture and restore `SettingsManager.shared.showBatterySection`, `showThermalSection`, `showFanSection`, and `showProcessSection` so tests are deterministic regardless of local UserDefaults. Assert same-visible-section heights are equal within `0.5` for short/extreme fixtures. Assert every `StableValueWidth` constant matches the UI-SPEC values from Plan 12-01. Add a process-row layout test using `ProcessMetricRow(trailingWidth: StableValueWidth.processNetworkPair)` with a short name and a long name to prove the measured row width and trailing reservation remain stable. Add DEBUG/test-only geometry probes if they do not already exist: define `LayoutProbeKey`, `LayoutProbeID`, and `LayoutProbeFrameSnapshot` without creating any user-facing UI; attach stable identifiers to the value surfaces required by D-12. The required identifiers are `networkMetricCardValue`, `temperatureValueColumn`, `fanRPMValueColumn`, `batteryPowerValueColumn`, `systemPowerValueColumn`, `networkProcessTrailingValue`, `cpuProcessTrailingValue`, and `memoryProcessTrailingValue`. Implement `valueColumnFrameSnapshot(for:)` to return a dictionary keyed by `LayoutProbeID` containing each value column's `CGRect`, then compare short and extreme snapshots with `assertStableValueColumnFrames(_:_:ids:)`, asserting both `origin.x` and `width` are equal within `0.5pt` for every required identifier. This comparison must cover the network metric card value block, temperature value column, fan RPM value column, battery/power value columns, and process trailing values, not only process rows. After tests pass, create `12-LAYOUT-VERIFICATION.md` with command output summaries for `xcodebuild test`, required fixture values, source gates for `372`, fixed widths, geometry probe identifiers, frame snapshot comparisons, no `frame(width: 320)`, no hotspot `frame(minWidth:)`, and no forbidden Phase 13+ scope. Do not rely on SwiftUI previews or manual visual inspection as the only evidence.</action>
  <verify>
    <automated>xcodebuild test -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -destination 'platform=macOS' -derivedDataPath build</automated>
    <automated>rg -n "testPopoverWidthIsFixedForShortAndExtremeFixtures|testSameVisibilityFixturesKeepStableHeight|testStableValueWidthContractMatchesUISpec|testProcessRowsReserveTrailingValueColumns|measuredDashboardSize|withAllPopoverSectionsVisible|accuracy: 0\\.5|DashboardLayout\\.popoverWidth" MacStatus/MacStatusTests/DashboardLayoutStabilityTests.swift</automated>
    <automated>rg -n "LayoutProbeKey|LayoutProbeID|LayoutProbeFrameSnapshot|valueColumnFrameSnapshot|assertStableValueColumnFrames|testDashboardValueColumnFramesStayStableAcrossFixtures|networkMetricCardValue|temperatureValueColumn|fanRPMValueColumn|batteryPowerValueColumn|systemPowerValueColumn|networkProcessTrailingValue|cpuProcessTrailingValue|memoryProcessTrailingValue|origin\\.x|\\.width" MacStatus/MacStatusTests/DashboardLayoutStabilityTests.swift MacStatus/MacStatus/UI/Views/StableValueLayout.swift MacStatus/MacStatus/UI/Views/DashboardView.swift MacStatus/MacStatus/UI/Views/ProcessListView.swift</automated>
    <automated>bash -lc 'if rg -n "\\.frame\\(width: 320\\)|frame\\(minWidth: (52|72)" MacStatus/MacStatus/UI/Views/DashboardView.swift MacStatus/MacStatus/UI/Views/ProcessListView.swift; then exit 1; fi'</automated>
    <automated>bash -lc 'if rg -n "cmdWriteBytes|writeValue|writeRaw|helper|XPC|Slider\\(|Stepper\\(|Button\\(\".*风扇|StatusBarManager.*(thermal|fan)|传感器浏览|raw SMC|Chart\\(|Alert\\(" MacStatus/MacStatus; then exit 1; fi'</automated>
    <automated>rg -n "xcodebuild test|BUILD SUCCEEDED|TEST SUCCEEDED|372|9999 RPM|100°C|999T|N/A|999\\.9W|networkMetricCardValue|temperatureValueColumn|fanRPMValueColumn|batteryPowerValueColumn|systemPowerValueColumn|networkProcessTrailingValue|cpuProcessTrailingValue|memoryProcessTrailingValue|x-position|width|no fan control|no SMC write|no status-bar fan|no raw sensor browsing" .planning/phases/12-popover-layout-stability/12-LAYOUT-VERIFICATION.md</automated>
  </verify>
  <acceptance_criteria>
    - Behavior assertion: short and extreme fixture widths both equal `372pt` within `0.5pt`.
    - Behavior assertion: same-visible-section short/extreme fixture heights are equal within `0.5pt`.
    - Behavior assertion: fixed value-width constants match UI-SPEC exactly and process-row trailing reservation is invariant under long process names.
    - Behavior assertion: `LayoutProbeFrameSnapshot` dictionaries for short and extreme fixtures contain and compare `origin.x` plus `width` for `networkMetricCardValue`, `temperatureValueColumn`, `fanRPMValueColumn`, `batteryPowerValueColumn`, `systemPowerValueColumn`, `networkProcessTrailingValue`, `cpuProcessTrailingValue`, and `memoryProcessTrailingValue`.
    - Behavior assertion: network metric card value block, temperature value column, fan RPM value column, battery/power value columns, and process trailing values keep the same x-position and width between short and extreme fixtures within `0.5pt`.
    - Evidence assertion: `12-LAYOUT-VERIFICATION.md` captures the test command result and source gates, including D-11 extreme values.
    - Evidence assertion: `12-LAYOUT-VERIFICATION.md` records the required geometry probe identifiers and short/extreme frame comparison results.
    - Scope assertion: verification proves no fan control, SMC write/helper/XPC, status-bar feature work, alerts, charts, network/API surface, secrets, or raw sensor browsing were added.
    - Test command: `xcodebuild test` succeeds.
  </acceptance_criteria>
  <done>Deterministic layout tests pass, verification evidence is recorded, and Phase 12 has an automated gate for fixed width, stable height, fixed value widths, required value-column x-position/width frame stability, and forbidden-scope absence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| deterministic fixtures -> XCTest runner | Fixture data is test/debug-only and must not become a user-facing debug mode. |
| UserDefaults -> layout tests | Local section visibility settings can hide sections; tests must force and restore all relevant popover section toggles. |
| layout tests -> hardware boundary | Tests must measure SwiftUI layout only and must not touch fan control, SMC writes, helper/XPC, network/API, secrets, or raw sensor browsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-R | Repudiation | Phase 12 verification evidence | mitigate | Record `xcodebuild test` and source gate summaries in `12-LAYOUT-VERIFICATION.md`. |
| T-12-03-DOS | Denial of Service | `DashboardView` and `ProcessMetricRow` layout | mitigate | XCTest measures short/extreme fixtures and long process rows. |
| T-12-03-T | Tampering | hardware/control boundary | mitigate | Source gates block fan control, SMC writes, helper/XPC, status-bar fan/thermal features, alerts, charts, and raw sensor browsing. |
| T-12-03-I | Information Integrity | deterministic fixture assertions | mitigate | Tests assert UI-SPEC constants and exact width/height tolerances instead of subjective visual inspection. |
| T-12-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned; XCTest is part of Xcode. |
</threat_model>

<verification>
Overall plan verification:
- `xcodebuild test -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -destination 'platform=macOS' -derivedDataPath build`
- `rg -n "DashboardLayoutStabilityTests|DashboardLayoutFixture|DashboardLayout\\.popoverWidth|StableValueWidth" MacStatus/MacStatusTests MacStatus/MacStatus/UI`
- `rg -n "xcodebuild test|TEST SUCCEEDED|372|9999 RPM|100°C|999T|N/A|no SMC write" .planning/phases/12-popover-layout-stability/12-LAYOUT-VERIFICATION.md`
</verification>

<success_criteria>
Plan 12-03 is complete when the deterministic short/extreme fixture tests pass through `xcodebuild test`, the verification artifact records the evidence, and source gates prove Phase 12 remained layout-only with no fan-control, SMC-write, status-bar, alert, chart, network/API, secret, or raw sensor browser surface.
</success_criteria>

<output>
Create `.planning/phases/12-popover-layout-stability/12-03-SUMMARY.md` when done.
</output>
