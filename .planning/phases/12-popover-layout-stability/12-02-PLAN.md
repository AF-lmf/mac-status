---
phase: 12-popover-layout-stability
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - MacStatus/MacStatus/UI/Views/ProcessListView.swift
  - MacStatus/MacStatus/UI/Fixtures/DashboardLayoutFixtures.swift
  - MacStatus/MacStatus.xcodeproj/project.pbxproj
autonomous: true
requirements: [LAYOUT-01, LAYOUT-02, LAYOUT-03, UAT-04]
requirements_addressed: [LAYOUT-01, LAYOUT-02, LAYOUT-03, UAT-04]
must_haves:
  truths:
    - "D-02/D-07: Top-N process left text and PID yield before network/CPU/memory trailing values move."
    - "D-10/D-11: deterministic DEBUG fixtures exist for short and extreme states covering required network, RPM, temperature, N/A, power, long text, and mixed availability cases."
    - "D-09: network rows keep current compact upload/download semantics and only reserve stable trailing space."
  artifacts:
    - path: "MacStatus/MacStatus/UI/Views/ProcessListView.swift"
      provides: "Stable network process trailing value block and left-yields process row"
      exports: ["NetworkTrafficValueBlock"]
    - path: "MacStatus/MacStatus/UI/Fixtures/DashboardLayoutFixtures.swift"
      provides: "DEBUG deterministic DashboardState short/extreme fixtures"
      exports: ["DashboardLayoutFixture"]
  key_links:
    - from: "MacStatus/MacStatus/UI/Views/ProcessListView.swift"
      to: "MacStatus/MacStatus/UI/Views/StableValueLayout.swift"
      via: "StableValueWidth and StableValueText"
      pattern: "StableValueWidth\\.(processNetworkRate|processNetworkPair)"
    - from: "MacStatus/MacStatus/UI/Fixtures/DashboardLayoutFixtures.swift"
      to: "MacStatus/MacStatus/UI/Views/DashboardView.swift"
      via: "DashboardState fixture population"
      pattern: "DashboardState|BatterySnapshot|ThermalSnapshot|FanSnapshot"
---

<objective>
Stabilize Top-N process rows and create deterministic short/extreme dashboard fixtures for Phase 12 layout verification.

Purpose: complete D-02, D-07, D-09, D-10, and D-11 surfaces that are outside the dashboard-owned rows handled by Plan 12-01.
Output: fixed-width process trailing rows plus DEBUG-only fixture data that later tests can measure without live hardware readings.
</objective>

<execution_context>
@/Users/halo/.codex/gsd-core/workflows/execute-plan.md
@/Users/halo/.codex/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-popover-layout-stability/12-CONTEXT.md
@.planning/phases/12-popover-layout-stability/12-RESEARCH.md
@.planning/phases/12-popover-layout-stability/12-PATTERNS.md
@.planning/phases/12-popover-layout-stability/12-UI-SPEC.md
@.planning/phases/12-popover-layout-stability/12-01-SUMMARY.md
@MacStatus/MacStatus/UI/Views/ProcessListView.swift
@MacStatus/MacStatus/UI/Views/DashboardView.swift
@MacStatus/MacStatus/Readers/BatteryReader.swift
@MacStatus/MacStatus/Readers/ThermalReader.swift
@MacStatus/MacStatus/Readers/FanReader.swift
@MacStatus/MacStatus/Readers/ProcessNetworkReader.swift
@MacStatus/MacStatus/Readers/ProcessResourceReader.swift
@MacStatus/MacStatus/Utils/ByteFormatting.swift
@MacStatus/MacStatus.xcodeproj/project.pbxproj
</context>

<artifacts_this_phase_produces>
This plan creates:
- File: `MacStatus/MacStatus/UI/Fixtures/DashboardLayoutFixtures.swift`
- Symbol: `struct NetworkTrafficValueBlock`
- Symbol: `enum DashboardLayoutFixture`
- Symbol: `DashboardLayoutFixture.Kind`
- Cases: `DashboardLayoutFixture.Kind.short`, `DashboardLayoutFixture.Kind.extreme`
- Function: `DashboardLayoutFixture.make(_:)`
- Function: `DashboardLayoutFixture.apply(_:to:)`
- Function: `DashboardLayoutFixture.applyShort(to:)`
- Function: `DashboardLayoutFixture.applyExtreme(to:)`
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Stabilize process row trailing value blocks</name>
  <files>MacStatus/MacStatus/UI/Views/ProcessListView.swift</files>
  <read_first>
    - `.planning/phases/12-popover-layout-stability/12-CONTEXT.md` decisions D-02, D-03, D-07, D-09.
    - `.planning/phases/12-popover-layout-stability/12-UI-SPEC.md` sections "Stable Value Widths", "Text Yield Rules", and "Section Contracts".
    - `.planning/phases/12-popover-layout-stability/12-PATTERNS.md` sections for `ProcessListView.swift` and "Stable Process Row".
    - `.planning/phases/12-popover-layout-stability/12-01-SUMMARY.md` for the exact `StableValueLayout.swift` helper names created in Plan 12-01.
    - `MacStatus/MacStatus/UI/Views/ProcessListView.swift`.
    - `MacStatus/MacStatus/Utils/ByteFormatting.swift`.
  </read_first>
  <action>Update `ProcessMetricRow` so the process name and optional PID are grouped in a left `HStack` with `frame(maxWidth: .infinity, alignment: .leading)`, one-line tail truncation, and lower layout priority. Add a `trailingWidth: CGFloat` parameter to `ProcessMetricRow` and wrap `trailing()` in `frame(width: trailingWidth, alignment: .trailing)` with higher layout priority per D-07. Create `NetworkTrafficValueBlock` for network process rows: keep the current upload/download values from `ByteFormatting.format(...)+"/s"` and SF Symbol direction semantics, but render them as two fixed `StableValueWidth.processNetworkRate` cells with a total `StableValueWidth.processNetworkPair` trailing reservation per D-02/D-03/D-09. In `ProcessListView`, pass `trailingWidth: StableValueWidth.processNetworkPair`. If touching loading/empty copy, replace English strings with UI-SPEC copy: `采样中...`, `暂无活跃网络进程`, and `等待新的网络进程活动`; keep states inline and non-modal. Do not change `ByteFormatting`, process sampling, status-bar output, fan controls, alerts, charts, or raw sensor browsing.</action>
  <verify>
    <automated>xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build</automated>
    <automated>rg -n "NetworkTrafficValueBlock|trailingWidth|StableValueWidth\\.(processNetworkRate|processNetworkPair)|lineLimit\\(1\\)|truncationMode\\(\\.tail\\)|layoutPriority" MacStatus/MacStatus/UI/Views/ProcessListView.swift</automated>
    <automated>bash -lc 'if rg -n "No active network processes|Sampling\\.\\.\\." MacStatus/MacStatus/UI/Views/ProcessListView.swift; then exit 1; fi'</automated>
    <automated>bash -lc 'if git diff -- MacStatus/MacStatus/Utils/ByteFormatting.swift | rg -n "."; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: process row trailing content has an explicit `trailingWidth` and does not rely on `Spacer()` alone.
    - Source assertion: network upload/download values keep current `ByteFormatting.format(...)+"/s"` meaning and reserve `148pt` total trailing width.
    - Behavior assertion: long process names and PID text truncate in the left region before upload/download, CPU, or memory trailing values move.
    - Scope assertion: this task does not edit `ByteFormatting.swift` or introduce new data collection.
    - Test command: Debug `xcodebuild` succeeds and source gates pass.
  </acceptance_criteria>
  <done>Network Top Processes rows use a fixed trailing block, and the shared `ProcessMetricRow` can be consumed by CPU/memory process sections without letting names or PIDs compress values.</done>
</task>

<task type="auto">
  <name>Task 2: Add DEBUG deterministic dashboard layout fixtures</name>
  <files>MacStatus/MacStatus/UI/Fixtures/DashboardLayoutFixtures.swift, MacStatus/MacStatus.xcodeproj/project.pbxproj</files>
  <read_first>
    - `.planning/phases/12-popover-layout-stability/12-CONTEXT.md` decisions D-10 and D-11.
    - `.planning/phases/12-popover-layout-stability/12-UI-SPEC.md` section "Deterministic Verification Contract".
    - `.planning/phases/12-popover-layout-stability/12-PATTERNS.md` section for `DashboardLayoutFixtures.swift`.
    - `MacStatus/MacStatus/UI/Views/DashboardView.swift` for `DashboardState` fields.
    - `MacStatus/MacStatus/Readers/BatteryReader.swift` for `BatterySnapshot`.
    - `MacStatus/MacStatus/Readers/ThermalReader.swift` for `ThermalSnapshot` and `SystemThermalState`.
    - `MacStatus/MacStatus/Readers/FanReader.swift` for `FanSnapshot`, `FanReading`, and `FanCapabilities`.
    - `MacStatus/MacStatus/Readers/ProcessNetworkReader.swift` and `MacStatus/MacStatus/Readers/ProcessResourceReader.swift` for process fixture types.
    - `MacStatus/MacStatus.xcodeproj/project.pbxproj`.
  </read_first>
  <action>Create `MacStatus/MacStatus/UI/Fixtures/` and `DashboardLayoutFixtures.swift`, register it in the app target, and wrap fixture declarations in `#if DEBUG` so they are not part of Release behavior. Define `@MainActor enum DashboardLayoutFixture` with `enum Kind { case short, extreme }`, `static func make(_ kind: Kind) -> DashboardState`, and `static func apply(_ kind: Kind, to state: DashboardState)`. The short fixture must include same-visible-section data with short network rates (`↑1K` and `↓2K` in the current two-line network card convention), one `999 RPM` fan, nominal `35°C`, short process names, mixed available/unavailable rows, a battery snapshot, and CPU/memory/network/GPU sample arrays. The extreme fixture must keep the same section visibility and include large network text using the current formatter equivalent of `999T`, `9999 RPM`, `100°C`, `N/A`, `充电 999.9W` or `耗电 999.9W`, `99小时59分`, `100%（999 次循环）`, long process names, long fan/sensor labels used only inside the fixture, long fan range/target/status captions through existing fields, and mixed battery/thermal/fan/process availability per D-10/D-11. Do not reference `DashboardLayoutFixture` from live app entry points, collectors, readers, `PopoverManager`, `StatusBarManager`, settings UI, or status-bar formatting.</action>
  <verify>
    <automated>xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build</automated>
    <automated>rg -n "DashboardLayoutFixture|enum Kind|case short|case extreme|make\\(|apply\\(|9999|100°C|999T|999\\.9W|99小时59分|100%（999 次循环）|N/A|ProcessNetworkUsage|ProcessResourceUsage|BatterySnapshot|ThermalSnapshot|FanSnapshot" MacStatus/MacStatus/UI/Fixtures/DashboardLayoutFixtures.swift</automated>
    <automated>rg -n "DashboardLayoutFixtures.swift" MacStatus/MacStatus.xcodeproj/project.pbxproj</automated>
    <automated>bash -lc 'if rg -n "DashboardLayoutFixture" MacStatus/MacStatus/App MacStatus/MacStatus/Collectors MacStatus/MacStatus/UI/StatusBarManager.swift MacStatus/MacStatus/UI/PopoverManager.swift MacStatus/MacStatus/UI/Views/SettingsView.swift; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: fixture declarations are guarded by `#if DEBUG`.
    - Source assertion: both `.short` and `.extreme` fixtures populate the same visible section set: metric grid, battery, temperature/fan, network Top Processes, CPU Top 5, and Memory Top 5.
    - Behavior assertion: fixture values cover every D-11 required extreme: short/large network rates, `9999 RPM`, `100°C`, `N/A`, large power values, long process names, long sensor/fan labels, and mixed availability.
    - Scope assertion: fixtures are not referenced by live collectors, readers, status-bar code, popover manager, settings UI, or app entry points.
    - Test command: Debug `xcodebuild` succeeds and fixture grep gates pass.
  </acceptance_criteria>
  <done>DEBUG-only deterministic short/extreme dashboard fixtures exist, build in Debug, stay out of live UI paths, and are ready for Plan 12-03 layout measurement tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| process text -> process row layout | Untrusted-length process names and PID strings enter SwiftUI rows and must yield before trailing values. |
| deterministic fixtures -> runtime app | Fixture data must remain DEBUG/test-only and must not be reachable from live collectors, popover entry points, settings, or status-bar code. |
| layout phase -> hardware boundary | This plan must not add hardware write paths, fan control, helper/XPC, network/API surfaces, secrets, alerts, charts, or raw sensor browsing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-02-DOS | Denial of Service | `ProcessMetricRow` | mitigate | Reserve fixed trailing width and force left text truncation. |
| T-12-02-I | Information Integrity | `NetworkTrafficValueBlock` and process trailing metrics | mitigate | Use fixed monospaced cells and keep current formatter semantics. |
| T-12-02-T | Tampering | `DashboardLayoutFixture` reachability | mitigate | Guard fixture declarations with `#if DEBUG` and grep that live app paths do not reference the fixture type. |
| T-12-02-SC | Tampering | npm/pip/cargo installs | accept | No package-manager installs are planned; package legitimacy audit says none. |
</threat_model>

<verification>
Overall plan verification:
- `xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build`
- `rg -n "NetworkTrafficValueBlock|DashboardLayoutFixture|9999|100°C|999T" MacStatus/MacStatus/UI`
- `bash -lc 'if rg -n "DashboardLayoutFixture" MacStatus/MacStatus/App MacStatus/MacStatus/Collectors MacStatus/MacStatus/UI/StatusBarManager.swift MacStatus/MacStatus/UI/PopoverManager.swift MacStatus/MacStatus/UI/Views/SettingsView.swift; then exit 1; fi'`
</verification>

<success_criteria>
Plan 12-02 is complete when process rows have fixed trailing metric regions, deterministic short/extreme dashboard fixtures exist behind DEBUG only, and no fixture or layout helper becomes a user-facing feature or live data path.
</success_criteria>

<output>
Create `.planning/phases/12-popover-layout-stability/12-02-SUMMARY.md` when done.
</output>
