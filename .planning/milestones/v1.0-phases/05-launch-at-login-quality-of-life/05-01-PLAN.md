---
phase: 05-launch-at-login-quality-of-life
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - MacStatus/MacStatus/App/AppDelegate.swift
  - MacStatus/MacStatus/UI/StatusBarManager.swift
  - MacStatus/MacStatus.xcodeproj/project.pbxproj
autonomous: true
requirements: [LIFE-02, LIFE-04]

must_haves:
  truths:
    - "LIFE-02 is implemented: the app registers itself for launch at login through `SMAppService.mainApp.register()`"
    - "LIFE-04 is implemented: right-clicking the status bar item shows a native menu with `Quit MacStatus`"
    - "Phase 5 success criterion 1 is implemented: launch-at-login registration happens on app launch"
    - "Phase 5 success criterion 2 is implemented: the status item has a right-click menu with a Quit option"
    - "Phase 5 success criterion 3 is implemented: CPU, network, memory, and GPU readers stop before sleep and restart after wake"
    - "Phase 5 success criterion 4 is protected: no new high-frequency polling is introduced and existing reader intervals are reused"
    - "D-01 is implemented: launch at login uses `SMAppService.mainApp.register()`"
    - "D-02 is implemented: launch at login is enabled by default during app launch"
    - "D-03 is implemented: already-enabled registration is a no-op and registration failures are logged without stopping monitoring"
    - "D-04 is implemented: ServiceManagement is imported/linked in the app target"
    - "D-05 is implemented: right-clicking the existing combined status item opens an `NSMenu`"
    - "D-06 is implemented: the v1 menu contains only `Quit MacStatus`"
    - "D-07 is implemented: left click remains no-op"
    - "D-08 is implemented: `Quit MacStatus` calls `NSApp.terminate(nil)`"
    - "D-09 is implemented: `AppDelegate` observes `NSWorkspace.willSleepNotification` and `NSWorkspace.didWakeNotification`"
    - "D-10 is implemented: all existing readers stop before sleep and restart after wake"
    - "D-11 is implemented: sleep/wake does not blank existing displayed values"
    - "D-12 is implemented: observers are removed and readers are stopped during termination"
    - "D-13 is implemented: existing reader intervals and timer model are reused"
    - "D-14 is implemented: verification includes build, source assertions, smoke launch, and notes any manual 30+ minute soak gap"
  artifacts:
    - path: "MacStatus/MacStatus/App/AppDelegate.swift"
      provides: "launch-at-login registration and sleep/wake reader lifecycle recovery"
      contains: "SMAppService.mainApp"
    - path: "MacStatus/MacStatus/UI/StatusBarManager.swift"
      provides: "right-click native quit menu on the existing combined status item"
      contains: "Quit MacStatus"
  key_links:
    - from: "NSWorkspace.didWakeNotification"
      to: "TimerReader.start"
      via: "`AppDelegate.startReaders()`"
      pattern: "startReaders"
    - from: "NSWorkspace.willSleepNotification"
      to: "TimerReader.stop"
      via: "`AppDelegate.stopReaders()`"
      pattern: "stopReaders"
    - from: "NSStatusBarButton rightMouseUp"
      to: "NSApp.terminate"
      via: "`StatusBarManager` menu action"
      pattern: "Quit MacStatus"
---

<objective>
Complete the v1 quality-of-life lifecycle slice: enable launch at login by default, add a minimal right-click Quit menu to the existing combined menu bar item, and make all current readers recover cleanly after macOS sleep/wake without changing the metric display format.
</objective>

<execution_context>
@/Users/halo/.codex/get-shit-done/workflows/execute-plan.md
@/Users/halo/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-launch-at-login-quality-of-life/05-CONTEXT.md
@.planning/phases/05-launch-at-login-quality-of-life/05-RESEARCH.md
@.planning/phases/05-launch-at-login-quality-of-life/05-PATTERNS.md
@.planning/phases/05-launch-at-login-quality-of-life/05-UI-SPEC.md
@MacStatus/MacStatus/App/AppDelegate.swift
@MacStatus/MacStatus/UI/StatusBarManager.swift
@MacStatus/MacStatus/Readers/TimerReader.swift
@MacStatus/MacStatus.xcodeproj/project.pbxproj
</context>

<threat_model>
| Threat | Severity | Mitigation |
|--------|----------|------------|
| Login item registration throws in development builds | medium | Catch and log the error; do not block monitoring startup |
| Right-click menu changes left-click behavior | medium | Use `sendAction(on: [.rightMouseUp])` and avoid assigning `statusItem.menu` |
| Wake recovery creates duplicate timers | high | Use existing `TimerReader.start()`, which calls `stop()` before scheduling |
| Sleep/wake observers remain registered after termination | medium | Remove observers in `applicationWillTerminate` before niling readers |
| Phase 4 display format regresses | high | Do not modify combined title construction except button target/action setup |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add launch-at-login registration and reader lifecycle helpers</name>
  <files>MacStatus/MacStatus/App/AppDelegate.swift, MacStatus/MacStatus.xcodeproj/project.pbxproj</files>
  <read_first>
    <file>MacStatus/MacStatus/App/AppDelegate.swift</file>
    <file>MacStatus/MacStatus/Readers/TimerReader.swift</file>
    <file>MacStatus/MacStatus.xcodeproj/project.pbxproj</file>
    <file>.planning/phases/05-launch-at-login-quality-of-life/05-CONTEXT.md</file>
  </read_first>
  <action>
Modify `AppDelegate.swift`.

Add `import ServiceManagement`.

Add `configureLaunchAtLogin()`:
- Read `SMAppService.mainApp`.
- Return immediately when `status == .enabled`.
- Otherwise call `try service.register()`.
- Catch and log failures with `print(...)`.

Refactor launch reader setup into:
- `configureReaders()`
- `startReaders()`
- `stopReaders()`

`applicationDidFinishLaunching(_:)` should:
- create `StatusBarManager`
- call `configureLaunchAtLogin()`
- call `configureReaders()`
- call `registerSleepWakeObservers()`
- call `startReaders()`

Update the Xcode project to link `ServiceManagement.framework` if the build requires explicit framework membership.
  </action>
  <verify>
    <automated>`grep -n "import ServiceManagement" MacStatus/MacStatus/App/AppDelegate.swift` prints a match.</automated>
    <automated>`grep -n "SMAppService.mainApp" MacStatus/MacStatus/App/AppDelegate.swift` prints a match.</automated>
    <automated>`grep -n "startReaders" MacStatus/MacStatus/App/AppDelegate.swift` prints matches.</automated>
    <automated>`grep -n "stopReaders" MacStatus/MacStatus/App/AppDelegate.swift` prints matches.</automated>
  </verify>
  <acceptance_criteria>
- Launch-at-login registration is attempted once during app launch unless already enabled.
- Registration failure does not stop reader setup.
- Existing reader callback behavior remains unchanged.
- Reader start/stop logic is centralized and reusable.
  </acceptance_criteria>
  <done>
Launch startup owns login item registration and has reusable reader lifecycle helpers for wake recovery and termination.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add sleep/wake observers and right-click Quit menu</name>
  <files>MacStatus/MacStatus/App/AppDelegate.swift, MacStatus/MacStatus/UI/StatusBarManager.swift</files>
  <read_first>
    <file>MacStatus/MacStatus/App/AppDelegate.swift</file>
    <file>MacStatus/MacStatus/UI/StatusBarManager.swift</file>
    <file>.planning/phases/05-launch-at-login-quality-of-life/05-UI-SPEC.md</file>
  </read_first>
  <action>
Modify `AppDelegate.swift`.

Add:
- `registerSleepWakeObservers()`
- `unregisterSleepWakeObservers()`
- `@objc private func applicationWillSleep(_:)`
- `@objc private func applicationDidWake(_:)`

Wire:
- `NSWorkspace.willSleepNotification` -> `stopReaders()`
- `NSWorkspace.didWakeNotification` -> `startReaders()`
- `applicationWillTerminate(_:)` -> unregister observers, stop readers, nil readers/status manager

Modify `StatusBarManager.swift`.

Make `StatusBarManager` inherit from `NSObject` and adjust `init` to call `super.init()`.

Add a lazy native menu containing only:
- `Quit MacStatus`

Update `configureStatusButton(_:)` so the visible button:
- keeps existing clipping/single-line settings
- sets `target = self`
- sets `action = #selector(showStatusMenu(_:))`
- sends action on `.rightMouseUp`

Add selectors:
- `showStatusMenu(_:)` to pop up the menu
- `quitMacStatus(_:)` to call `NSApp.terminate(nil)`
  </action>
  <verify>
    <automated>`grep -n "willSleepNotification" MacStatus/MacStatus/App/AppDelegate.swift` prints a match.</automated>
    <automated>`grep -n "didWakeNotification" MacStatus/MacStatus/App/AppDelegate.swift` prints a match.</automated>
    <automated>`grep -n "Quit MacStatus" MacStatus/MacStatus/UI/StatusBarManager.swift` prints a match.</automated>
    <automated>`grep -n "rightMouseUp" MacStatus/MacStatus/UI/StatusBarManager.swift` prints a match.</automated>
    <automated>`grep -n "NSApp.terminate" MacStatus/MacStatus/UI/StatusBarManager.swift` prints a match.</automated>
  </verify>
  <acceptance_criteria>
- Sleep notification stops CPU, network, memory, and GPU readers.
- Wake notification restarts CPU, network, memory, and GPU readers.
- Termination unregisters observers and stops readers.
- Right-clicking the visible status item opens a menu with `Quit MacStatus`.
- Left-click does not open the menu.
  </acceptance_criteria>
  <done>
The app has a minimal right-click quit menu and reader lifecycle recovery across sleep/wake.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Build, source-check, smoke launch, and record verification limits</name>
  <files>MacStatus/MacStatus/App/AppDelegate.swift, MacStatus/MacStatus/UI/StatusBarManager.swift, MacStatus/MacStatus.xcodeproj/project.pbxproj</files>
  <read_first>
    <file>MacStatus/MacStatus/App/AppDelegate.swift</file>
    <file>MacStatus/MacStatus/UI/StatusBarManager.swift</file>
    <file>MacStatus/MacStatus.xcodeproj/project.pbxproj</file>
  </read_first>
  <action>
Run:

`xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build`

Run source assertions:
- `grep -n "SMAppService.mainApp" MacStatus/MacStatus/App/AppDelegate.swift`
- `grep -n "willSleepNotification" MacStatus/MacStatus/App/AppDelegate.swift`
- `grep -n "didWakeNotification" MacStatus/MacStatus/App/AppDelegate.swift`
- `grep -n "Quit MacStatus" MacStatus/MacStatus/UI/StatusBarManager.swift`
- `grep -n "rightMouseUp" MacStatus/MacStatus/UI/StatusBarManager.swift`
- `grep -n "NSApp.terminate" MacStatus/MacStatus/UI/StatusBarManager.swift`

Relaunch the Debug app from `build/Build/Products/Debug/MacStatus.app`.

If the 30+ minute CPU soak is not run in-session, record that as a manual verification limit in `05-VERIFICATION.md` rather than claiming it was performed.
  </action>
  <verify>
    <automated>`xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build` exits 0.</automated>
    <automated>All source assertion commands print at least one match.</automated>
    <automated>`pgrep -fl "MacStatus.app/Contents/MacOS/MacStatus"` prints a running Debug app process after relaunch.</automated>
  </verify>
  <acceptance_criteria>
- Debug build succeeds.
- Source assertions confirm launch-at-login, sleep/wake, and right-click quit hooks.
- Debug app launches and stays running.
- Verification honestly records whether manual sleep/wake, right-click, and 30+ minute soak checks were performed.
  </acceptance_criteria>
  <done>
Phase 5 implementation builds, launches, and has clear automated plus manual verification evidence.
  </done>
</task>

</tasks>

<verification>
Run:

```bash
xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build build
```

Then verify:

```bash
grep -n "SMAppService.mainApp" MacStatus/MacStatus/App/AppDelegate.swift
grep -n "willSleepNotification" MacStatus/MacStatus/App/AppDelegate.swift
grep -n "didWakeNotification" MacStatus/MacStatus/App/AppDelegate.swift
grep -n "Quit MacStatus" MacStatus/MacStatus/UI/StatusBarManager.swift
grep -n "rightMouseUp" MacStatus/MacStatus/UI/StatusBarManager.swift
grep -n "NSApp.terminate" MacStatus/MacStatus/UI/StatusBarManager.swift
pgrep -fl "MacStatus.app/Contents/MacOS/MacStatus"
```
</verification>

<success_criteria>
- Launch at login uses `SMAppService.mainApp.register()`.
- Right-clicking the status item opens a native menu with `Quit MacStatus`.
- Sleep stops all existing readers and wake restarts all existing readers.
- The existing combined metric display format remains unchanged.
- Debug build succeeds and the app launches.
- Manual-only verification gaps are explicitly recorded instead of hidden.
</success_criteria>
