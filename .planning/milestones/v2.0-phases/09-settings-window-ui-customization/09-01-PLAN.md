---
phase: "09-settings-window-ui-customization"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - MacStatus/MacStatus/Utils/SettingsManager.swift
autonomous: true
requirements:
  - SET-02
  - SET-06
must_haves:
  truths:
    - "SettingsManager.Keys enum 包含 showBatterySection 和 showProcessSection 两个新条目"
    - "showBatterySection/showProcessSection 各有 @ObservationIgnored 私有 backing 变量，初始值 true"
    - "两个属性的计算 get/set 写 UserDefaults + postChange，与 showIcons 形状完全一致"
    - "loadAll() 中两个新 key 使用 object(forKey:) == nil 判断，nil 时赋 true（防止 UserDefaults.bool 返回 false 的默认值陷阱）"
    - "不触碰 migrateToV1() 和 schemaVersion（两个新键通过 loadAll nil-check 处理，无需迁移）"
    - "xcodebuild 编译成功，exit 0"
  artifacts:
    - path: "MacStatus/MacStatus/Utils/SettingsManager.swift"
      provides: "showBatterySection + showProcessSection 两个 bool 属性"
      contains: "showBatterySection"
  key_links:
    - from: "SettingsManager.showBatterySection setter"
      to: "postChange(keys:)"
      via: "NotificationCenter .settingsDidChange"
      pattern: "postChange\\(keys: \\[Keys\\.showBatterySection"
---

<objective>
Plan 01：为 SettingsManager 添加 showBatterySection 和 showProcessSection 两个新 bool 键。

Purpose: 这是 Phase 9 的纯模型层变更，独立于任何视图改造，可单独构建验证。两个新键控制弹窗的电池区块和进程区块是否显示（默认 true，即默认显示），供 Plan 02 的 SettingsView Toggles 绑定，以及 Plan 03 的 DashboardView 门控使用。

Output: SettingsManager.swift 中新增 showBatterySection、showProcessSection 两个属性（Keys 常量 + backing 变量 + computed get/set + loadAll 初始化）。无新文件，无 pbxproj 变更。
</objective>

<execution_context>
@/Users/halo/work/my-project/mac-status/.claude/gsd-core/workflows/execute-plan.md
@/Users/halo/work/my-project/mac-status/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/halo/work/my-project/mac-status/.planning/ROADMAP.md
@/Users/halo/work/my-project/mac-status/.planning/phases/09-settings-window-ui-customization/09-CONTEXT.md
@/Users/halo/work/my-project/mac-status/.planning/phases/09-settings-window-ui-customization/09-RESEARCH.md
@/Users/halo/work/my-project/mac-status/.planning/phases/09-settings-window-ui-customization/09-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1：SettingsManager 新增 showBatterySection / showProcessSection</name>
  <files>MacStatus/MacStatus/Utils/SettingsManager.swift</files>
  <read_first>
    - MacStatus/MacStatus/Utils/SettingsManager.swift — 完整读取，重点关注：
      - Keys enum（第 64–82 行）：在 launchAtLogin 之后新增两条 static let
      - Backing Storage 区块（第 95–107 行）：在 _launchAtLogin 之后新增两个 @ObservationIgnored bool 变量，初始值 true
      - showIcons 属性（第 142–149 行）：即 Bool computed get/set 的完整模式——新两个属性照此形状
      - launchAtLogin 属性（第 217–235 行）：注意其 setter 带 SMAppService 副作用——showBattery/showProcess 不需要副作用，模式更简单（照 showIcons）
      - loadAll() 函数（第 338–406 行）：在 _launchAtLogin 赋值（第 384 行）之后新增两个 nil-check 块
    - 09-PATTERNS.md "SettingsManager.swift — New bool keys" 节：完整的四段代码（Keys、backing、computed、loadAll）可直接照抄
    - 09-RESEARCH.md Q4 节："新增模式（完全复用既有 @ObservationIgnored + 计算 getter/setter 模式）" 和 "迁移策略：不新增 schemaVersion" — 验证设计正确性
  </read_first>
  <action>
    在 SettingsManager.swift 的三处位置添加代码，不改动任何现有代码：

    位置 1 — Keys enum 末尾（在 launchAtLogin 条目之后）：
    新增 showBatterySection = "showBatterySection" 和 showProcessSection = "showProcessSection" 两个 static let。

    位置 2 — Backing Storage 区块（在 _launchAtLogin 之后）：
    新增 @ObservationIgnored private var _showBatterySection: Bool = true 和 @ObservationIgnored private var _showProcessSection: Bool = true。初始值 true 是为代码阅读友好；实际运行值由 loadAll() 决定。

    位置 3 — Public Properties 区块（在 launchAtLogin 属性之后，customThresholds 之前）：
    新增 showBatterySection 和 showProcessSection 两个计算属性，形状与 showIcons 完全一致（get 返回 backing 变量，set 写 backing + defaults.set + postChange），per D-03（从 CONTEXT.md 决策：showBatterySection/showProcessSection bool 键，默认 true，computed getter/setter + backing + postChange，迁移补默认）。

    位置 4 — loadAll() 函数内（在 _launchAtLogin = defaults.bool(forKey: Keys.launchAtLogin) 之后）：
    为两个新 key 各添加一个 nil-check 块：
    - if defaults.object(forKey: Keys.showBatterySection) == nil { _showBatterySection = true } else { _showBatterySection = defaults.bool(forKey: Keys.showBatterySection) }
    - if defaults.object(forKey: Keys.showProcessSection) == nil { _showProcessSection = true } else { _showProcessSection = defaults.bool(forKey: Keys.showProcessSection) }

    约束（强制）：
    - 不修改 migrateToV1() 和 runMigrations()（新键通过 loadAll nil-check 处理，与 customThresholds/customColors 同模式）
    - 不 bump schemaVersion（研究已确认此策略正确）
    - 两个属性的 postChange 调用仍然传递各自的 key 字符串，与其他属性完全一致
    - 不在文件中增加 import（无需新依赖）
  </action>
  <verify>
    <automated>cd /Users/halo/work/my-project/mac-status && xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build.noindex build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    源码断言（用 grep 验证，任一失败则本任务未完成）：
    - grep -c "showBatterySection" MacStatus/MacStatus/Utils/SettingsManager.swift 返回 >= 6（Keys + backing + get + set + defaults.set + postChange + loadAll 共约 8 处）
    - grep -c "showProcessSection" MacStatus/MacStatus/Utils/SettingsManager.swift 返回 >= 6
    - grep -c "_showBatterySection: Bool = true" MacStatus/MacStatus/Utils/SettingsManager.swift 返回 1
    - grep -c "_showProcessSection: Bool = true" MacStatus/MacStatus/Utils/SettingsManager.swift 返回 1
    - grep -v "^//" MacStatus/MacStatus/Utils/SettingsManager.swift | grep -c "object(forKey: Keys.showBatterySection)" 返回 1
    - grep -v "^//" MacStatus/MacStatus/Utils/SettingsManager.swift | grep -c "object(forKey: Keys.showProcessSection)" 返回 1
    - grep -c "migrateToV1" MacStatus/MacStatus/Utils/SettingsManager.swift 的结果与改动前相同（未修改迁移函数）
    构建断言：xcodebuild exit 0
    行为验证（构建后的逻辑验证，不需要运行应用）：
    - loadAll nil-check 保证首次安装时 showBatterySection/showProcessSection 默认 true（而非 UserDefaults.bool 的 false 零值）
  </acceptance_criteria>
  <done>SettingsManager.swift 包含 showBatterySection 和 showProcessSection 两个完整属性（Keys + backing + computed get/set + loadAll）；不触碰 migrateToV1；xcodebuild exit 0</done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| SettingsManager setter → UserDefaults | 用户通过 UI 控件写入布尔偏好；值域已确定为 Bool，无注入风险 |

## STRIDE 威胁登记

| Threat ID | 类别 | 组件 | 处置 | 缓解措施 |
|-----------|------|------|------|---------|
| T-09-01-S | Spoofing | showBatterySection/showProcessSection setters | accept | 仅本进程 main actor 可写；Bool 无格式注入面 |
| T-09-01-T | Tampering | UserDefaults 存储（`defaults write` CLI） | accept | 非法 Bool 写入会被 `defaults.bool(forKey:)` 安全解析为 false（用户只是看不到区块，不会崩溃）；设置窗口 Toggle 可立即覆盖 |
| T-09-01-SC | Tampering | npm/pip/cargo installs | accept | 本阶段零外部包安装，无供应链威胁面 |
</threat_model>

<verification>
构建：cd /Users/halo/work/my-project/mac-status && xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build.noindex build 2>&1 | tail -5

源码断言总览：
- grep -c "showBatterySection" MacStatus/MacStatus/Utils/SettingsManager.swift >= 6
- grep -c "showProcessSection" MacStatus/MacStatus/Utils/SettingsManager.swift >= 6
- migrateToV1 引用数量不变（未改动迁移）
</verification>

<success_criteria>
- SettingsManager.swift 包含 showBatterySection 和 showProcessSection 两个完整属性
- loadAll() 使用 object(forKey:) == nil 为两个新键提供 true 默认值（防止 UserDefaults.bool 陷阱）
- migrateToV1() 和 schemaVersion 未改动
- xcodebuild 编译成功，exit 0
- Plan 02 和 Plan 03 可在此基础上绑定 $settings.showBatterySection / $settings.showProcessSection
</success_criteria>

<output>
完成后创建 .planning/phases/09-settings-window-ui-customization/09-01-SUMMARY.md
</output>
