---
phase: "07-battery-power"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - MacStatus/MacStatus/Readers/BatteryReader.swift
autonomous: true
requirements:
  - BATT-01
  - BATT-02
  - BATT-03
  - BATT-04
  - BATT-05

must_haves:
  truths:
    - "BatteryReader.readValue() 在有电池的 Mac 上返回非 nil 的 BatterySnapshot，包含 chargePercent、isCharging、isOnAC"
    - "BatteryReader.readValue() 在无 AppleSmartBattery 服务（台式机）时返回 nil"
    - "每一个 AppleSmartBattery IORegistry 键均以 as? 类型转换（probe-and-nil），任一缺失则对应字段为 nil，绝不强解包"
    - "IOObjectRelease 通过 defer 块调用，在 guard service != IO_OBJECT_NULL 检查之后立即设置，防止 Mach 端口泄漏"
    - "IOPSGetPowerSourceDescription 使用 takeUnretainedValue()，IOPSCopyPowerSourcesInfo / IOPSCopyPowerSourcesList 使用 takeRetainedValue()"
    - "健康度公式为 AppleRawMaxCapacity / DesignCapacity × 100，上限 100%，不使用 MaxCapacity（Apple Silicon 上为百分比非 mAh）"
    - "Watts 幅值来自 abs(Amperage × Voltage / 1_000_000)，符号来自 kIOPSIsChargingKey（非 Amperage 本身符号），|watts| < 0.1 时为 nil（显示 —）"
    - "充电三态逻辑：isCharging→充电中；!isCharging && state==AC Power→已充满；state==Battery Power→使用电池（不依赖 kIOPSIsChargedKey）"
    - "时间哨兵处理：-1→计算中；0→— ；>0→格式化分钟数；nil 字段→计算中（post-wake 跳过）"
    - "BatteryReader.setup() 注册 NSWorkspace.didWakeNotification 观察器，唤醒后 postWakeSkipCount=3，readValue() 期间递减并将时间字段强制为 nil（显示 计算中）"
    - "deinit 移除 wakeObserver，防止通知中心保留循环"
    - "BatterySnapshot: Sendable, Equatable 所有字段均为值类型（Int、Bool、Double、Optional），无 CF 类型跨越 actor 边界"
    - "BatteryReader 是 final class，不继承 TimerReader（MetricCollector 通过 readValue() 同步调用驱动节奏）"
  artifacts:
    - path: "MacStatus/MacStatus/Readers/BatteryReader.swift"
      provides: "BatterySnapshot struct + BatteryReader class"
      exports: ["BatterySnapshot", "BatteryReader"]
      contains: "struct BatterySnapshot"
  key_links:
    - from: "BatteryReader.readValue()"
      to: "IOPSCopyPowerSourcesInfo / IOPSGetPowerSourceDescription"
      via: "Layer 1: Power Sources API"
      pattern: "IOPSCopyPowerSourcesInfo"
    - from: "BatteryReader.readValue()"
      to: "IOServiceGetMatchingService(AppleSmartBattery)"
      via: "Layer 2: AppleSmartBattery IORegistry"
      pattern: "IOServiceGetMatchingService.*AppleSmartBattery"
    - from: "BatteryReader.setup()"
      to: "NSWorkspace.didWakeNotification"
      via: "wakeObserver"
      pattern: "didWakeNotification"
---

<objective>
创建 `MacStatus/MacStatus/Readers/BatteryReader.swift`，包含：

1. `BatterySnapshot: Sendable, Equatable` struct — 所有 8 个字段均为值类型 + Optional（chargePercent、isCharging、isOnAC、timeToEmptyMinutes、timeToFullMinutes、watts、healthPercent、cycleCount）
2. `BatteryReader` final class — 同步 `readValue() -> BatterySnapshot?`（nil = 无电池 = 台式机），双层 IOKit 读取（Power Sources + AppleSmartBattery），probe-and-nil 防御，IOObjectRelease defer，sleep/wake 观察器

Purpose: 这是本阶段风险最高、最独立的部分——纯新文件，封装所有 IOKit 调用，零 UI 依赖，可单独编译验证。Plan 02 在此类型上构建集成层。

Output: `BatteryReader.swift`（含 BatterySnapshot + BatteryReader），xcodebuild 编译成功
</objective>

<execution_context>
@/Users/halo/work/my-project/mac-status/.claude/gsd-core/workflows/execute-plan.md
@/Users/halo/work/my-project/mac-status/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/halo/work/my-project/mac-status/.planning/ROADMAP.md
@/Users/halo/work/my-project/mac-status/.planning/phases/07-battery-power/07-RESEARCH.md
@/Users/halo/work/my-project/mac-status/.planning/phases/07-battery-power/07-PATTERNS.md
@/Users/halo/work/my-project/mac-status/MacStatus/MacStatus/Readers/GPUReader.swift
@/Users/halo/work/my-project/mac-status/MacStatus/MacStatus/Readers/NetworkReader.swift
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 创建 BatterySnapshot struct 与 BatteryReader class</name>
  <files>MacStatus/MacStatus/Readers/BatteryReader.swift</files>

  <read_first>
    - RESEARCH.md §"Complete BatterySnapshot Struct (Swift 6)" — copy-paste-ready struct 定义（所有字段均为值类型 + Optional）
    - RESEARCH.md §"BatteryReader.readValue() Core Pattern" — readValue() 的完整两层读取核心（Power Sources + AppleSmartBattery）
    - RESEARCH.md §"BatteryReader Wake Observer" — setup()/deinit wake observer 模式
    - RESEARCH.md §"API 1: Power Sources — Exact Keys" — 精确键名/类型/哨兵（kIOPSCurrentCapacityKey、kIOPSIsChargingKey、kIOPSPowerSourceStateKey、kIOPSTimeToEmptyKey、kIOPSTimeToFullChargeKey、kIOPSTypeKey、kIOPSIsPresentKey、kIOPSACPowerValue、kIOPSBatteryPowerValue、kIOPSInternalBatteryType）
    - RESEARCH.md §"API 2: AppleSmartBattery IORegistry" — 键名/类型/单位（"Amperage" as? Int mA、"Voltage" as? Int mV、"AppleRawMaxCapacity" as? Int mAh、"DesignCapacity" as? Int mAh、"CycleCount" as? Int）
    - RESEARCH.md §"API 3: Health % Calculation" — 公式与 min(100.0, ...) 边界处理
    - RESEARCH.md §"Common Pitfalls" P1–P8 — 所有实现陷阱（takeUnretainedValue、IOObjectRelease、MaxCapacity 错误、Amperage 符号、时间哨兵、post-wake 陈旧值）
    - PATTERNS.md §"BatteryReader.swift" — IOKit defer release、probe-and-nil、readValue() 整体结构、import 块、wake observer + deinit
    - GPUReader.swift 第 1–3 行（import 块），第 55/60–62 行（IOObjectRelease defer 模式），第 127–155 行（readValue() 同步结构）
    - NetworkReader.swift 第 44 行（wakeObserver 声明），第 55–67 行（setup() 注册观察器），第 107–111 行（deinit 清理）
  </read_first>

  <behavior>
    - BatterySnapshot: Sendable, Equatable struct 有 8 个字段，全部值类型：chargePercent: Int，isCharging: Bool，isOnAC: Bool，timeToEmptyMinutes: Int?，timeToFullMinutes: Int?，watts: Double?，healthPercent: Double?，cycleCount: Int?
    - BatteryReader 是 final class（非 TimerReader 子类），有 private var wakeObserver: NSObjectProtocol?，private var postWakeSkipCount: Int = 0，private let postWakeSkipTotal = 3
    - readValue() 先读 Layer 1（Power Sources）：guard IOPSCopyPowerSourcesInfo / IOPSCopyPowerSourcesList 均不为 nil（takeRetainedValue()）；遍历 list 找 kIOPSTypeKey == kIOPSInternalBatteryType && kIOPSIsPresentKey == true 的源；guard chargePercent、isCharging、stateStr 均不为 nil，否则 return nil（台式机路径）
    - 时间字段：读 kIOPSTimeToEmptyKey as? Int 和 kIOPSTimeToFullChargeKey as? Int；若 postWakeSkipCount > 0，则递减并将两个时间字段强制为 nil
    - readValue() 再读 Layer 2（AppleSmartBattery）：IOServiceGetMatchingService(kIOMainPortDefault, IOServiceMatching("AppleSmartBattery"))；guard service != IO_OBJECT_NULL；defer { IOObjectRelease(service) }；IORegistryEntryCreateCFProperties + takeRetainedValue() as? [String: Any]；若 props 读取失败，返回仅含 Layer 1 数据的 BatterySnapshot（watts/health/cycles 全为 nil）
    - Watts 计算：guard props["Amperage"] as? Int 和 props["Voltage"] as? Int；magnitude = abs(Double(amp)) / 1000.0 * Double(volt) / 1000.0；若 magnitude < 0.1 则 watts = nil；否则 isCharging ? +magnitude : -magnitude
    - 健康度：guard props["AppleRawMaxCapacity"] as? Int 和 props["DesignCapacity"] as? Int 且 design > 0；healthPercent = min(100.0, Double(rawMax) / Double(design) * 100.0)
    - cycleCount = props["CycleCount"] as? Int（probe-and-nil，无强解包）
    - setup() 注册 NSWorkspace.shared.notificationCenter.addObserver(forName: NSWorkspace.didWakeNotification, ...) { [weak self] _ in self?.postWakeSkipCount = self?.postWakeSkipTotal ?? 3 }
    - deinit 调用 NSWorkspace.shared.notificationCenter.removeObserver(wakeObserver)
    - import 块：import Foundation、import IOKit、import IOKit.ps
  </behavior>

  <action>
    新建文件 MacStatus/MacStatus/Readers/BatteryReader.swift。

    文件顶部 import：Foundation、IOKit、IOKit.ps（per RESEARCH.md §Standard Stack 与 PATTERNS.md §Import block）。

    先定义 BatterySnapshot: Sendable, Equatable struct，字段顺序与 RESEARCH.md "Complete BatterySnapshot Struct" 一致，8 个字段全部值类型（chargePercent: Int 非 Optional，因为走到此处必有电池；isCharging: Bool；isOnAC: Bool；timeToEmptyMinutes: Int?；timeToFullMinutes: Int?；watts: Double?；healthPercent: Double?；cycleCount: Int?）。

    然后定义 BatteryReader final class（不继承 TimerReader，per RESEARCH.md Primary Recommendation）。私有属性：wakeObserver: NSObjectProtocol?，postWakeSkipCount: Int = 0，postWakeSkipTotal = 3（per PATTERNS.md §Wake observer pattern）。

    readValue() -> BatterySnapshot? 分两层（per RESEARCH.md §"readValue() Core Pattern"）：

    Layer 1 Power Sources：
    - guard IOPSCopyPowerSourcesInfo()?.takeRetainedValue() 和 IOPSCopyPowerSourcesList(info)?.takeRetainedValue() as? [CFTypeRef]
    - 遍历 list，对每个 source 调用 IOPSGetPowerSourceDescription(info, source)?.takeUnretainedValue()（per RESEARCH.md Pitfall 2——绝不用 takeRetainedValue），cast as? [String: Any]
    - 筛选条件：desc[kIOPSTypeKey] as? String == kIOPSInternalBatteryType 且 desc[kIOPSIsPresentKey] as? Bool == true
    - guard chargePercent（kIOPSCurrentCapacityKey as? Int）、isCharging（kIOPSIsChargingKey as? Bool）、stateStr（kIOPSPowerSourceStateKey as? String）均不为 nil，否则 return nil（无内置电池 = 台式机）
    - isOnAC = (stateStr == kIOPSACPowerValue)
    - 读时间字段（均为 as? Int，绝不强解包），然后 if postWakeSkipCount > 0 { postWakeSkipCount -= 1; timeToEmpty = nil; timeToFull = nil }

    Layer 2 AppleSmartBattery：
    - IOServiceGetMatchingService(kIOMainPortDefault, IOServiceMatching("AppleSmartBattery"))；guard service != IO_OBJECT_NULL，否则返回仅含 Layer 1 字段的 BatterySnapshot（watts/health/cycles 为 nil）（per PATTERNS.md §IOKit service acquisition + defer release 与 RESEARCH.md API 4）
    - defer { IOObjectRelease(service) } 紧跟 guard 之后（per RESEARCH.md Pitfall 6）
    - var propsRef: Unmanaged<CFMutableDictionary>?，IORegistryEntryCreateCFProperties(service, &propsRef, kCFAllocatorDefault, 0) == KERN_SUCCESS，props = propsRef?.takeRetainedValue() as? [String: Any]（per PATTERNS.md §probe-and-nil IORegistry property read pattern）
    - Watts：per RESEARCH.md §"Recommended safe Watts calculation"——guard props["Amperage"] as? Int 和 props["Voltage"] as? Int；magnitude = abs(Double(amp)) / 1000.0 * Double(volt) / 1000.0；magnitude < 0.1 则 watts = nil，否则用 isCharging 决定符号（per CONTEXT.md "研究阶段澄清" + RESEARCH.md Note 2）
    - Health：per RESEARCH.md §"API 3"——guard props["AppleRawMaxCapacity"] as? Int 和 props["DesignCapacity"] as? Int 且 design > 0；min(100.0, Double(rawMax) / Double(design) * 100.0)（绝不用 "MaxCapacity"，per RESEARCH.md Pitfall 1 + CONTEXT.md 研究阶段澄清）
    - cycleCount = props["CycleCount"] as? Int

    setup() 注册 NSWorkspace.didWakeNotification 观察器，用 [weak self] 捕获（per PATTERNS.md §Wake observer + deinit cleanup pattern + shared patterns §weak self）。

    deinit 移除观察器（per NetworkReader.swift 107–111 行模式）。

    Swift 6 严格并发：BatterySnapshot 所有字段为值类型，自动满足 Sendable；readValue() 是同步方法，从 @MainActor tick 调用安全（per CONTEXT.md §Concurrency）。
  </action>

  <verify>
    <automated>cd /Users/halo/work/my-project/mac-status && xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build.noindex build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    源文件断言（grep 验证）：
    - BatteryReader.swift 包含 struct BatterySnapshot：`grep -c "struct BatterySnapshot" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 1
    - 包含 Sendable 声明：`grep -c "Sendable" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 ≥ 1
    - 包含 IOPSCopyPowerSourcesInfo：`grep -c "IOPSCopyPowerSourcesInfo" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 ≥ 1
    - 包含 takeUnretainedValue（IOPS GetPowerSourceDescription，非 takeRetainedValue）：`grep -c "takeUnretainedValue" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 ≥ 1
    - 包含 AppleSmartBattery：`grep -c "AppleSmartBattery" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 ≥ 1
    - 包含 IOObjectRelease：`grep -c "IOObjectRelease" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 ≥ 1
    - 包含 defer：`grep -c "defer" MacStatus/MacStatus/Readers/BatteryReader.swift` 或文件中 defer 与 IOObjectRelease 相关
    - 包含 AppleRawMaxCapacity（非 "MaxCapacity" 用于健康度）：`grep -c "AppleRawMaxCapacity" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 ≥ 1
    - 不包含强解包（as!）：`grep -c " as! " MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 0
    - 包含 postWakeSkipCount：`grep -c "postWakeSkipCount" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 ≥ 1
    - 包含 didWakeNotification：`grep -c "didWakeNotification" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 ≥ 1
    - 包含 func readValue：`grep -c "func readValue" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 ≥ 1
    - xcodebuild 返回 exit 0（BUILD SUCCEEDED）
    - 行为验证（运行时，仅限笔记本）：在笔记本上启动 app 后，BatteryReader().readValue() 应返回非 nil，chargePercent 在 0–100 范围内；台式机上返回 nil
  </acceptance_criteria>

  <done>
    BatteryReader.swift 存在；包含 BatterySnapshot struct（8 字段，全值类型）；BatteryReader class 有 readValue() -> BatterySnapshot?、setup()、deinit；probe-and-nil 贯穿所有 IOKit 键读取；IOObjectRelease 在 defer 中；takeUnretainedValue 用于 GetPowerSourceDescription；AppleRawMaxCapacity 用于健康度分母；postWakeSkipCount 机制在位；xcodebuild 编译通过
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| macOS IOKit → BatteryReader | 本地内核数据，无用户输入，无网络，无凭证 |

## STRIDE 威胁登记表

| 威胁 ID | 类别 | 组件 | 处置 | 缓解措施 |
|---------|------|------|------|---------|
| T-07-01 | Information Disclosure | IOKit Power Sources / AppleSmartBattery | 接受 | 数据为本地只读电池遥测（非 PII），Apple 公开 API，无新沙盒权限 |
| T-07-02 | Tampering / Crash | BatteryReader.readValue() | 缓解 | probe-and-nil（as?）防御性转换——IOKit 返回格式异常的注册表值时字段降级为 nil，不崩溃；彻底消除强解包（as!） |
| T-07-03 | Denial of Service | Mach 端口泄漏 | 缓解 | defer { IOObjectRelease(service) } 紧跟 guard；每 tick 释放，防止 ~43,200 个/天端口泄漏 |
| T-07-04 | Tampering | CF 内存管理（over-release） | 缓解 | IOPSGetPowerSourceDescription 使用 takeUnretainedValue()；Copy* 函数使用 takeRetainedValue()；经 SDK 头文件核实，防止 EXC_BAD_ACCESS |
| T-07-SC | Tampering | npm/pip/cargo 安装 | 不适用 | 本阶段无外部包安装；所有 API 均为 Apple SDK（IOKit、Foundation） |

无高严重性威胁；所有威胁已缓解或影响极低。
</threat_model>

<verification>
整体验证（Plan 01 完成时）：
1. `xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build.noindex build 2>&1 | grep -E "BUILD (SUCCEEDED|FAILED)"` 输出 BUILD SUCCEEDED
2. `grep -c "struct BatterySnapshot" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 1
3. `grep -c " as! " MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 0（零强解包）
4. `grep -c "takeUnretainedValue" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 ≥ 1
5. `grep -c "AppleRawMaxCapacity" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 ≥ 1（健康度公式正确）
6. `grep -c "IOObjectRelease" MacStatus/MacStatus/Readers/BatteryReader.swift` 输出 ≥ 1（防端口泄漏）
</verification>

<success_criteria>
- BatteryReader.swift 已创建，含 BatterySnapshot struct（8 字段）与 BatteryReader class
- readValue() 实现双层 IOKit 读取（Power Sources + AppleSmartBattery）
- 所有 IOKit 键读取均为 probe-and-nil（as?），零强解包
- CF 内存管理正确（takeUnretainedValue / takeRetainedValue / IOObjectRelease defer）
- 健康度公式：AppleRawMaxCapacity / DesignCapacity × 100（非 MaxCapacity）
- Watts 符号来源：kIOPSIsChargingKey（非 Amperage 符号）
- 充电三态逻辑：不依赖 kIOPSIsChargedKey
- Sleep/wake 恢复：postWakeSkipCount=3，唤醒后 3 tick 内时间字段为 nil（显示 计算中）
- 台式机（无 AppleSmartBattery 服务）：readValue() 返回 nil
- xcodebuild 编译通过，exit 0
- 覆盖需求：BATT-01（充电状态数据层）、BATT-02（时间估计数据层）、BATT-03（Watts 数据层）、BATT-04（健康度+循环次数数据层）、BATT-05（nil 降级数据层）
</success_criteria>

<artifacts>
## 本 Plan 产出的符号

| 符号 | 类型 | 文件 | 说明 |
|------|------|------|------|
| `BatterySnapshot` | struct | BatteryReader.swift | Sendable + Equatable，8 字段值类型；Plan 02 的 DashboardState 与 MetricCollector 消费此类型 |
| `BatterySnapshot.chargePercent` | Int | BatteryReader.swift | 来自 kIOPSCurrentCapacityKey，0–100% |
| `BatterySnapshot.isCharging` | Bool | BatteryReader.swift | 来自 kIOPSIsChargingKey |
| `BatterySnapshot.isOnAC` | Bool | BatteryReader.swift | 来自 kIOPSPowerSourceStateKey == "AC Power" |
| `BatterySnapshot.timeToEmptyMinutes` | Int? | BatteryReader.swift | 来自 kIOPSTimeToEmptyKey；nil=post-wake；-1=计算中 |
| `BatterySnapshot.timeToFullMinutes` | Int? | BatteryReader.swift | 来自 kIOPSTimeToFullChargeKey；nil=post-wake；-1=计算中 |
| `BatterySnapshot.watts` | Double? | BatteryReader.swift | 带符号瓦数；nil=键缺失或<0.1W |
| `BatterySnapshot.healthPercent` | Double? | BatteryReader.swift | AppleRawMaxCapacity/DesignCapacity×100；nil=键缺失 |
| `BatterySnapshot.cycleCount` | Int? | BatteryReader.swift | 来自 "CycleCount"；nil=键缺失 |
| `BatteryReader` | final class | BatteryReader.swift | 非 TimerReader 子类；由 MetricCollector 同步调用 |
| `BatteryReader.readValue()` | func | BatteryReader.swift | -> BatterySnapshot?（nil=台式机/无电池） |
| `BatteryReader.setup()` | func | BatteryReader.swift | 注册 didWakeNotification 观察器 |
</artifacts>

<output>
完成后创建 `.planning/phases/07-battery-power/07-01-SUMMARY.md`
</output>
