---
phase: "07-battery-power"
plan: "02"
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - MacStatus/MacStatus/Collectors/MetricCollector.swift
  - MacStatus/MacStatus/UI/Views/DashboardView.swift
autonomous: true
requirements:
  - BATT-01
  - BATT-02
  - BATT-03
  - BATT-04
  - BATT-05

must_haves:
  truths:
    - "MetricCollector.start() 调用 batteryReader.setup() 和首次 _ = batteryReader.readValue()，与其他 reader 同批"
    - "MetricCollector.tick() 调用 let battery = batteryReader.readValue()，结果存入 lastBatterySnapshot（非 MetricSample / RingBuffer / HistoryStore）"
    - "MetricCollector.updateUI(sample:) 调用 dashboard.updateBattery(lastBatterySnapshot)，不向 StatusBarManager.updateTitle 传递电池数据"
    - "MetricCollector.reconfigure() 不触碰 batteryReader（唤醒观察器跨计时器重配置持续存活）"
    - "DashboardState 新增 @Published var hasBattery: Bool = false 和 @Published var battery: BatterySnapshot? = nil"
    - "DashboardState.updateBattery(_:) 设置 battery = snapshot 和 hasBattery = snapshot != nil"
    - "DashboardView body 在 LazyVGrid 结束括号与 ProcessListView 之间条件插入 if state.hasBattery { BatterySectionView(snapshot: state.battery) }"
    - "BatterySectionView 是同文件 private struct，渲染：充电态文案（充电中/已充满/使用电池）、时间行（充电中→距充满，使用电池→剩余，格式 X小时Y分/Y分钟/计算中）、功率行（+18.5W/−12.3W/—）、健康度行（健康度 92%（XXX 次循环）/—）"
    - "BatterySectionView 使用与 MetricCardWithSparkline 一致的卡片背景样式（RoundedRectangle cornerRadius 8，Color.primary.opacity(0.04)）"
    - "字段级降级：笔记本上 watts/healthPercent/cycleCount 任一为 nil 时对应行显示 —，不隐藏整个 section（整段隐藏仅在 hasBattery=false 即台式机时）"
    - "台式机（hasBattery=false）：DashboardView 不渲染任何电池内容，无空字段、无零/占位符"
    - "StatusBarManager 的 case .battery: break 保持不变，.battery 不出现在 enabledMetrics/metricOrder"
    - "xcodebuild 编译成功，exit 0"
  artifacts:
    - path: "MacStatus/MacStatus/Collectors/MetricCollector.swift"
      provides: "batteryReader 集成（setup/tick/updateUI，不含 reconfigure）"
      contains: "batteryReader"
    - path: "MacStatus/MacStatus/UI/Views/DashboardView.swift"
      provides: "DashboardState 电池 @Published 字段 + updateBattery + BatterySectionView"
      contains: "BatterySectionView"
  key_links:
    - from: "MetricCollector.tick()"
      to: "BatteryReader.readValue()"
      via: "batteryReader.readValue()"
      pattern: "batteryReader\\.readValue"
    - from: "MetricCollector.updateUI()"
      to: "DashboardState.updateBattery(_:)"
      via: "dashboard.updateBattery"
      pattern: "dashboard\\.updateBattery"
    - from: "DashboardView body"
      to: "BatterySectionView"
      via: "if state.hasBattery"
      pattern: "state\\.hasBattery"
    - from: "BatterySectionView"
      to: "BatterySnapshot fields"
      via: "snapshot?.chargePercent / .watts / .healthPercent / .cycleCount"
      pattern: "BatterySectionView"
---

<objective>
将 Plan 01 产出的 `BatteryReader` / `BatterySnapshot` 接入 MetricCollector，并在 DashboardView 中新增电池显示区。

修改三处：
1. `MetricCollector.swift` — 声明 batteryReader，在 start()/tick()/updateUI() 中接入，reconfigure() 不改动
2. `DashboardView.swift` — DashboardState 新增 @Published 电池字段 + updateBattery()；DashboardView body 插入 BatterySectionView；新建 BatterySectionView private struct（含充电态、时间、功率、健康度行，字段级 — 降级）

Purpose: 打通数据流（Reader → MetricCollector → DashboardState → SwiftUI），使电池数据在 popover 中可见；台式机整段隐藏。

Output: 修改后的 MetricCollector.swift 与 DashboardView.swift，笔记本 popover 中出现电池区块，台式机不显示任何内容，xcodebuild 编译通过。
</objective>

<execution_context>
@/Users/halo/work/my-project/mac-status/.claude/gsd-core/workflows/execute-plan.md
@/Users/halo/work/my-project/mac-status/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@/Users/halo/work/my-project/mac-status/.planning/ROADMAP.md
@/Users/halo/work/my-project/mac-status/.planning/phases/07-battery-power/07-RESEARCH.md
@/Users/halo/work/my-project/mac-status/.planning/phases/07-battery-power/07-PATTERNS.md
@/Users/halo/work/my-project/mac-status/MacStatus/MacStatus/Collectors/MetricCollector.swift
@/Users/halo/work/my-project/mac-status/MacStatus/MacStatus/UI/Views/DashboardView.swift
@/Users/halo/work/my-project/mac-status/.planning/phases/07-battery-power/07-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MetricCollector 接入 batteryReader</name>
  <files>MacStatus/MacStatus/Collectors/MetricCollector.swift</files>

  <read_first>
    - MetricCollector.swift 第 29–32 行（reader 声明区，在 gpuReader 后新增 batteryReader）
    - MetricCollector.swift 第 54–65 行（start() — cpuReader.setup() + _ = readValue() 区块；在 gpuReader 之后同批添加）
    - MetricCollector.swift 第 97–106 行（reconfigure()——不得修改此方法，仅供参考）
    - MetricCollector.swift 第 140–178 行（tick()——let gpu = gpuReader.readValue() 之后新增 let battery = batteryReader.readValue()；MetricSample 初始化不添加电池字段）
    - MetricCollector.swift 第 180–220 行（updateUI(sample:)——dashboard.updateGPU 调用之后新增 dashboard.updateBattery；不向 StatusBarManager.updateTitle 传递电池数据）
    - PATTERNS.md §MetricCollector §"Reader property declaration"（第 29–32 行模式）
    - PATTERNS.md §MetricCollector §"setup() + first read pattern"（第 54–65 行模式）
    - PATTERNS.md §MetricCollector §"tick() read pattern"（第 143–147 行模式）
    - PATTERNS.md §MetricCollector §"updateUI() push pattern"（battery 用 lastBatterySnapshot 私有属性方案）
    - PATTERNS.md §MetricCollector §"reconfigure() — do NOT touch batteryReader"
    - RESEARCH.md §"MetricCollector Integration (minimal diff)"（架构图中 tick/updateUI 路径）
  </read_first>

  <behavior>
    - 第 33 行（gpuReader 声明之后）：private let batteryReader = BatteryReader()
    - 新增私有属性：private var lastBatterySnapshot: BatterySnapshot? = nil（供 updateUI 引用，与 lastSample 类比）
    - start() 中：batteryReader.setup() 加入 setup 区块；_ = batteryReader.readValue() 加入首读区块（与 gpuReader 同批）
    - tick() 中：let battery = batteryReader.readValue() 紧跟 let gpu = 之后；lastBatterySnapshot = battery；MetricSample 初始化不添加任何电池字段
    - updateUI(sample:) 中：dashboard.updateBattery(lastBatterySnapshot) 紧跟 dashboard.updateGPU(...)之后；StatusBarManager.updateTitle 调用不变（不传电池参数）
    - reconfigure() 保持原样，不添加任何 batteryReader 调用
    - applyNow() 调用 updateUI(sample:)，故 lastBatterySnapshot 可在不触发 readValue() 的前提下重新推送电池数据
  </behavior>

  <action>
    编辑 MacStatus/MacStatus/Collectors/MetricCollector.swift，做四处最小化改动（per PATTERNS.md §MetricCollector 各节）：

    1. Reader 声明区（第 32 行之后）：新增 `private let batteryReader = BatteryReader()`。同一行风格，与 gpuReader 对齐。

    2. 同一区域新增私有状态属性：`private var lastBatterySnapshot: BatterySnapshot? = nil`（与 lastSample 相邻，方便 updateUI 引用）。

    3. start() 方法：
       - setup 区块末尾添加 `batteryReader.setup()`（per PATTERNS.md §setup pattern，第 59 行后）
       - 首读区块末尾添加 `_ = batteryReader.readValue()`（per PATTERNS.md，第 65 行后）

    4. tick() 方法（第 147 行，let gpu = 之后）：
       - 添加 `let battery = batteryReader.readValue()`
       - 添加 `lastBatterySnapshot = battery`
       - MetricSample 初始化行不改动（电池不进入 MetricSample/RingBuffer/HistoryStore，per CONTEXT.md）

    5. updateUI(sample:)（dashboard.updateGPU 调用之后）：
       - 添加 `dashboard.updateBattery(lastBatterySnapshot)`（per PATTERNS.md §updateUI push pattern 与 RESEARCH.md 架构图）
       - StatusBarManager.updateTitle 调用不变

    reconfigure() 方法不改动，确保唤醒观察器跨计时器重配置持续存活（per CONTEXT.md 锁定决策与 PATTERNS.md §reconfigure）。
  </action>

  <verify>
    <automated>cd /Users/halo/work/my-project/mac-status && xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build.noindex build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    源文件断言：
    - `grep -c "batteryReader" MacStatus/MacStatus/Collectors/MetricCollector.swift` 输出 ≥ 4（声明 + setup + readValue + updateBattery 至少 4 处）
    - `grep -c "lastBatterySnapshot" MacStatus/MacStatus/Collectors/MetricCollector.swift` 输出 ≥ 2（赋值 + 使用）
    - `grep -c "dashboard.updateBattery" MacStatus/MacStatus/Collectors/MetricCollector.swift` 输出 1
    - reconfigure() 不含 batteryReader：`grep -A 15 "func reconfigure" MacStatus/MacStatus/Collectors/MetricCollector.swift | grep -c "batteryReader"` 输出 0
    - MetricSample 初始化不含 battery：`grep -A 8 "let sample = MetricSample" MacStatus/MacStatus/Collectors/MetricCollector.swift | grep -c "battery"` 输出 0
    - xcodebuild 返回 exit 0
  </acceptance_criteria>

  <done>
    MetricCollector.swift 包含 batteryReader 声明、lastBatterySnapshot 属性、start() 中的 setup/首读、tick() 中的 readValue/缓存、updateUI 中的 dashboard.updateBattery 调用；reconfigure() 无改动；编译通过
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DashboardState 电池字段 + DashboardView 电池 section</name>
  <files>MacStatus/MacStatus/UI/Views/DashboardView.swift</files>

  <read_first>
    - DashboardView.swift 第 154–263 行（完整 DashboardState class——在 GPU 区块 173–175 行之后新增 battery 字段）
    - DashboardView.swift 第 240–249 行（updateGPU 模式——updateBattery 遵循同一 guard-let 形态）
    - DashboardView.swift 第 10–95 行（DashboardView body VStack——LazyVGrid 第 21–59 行、ProcessListView 第 62–66 行；电池 section 插入两者之间）
    - DashboardView.swift 第 98–150 行（MetricCardWithSparkline——卡片背景样式 .padding(10) + RoundedRectangle(cornerRadius: 8).fill(Color.primary.opacity(0.04))；BatterySectionView 复用此样式）
    - PATTERNS.md §DashboardView §"@Published field declaration"（GPU 区块后新增 hasBattery + battery）
    - PATTERNS.md §DashboardView §"updateGPU nil-degradation update method"（updateBattery 采用同一 guard 结构但不 appendSample）
    - PATTERNS.md §DashboardView §"Card background style"（RoundedRectangle(cornerRadius: 8).fill(Color.primary.opacity(0.04))）
    - PATTERNS.md §DashboardView §"DashboardView body layout insertion point"（LazyVGrid 59 行后、ProcessListView 62 行前插入 if state.hasBattery）
    - PATTERNS.md §DashboardView §"Footer HStack row style"（HStack { Text(label).foregroundStyle(.secondary) Spacer() Text(value).font(.system(.body, design: .monospaced)) } 模式）
    - RESEARCH.md §"DashboardState.updateBattery(_:) Pattern"（精确方法定义）
    - RESEARCH.md §"Time Formatting Helper"（formatTimeRemaining，哨兵 -1→计算中，0→—，>0→X小时Y分/Y分钟，nil→计算中）
    - CONTEXT.md §"展示与格式"（中文文案：充电中/已充满/使用电池；+18.5W/−12.3W；健康度 92%（XXX 次循环）；不可读→—）
    - CONTEXT.md §"研究阶段澄清" §"|watts| < 0.1 → —"
  </read_first>

  <behavior>
    DashboardState 新增字段：
    - @Published var hasBattery: Bool = false（GPU 区块之后）
    - @Published var battery: BatterySnapshot? = nil

    DashboardState 新增方法 updateBattery(_ snapshot: BatterySnapshot?)：
    - battery = snapshot；hasBattery = snapshot != nil（无 appendSample 无 guard-let，per PATTERNS.md §updateBattery）

    DashboardView body：
    - LazyVGrid 结束括号之后、ProcessListView 之前插入：if state.hasBattery { BatterySectionView(snapshot: state.battery) }

    BatterySectionView private struct：
    - 接受 snapshot: BatterySnapshot? 参数
    - 使用卡片背景样式（RoundedRectangle cornerRadius 8，Color.primary.opacity(0.04)，.padding(10)）
    - 行 1 充电态：Text("电池") label + Text(chargeStateText) value；chargeStateText 来自 snapshot?.isCharging==true→"充电中 {chargePercent}%"，isOnAC&&!isCharging→"已充满 {chargePercent}%"，Battery Power→"使用电池 {chargePercent}%"；snapshot==nil→"—"
    - 行 2 时间：Text(timeLabel) label（充电中→"距充满"，使用电池→"剩余"，已充满→隐藏此行或显示"—"）+ Text(formatTimeRemaining(...)) value；formatTimeRemaining nil→"计算中"，-1→"计算中"，0→"—"，>0→"X小时Y分"/"Y分钟"
    - 行 3 功率（watts）：Text("功率") label + Text(wattsText) value；wattsText：nil→"—"；watts>=0.1→"+{String(format: "%.1f", watts)}W"；watts<-0.1→"−{String(format: "%.1f", abs(watts))}W"；|watts|<0.1→"—"（per CONTEXT.md 研究阶段澄清）
    - 行 4 健康度：Text("健康度") label + Text(healthText) value；healthText：healthPercent 和 cycleCount 均不为 nil→"健康度 {Int(healthPercent)}%（{cycleCount} 次循环）"；任一为 nil→"—"
    - 每行使用 HStack { label Spacer() value } 风格，label .font(.caption).foregroundStyle(.secondary)，value .font(.system(.body, design: .monospaced)).fontWeight(.medium)
    - 整段已充满时，时间行可以只显示"—"（无剩余时间/充满时间可显示），不报错
  </behavior>

  <action>
    编辑 MacStatus/MacStatus/UI/Views/DashboardView.swift，三处改动：

    **改动 1 — DashboardState 新增电池 @Published 字段**（第 174–175 行 GPU 区块之后，per PATTERNS.md §@Published field declaration）：
    新增注释 `// Battery` 区块，包含 `@Published var hasBattery: Bool = false` 和 `@Published var battery: BatterySnapshot? = nil`。

    **改动 2 — DashboardState 新增 updateBattery 方法**（updateGPU 方法之后，per PATTERNS.md §updateGPU nil-degradation 与 RESEARCH.md §updateBattery Pattern）：
    ```
    func updateBattery(_ snapshot: BatterySnapshot?) {
        battery = snapshot
        hasBattery = snapshot != nil
    }
    ```
    此方法不调用 appendSample（电池无 sparkline，per CONTEXT.md）。

    **改动 3 — DashboardView body 插入电池 section + 新建 BatterySectionView**：

    在 LazyVGrid 闭合括号（第 59 行）之后、ProcessListView 之前（第 62 行），插入：
    ```
    if state.hasBattery {
        BatterySectionView(snapshot: state.battery)
    }
    ```

    在 DashboardView.swift 文件末尾（DashboardState class 之前或之后，与 MetricCardWithSparkline 同级），新建 `private struct BatterySectionView: View`（per PATTERNS.md §DashboardView body layout insertion point）。

    BatterySectionView 内部：
    - 在 body 内使用 VStack(alignment: .leading, spacing: 6)，外层使用卡片背景（.padding(10) + RoundedRectangle(cornerRadius: 8).fill(Color.primary.opacity(0.04))），per PATTERNS.md §Card background style
    - 定义 private func chargeStateText(snapshot: BatterySnapshot?) -> String，实现充电三态（per CONTEXT.md §充电三态规则 + RESEARCH.md §Charging state mapping）：isCharging→"充电中 {N}%"；isOnAC&&!isCharging→"已充满 {N}%"；Battery Power→"使用电池 {N}%"；snapshot nil→"—"
    - 定义 private func formatTimeRemaining(_ minutes: Int?) -> String（per RESEARCH.md §Time Formatting Helper）：nil→"计算中"；-1→"计算中"；0→"—"；>0→hrs>0 ? "{h}小时{m}分" : "{m}分钟"；default→"—"
    - 定义 private func wattsText(snapshot: BatterySnapshot?) -> String：snapshot?.watts==nil→"—"；abs(watts)<0.1→"—"；watts>0→"+{format %.1f}W"；watts<0→"−{format %.1f abs}W"（per CONTEXT.md 研究阶段澄清）
    - 定义 private func healthText(snapshot: BatterySnapshot?) -> String：两者均不为 nil→"健康度 {Int(healthPercent)}%（{cycleCount} 次循环）"；否则"—"（per CONTEXT.md §健康度格式）
    - 渲染 4 行，每行 `HStack { Text(label).font(.caption).foregroundStyle(.secondary); Spacer(); Text(value).font(.system(.body, design: .monospaced)).fontWeight(.medium) }`（per PATTERNS.md §Footer HStack row style 与 §Metric card header row style）
    - 时间行 label 动态：isCharging→"距充满"，Battery Power→"剩余"，已充满→"充电时间"；value = formatTimeRemaining(相应时间字段)
    - 功率行 label = "功率"；value = wattsText(snapshot)
    - 健康度行 label = "健康度"；value = healthText(snapshot)

    不修改 StatusBarManager 相关任何代码（per CONTEXT.md §作用域：popover only）。
    不向 Metric 枚举的 enabledMetrics/metricOrder 添加 .battery（per CONTEXT.md Deferred 与 STATE.md 约束）。
  </action>

  <verify>
    <automated>cd /Users/halo/work/my-project/mac-status && xcodebuild -project MacStatus/MacStatus.xcodeproj -scheme MacStatus -configuration Debug -derivedDataPath build.noindex build 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    源文件断言：
    - `grep -c "hasBattery" MacStatus/MacStatus/UI/Views/DashboardView.swift` 输出 ≥ 2（声明 + 条件渲染）
    - `grep -c "BatterySectionView" MacStatus/MacStatus/UI/Views/DashboardView.swift` 输出 ≥ 2（定义 + 使用）
    - `grep -c "func updateBattery" MacStatus/MacStatus/UI/Views/DashboardView.swift` 输出 1
    - `grep -c "if state.hasBattery" MacStatus/MacStatus/UI/Views/DashboardView.swift` 输出 1
    - `grep -c "充电中\|已充满\|使用电池" MacStatus/MacStatus/UI/Views/DashboardView.swift` 输出 ≥ 1（中文文案）
    - `grep -c "计算中" MacStatus/MacStatus/UI/Views/DashboardView.swift` 输出 ≥ 1（时间哨兵文案）
    - `grep -c "healthPercent\|cycleCount" MacStatus/MacStatus/UI/Views/DashboardView.swift` 输出 ≥ 1（健康度行）
    - xcodebuild 返回 exit 0

    行为验证（在笔记本上运行 app，打开 popover）：
    - 笔记本：popover 中出现电池区块，位于 2x2 指标网格与进程列表之间；显示充电中/已充满/使用电池 + 充电百分比；显示时间行（计算中 或 X小时Y分 或 Y分钟）；显示功率（+N.NW 或 −N.NW 或 —）；显示健康度行（健康度 N%（N 次循环） 或 —）；卡片背景与其他指标卡一致
    - 台式机（hasBattery=false）：popover 无任何电池内容，无空字段，无占位符
    - 笔记本睡眠后唤醒约 6 秒内：时间行显示"计算中"，其余字段正常显示
  </acceptance_criteria>

  <done>
    DashboardView.swift 包含 @Published hasBattery/battery 字段、updateBattery 方法、if state.hasBattery 条件插入、BatterySectionView struct（含 4 行中文标签/值、字段级降级逻辑、卡片背景样式）；xcodebuild 编译通过；笔记本 popover 中电池区块可见且内容正确；台式机无内容显示
  </done>
</task>

</tasks>

<threat_model>
## 信任边界

| 边界 | 描述 |
|------|------|
| BatteryReader → MetricCollector | Sendable BatterySnapshot 跨 actor 传递（值类型，安全） |
| MetricCollector → DashboardState | @MainActor 内部调用，无跨线程边界 |
| DashboardState → SwiftUI | @Published ObservableObject，SwiftUI 主线程渲染 |

## STRIDE 威胁登记表

| 威胁 ID | 类别 | 组件 | 处置 | 缓解措施 |
|---------|------|------|------|---------|
| T-07-05 | Information Disclosure | DashboardView 电池数据渲染 | 接受 | 数据仅显示于本地 popover，无网络传输，无持久化（per CONTEXT.md：不写 MetricSample/RingBuffer/HistoryStore） |
| T-07-06 | Tampering | MetricSample / RingBuffer 完整性 | 缓解 | 电池数据通过 lastBatterySnapshot 独立路径流动，不注入 MetricSample；已有的 MetricSample 结构不变 |
| T-07-07 | Denial of Service | SwiftUI 渲染死循环 | 缓解 | hasBattery=false 时 BatterySectionView 完全不渲染（if state.hasBattery 条件守卫）；字段级降级用 "—" 字符串，无 nil 强解包路径 |
| T-07-SC | Tampering | npm/pip/cargo 安装 | 不适用 | 本阶段无外部包安装；修改仅限现有 Apple SDK 代码 |
</threat_model>

<verification>
整体验证（Plan 02 完成时，即本阶段全部完成）：

**编译验证：**
```
cd /Users/halo/work/my-project/mac-status && \
xcodebuild -project MacStatus/MacStatus.xcodeproj \
  -scheme MacStatus -configuration Debug \
  -derivedDataPath build.noindex build 2>&1 | \
  grep -E "BUILD (SUCCEEDED|FAILED)"
```
预期：BUILD SUCCEEDED

**源断言（全部 5 个 BATT 需求覆盖检验）：**
- BATT-01（充电状态）：`grep -c "充电中\|已充满\|使用电池" MacStatus/MacStatus/UI/Views/DashboardView.swift` ≥ 1
- BATT-02（时间估计）：`grep -c "计算中\|formatTimeRemaining\|timeToEmpty\|timeToFull" MacStatus/MacStatus/UI/Views/DashboardView.swift` ≥ 1
- BATT-03（功率）：`grep -c "watts\|功率" MacStatus/MacStatus/UI/Views/DashboardView.swift` ≥ 1
- BATT-04（健康度+循环次数）：`grep -c "healthPercent\|cycleCount\|健康度" MacStatus/MacStatus/UI/Views/DashboardView.swift` ≥ 1
- BATT-05（台式机隐藏）：`grep -c "hasBattery" MacStatus/MacStatus/UI/Views/DashboardView.swift` ≥ 2

**架构约束验证：**
- 电池不进 MetricSample：`grep -A 8 "let sample = MetricSample" MacStatus/MacStatus/Collectors/MetricCollector.swift | grep -c "battery"` 输出 0
- StatusBar 不传电池：`grep -A 6 "StatusBarManager.shared.updateTitle" MacStatus/MacStatus/Collectors/MetricCollector.swift | grep -c "battery"` 输出 0
- reconfigure() 无 batteryReader：`grep -A 15 "func reconfigure" MacStatus/MacStatus/Collectors/MetricCollector.swift | grep -c "batteryReader"` 输出 0
</verification>

<success_criteria>
- MetricCollector.swift：batteryReader 已声明、setup/首读已加入 start()、tick() 读取并缓存到 lastBatterySnapshot、updateUI 调用 updateBattery、reconfigure 无改动
- DashboardView.swift：DashboardState 有 hasBattery/@Published battery/updateBattery；DashboardView 在正确位置条件插入 BatterySectionView；BatterySectionView 渲染 4 行（充电态 + 时间 + 功率 + 健康度），含正确中文文案，字段级降级为 "—"
- 笔记本：打开 popover → 电池区块位于 2x2 网格下方、进程列表上方，充电态/时间/功率/健康度行均有内容
- 台式机：打开 popover → 无任何电池内容，无空行
- sleep/wake 恢复：唤醒 ≤6s 内时间行显示"计算中"，之后恢复正常
- 状态栏：不含电池段，enabledMetrics/metricOrder 无 .battery，StatusBarManager 的 case .battery: break 保持不变
- xcodebuild 编译通过，exit 0
- 覆盖需求：BATT-01、BATT-02、BATT-03、BATT-04、BATT-05（全部 5 条）
</success_criteria>

<artifacts>
## 本 Plan 产出的符号

| 符号 | 类型 | 文件 | 说明 |
|------|------|------|------|
| `MetricCollector.batteryReader` | private let | MetricCollector.swift | BatteryReader 实例，在 start() 中 setup，在 tick() 中 readValue |
| `MetricCollector.lastBatterySnapshot` | private var | MetricCollector.swift | BatterySnapshot? 缓存，供 updateUI 使用（不在 MetricSample 中） |
| `DashboardState.hasBattery` | @Published Bool | DashboardView.swift | false→整段隐藏（台式机）；true→显示 BatterySectionView |
| `DashboardState.battery` | @Published BatterySnapshot? | DashboardView.swift | 最新电池快照，视图从此读取所有字段 |
| `DashboardState.updateBattery(_:)` | func | DashboardView.swift | 设置 battery/hasBattery，由 MetricCollector.updateUI 调用 |
| `BatterySectionView` | private struct View | DashboardView.swift | 全宽电池区块：4 行中文标签/值 + 字段级 — 降级 + 卡片背景 |
</artifacts>

<output>
完成后创建 `.planning/phases/07-battery-power/07-02-SUMMARY.md`
</output>
