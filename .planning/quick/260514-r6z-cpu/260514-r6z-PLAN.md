# Quick Task 260514-r6z: 修复 CPU 状态不显示和网络速度换行溢出

## Scope

Fix the visible menu bar behavior reported by the user:

- CPU status should remain visible as its own status item.
- Network speed text must stay on one line and must not spill vertically outside the menu bar.

## Tasks

1. Inspect status item setup, CPU reader wiring, and network formatting.
2. Apply a focused UI fix in the status bar manager/formatting code.
3. Build the macOS target to verify the Swift code still compiles.

